;****************************************
;縁結び系の処理に使う特殊関数
;****************************************
;・使い方
;「XXXX」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;****************************************

;--------------------------------------------------
;左キャラ時選択条件
;--------------------------------------------------
@縁結び特殊条件_左キャラ時_XXXX(対象キャラ)
#DIM 対象キャラ
;通常の条件とは別の条件を設定するためのもの

; RETURN 0 で通常の条件に戻す
; RETURN -1 で問答無用で選択出来なくする
; RETURN 1 で選択出来るようにする

;基本的に縁結び相手条件_XXXXと組み合わせて使うこと

RETURN 0

;この関数は通常のキャラは弄らなくて良いが、以下の条件に当てはまるキャラの場合はRETURN 1を返してやらないと選択出来ない
;「無性別である」「性欲なしである」「性知識が獣並or無知である」「セフレ不可があり、CSVで恋慕不可が設定されている」
;これらのキャラで縁結びしたい場合はこの関数でRETURN 1すること
;恋慕不可だけCSV設定を見ているのは、「恋慕不可が解除されてもそれは相手があなたの時だけで、他の人には相変わらず恋慕不可のはず」
;という想定。

;--------------------------------------------------
;右キャラ時選択条件
;--------------------------------------------------
@縁結び相手条件_恋慕_XXXX(実行キャラ, 対象キャラ)
#DIM 実行キャラ
#DIM 対象キャラ

;相手に選ぶ時、及び選ばれた時に進展するかどうかで使用

; RETURN 0 で通常の条件に戻す
; RETURN -1 で問答無用で選択出来なくする
; RETURN 1 で問答無用で選択出来るようにする

RETURN 0

;例えば、「特定の相手だけ恋慕可能で他のキャラは封印」の時は以下のようになる
; IF NAME:対象キャラ == "任意のキャラのNAME"
; 	RETURN 1
; ELSE
; 	RETURN -1
; ENDIF

;自由に縁結び出来るシステムで相手を固定するのは、恋慕不可素質との併用などが望ましい

@縁結び相手条件_セフレ_XXXX(実行キャラ, 対象キャラ)
#DIM 実行キャラ
#DIM 対象キャラ

;相手に選ぶ時、及び選ばれた時に進展するかどうかで使用

; RETURN 0 で通常の条件に戻す
; RETURN -1 で問答無用で選択出来なくする
; RETURN 1 で問答無用で選択出来るようにする

RETURN 0

;例えば、「特定の相手だけはセフレお断りで他のキャラは通常通り」の時は以下のようになる
; IF NAME:対象キャラ == "任意のキャラのNAME"
; 	RETURN -1
; ELSE
; 	RETURN 0
; ENDIF


;--------------------------------------------------
;縁結び進行時処理
;--------------------------------------------------
@縁結び進行固有補正_恋慕_実行側_XXXX(実行キャラ, 対象キャラ, 進行度)
#DIM 実行キャラ
#DIM 対象キャラ
#DIM REF 進行度
;縁結び進行時に、進行値に処理を行う関数
;主に進行度に補正を掛ける目的で使用


@縁結び進行固有補正_恋慕_対象側_XXXX(対象キャラ, 実行キャラ, 進行度)
#DIM 実行キャラ
#DIM 対象キャラ
#DIM REF 進行度
;縁結び進行時に、進行値に処理を行う関数
;主に進行度に補正を掛ける目的で使用


@縁結び進行固有補正_セフレ_実行側_XXXX(実行キャラ, 対象キャラ, 進行度)
#DIM 実行キャラ
#DIM 対象キャラ
#DIM REF 進行度
;縁結び進行時に、進行値に処理を行う関数
;主に進行度に補正を掛ける目的で使用


@縁結び進行固有補正_セフレ_対象側_XXXX(対象キャラ, 実行キャラ, 進行度)
#DIM 実行キャラ
#DIM 対象キャラ
#DIM REF 進行度
;縁結び進行時に、進行値に処理を行う関数
;主に進行度に補正を掛ける目的で使用
