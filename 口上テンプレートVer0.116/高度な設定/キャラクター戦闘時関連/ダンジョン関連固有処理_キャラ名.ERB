;****************************************
;各種タイミングで処理を割り込ませるための関数群
;****************************************
;・使い方
;「XXXX」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;****************************************


;--------------------------------------------------
;ダンジョン突入時処理
;--------------------------------------------------
@ダンジョン固有初期処理_XXXX(対象キャラ)
#DIM 対象キャラ

;ダンジョン突入時に発動する処理を記入
;隊列が必要な時は「キャラ隊列検索(対象キャラ)」で取得可能




;--------------------------------------------------
;ダンジョン終了時処理
;--------------------------------------------------
@ダンジョン固有終了処理_XXXX(対象キャラ)
#DIM 対象キャラ

;ダンジョン終了時に発動する処理を記入
;隊列が必要な時は「キャラ隊列検索(対象キャラ)」で取得可能






;--------------------------------------------------
;固有バフ・デバフアイコン
;--------------------------------------------------
@固有状態変化アイコン_XXXX(対象キャラ, 対象枠, バフ・デバフ)
#FUNCTIONS
#DIM 対象キャラ
#DIMS 対象枠
#DIMS バフ・デバフ
;この関数は「特定キャラが特定バフ・デバフを受けた時」の関数

SELECTCASE バフ・デバフ
	CASE "バフ"
		SELECTCASE 対象状態変化
			;バフ・デバフの対象枠を入力。対象能力じゃないので注意
			CASE "攻撃力A"
				RSTR:リソース名受け渡し = ;任意の画像アドレスを入力

				;アドレス入力時、フォルダ構造は\\で区切ること。拡張子までいれること
				;例：「1アニラ\\顔_デフォルト.webp」など
		ENDSELECT
	CASE "デバフ"
		SELECTCASE 対象状態変化
			CASE "攻撃力A"
				RSTR:リソース名受け渡し = ;任意の画像アドレスを入力
		ENDSELECT
ENDSELECT


@固有状態変化アイコン_YYYY(バフ・デバフ)
;YYYYは「攻撃力A」など、バフ・デバフの対象枠を入力。対象能力じゃないので注意
#FUNCTIONS
#DIMS バフ・デバフ
;この関数は「キャラ関係なく特定バフ・デバフを受けた時」の関数
;固有バフを全体に撒く時などを想定

;バフ時とデバフ時で分ける必要ない気がするけど一応分けられる
SELECTCASE バフ・デバフ
	CASE "バフ"
		RSTR:リソース名受け渡し = ;任意の画像アドレスを入力

		;アドレス入力時、フォルダ構造は\\で区切ること。拡張子までいれること
		;例：「1アニラ\\顔_デフォルト.webp」など
	CASE "デバフ"
		RSTR:リソース名受け渡し = ;任意の画像アドレスを入力
ENDSELECT
