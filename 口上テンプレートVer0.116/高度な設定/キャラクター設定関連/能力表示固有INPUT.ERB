;****************************************
;能力表示画面で任意のINPUTを機能させるための関数
;****************************************
;・使い方
;「XXXX」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;能力表示画面のキャラ固有領域にて事前に対応するボタン表示を用意すること！！
;****************************************
;これらの処理を行った後、能力表示画面はRESTARTする
;変数などを操作して表示を制御する機能を想定

;--------------------------------------------------
;通常能力画面
;--------------------------------------------------
@能力表示固有INPUT_通常能力_XXXX(対象キャラ, INPUT数値)
#DIM 対象キャラ
#DIM INPUT数値
;通常能力画面のボタン動作を入れる関数

RETURN 0

;--------------------------------------------------
;性的能力画面
;--------------------------------------------------
@能力表示固有INPUT_性的能力_XXXX(対象キャラ, INPUT数値)
#DIM 対象キャラ
#DIM INPUT数値
;性的能力画面のボタン動作を入れる関数

RETURN 0

;--------------------------------------------------
;戦闘能力画面
;--------------------------------------------------
@能力表示固有INPUT_戦闘能力_XXXX(対象キャラ, INPUT数値)
#DIM 対象キャラ
#DIM INPUT数値
;戦闘能力画面のボタン動作を入れる関数

RETURN 0

;--------------------------------------------------
;キャラ解説画面
;--------------------------------------------------
@能力表示固有INPUT_キャラ解説_XXXX(対象キャラ, INPUT数値)
#DIM 対象キャラ
#DIM INPUT数値
;キャラ解説画面のボタン動作を入れる関数

RETURN 0

;--------------------------------------------------
;血縁関係画面
;--------------------------------------------------
@能力表示固有INPUT_血縁関係_XXXX(対象キャラ, INPUT数値)
#DIM 対象キャラ
#DIM INPUT数値
;血縁関係画面のボタン動作を入れる関数

RETURN 0

;--------------------------------------------------
;キャラ履歴画面
;--------------------------------------------------
@能力表示固有INPUT_キャラ履歴_XXXX(対象キャラ, INPUT数値)
#DIM 対象キャラ
#DIM INPUT数値
;キャラ履歴画面のボタン動作を入れる関数

RETURN 0
