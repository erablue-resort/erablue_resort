;****************************************
;COMABLEにキャラ固有の処理を入れるための関数
;****************************************
;・使い方
;「XXXX」をキャラクター番号に置換し、「YYYY」をCOM番号に置換。それぞれの関数に処理を入れる
;通常のCOMはYYYYをCOM番号、派生COMもYYYYに「10_1」などをいれる
;性処理の場合はYYYYは「手コキ処理」などが入る
;マッサージコマンドは「MASSAGE_1」のように記述
;むりやりうふふコマンドは「RAPE_1」のように記述
;
;なお、「コマンドを使用禁止する」ための関数のため、条件を無視してOKにする目的では使用出来ない
;
;特定の体位を封じる場合は、下にある体位を封じる場合の関数を使用する
;「XXXX」をキャラクター番号に置換し、「体位名」を体位名称に置換。それぞれの関数に処理を入れる
;体位名は正常位、後背位、寝バック、対面座位、背面座位、騎乗位、側位、屈曲位、駅弁、背面駅弁、対面立位、立ちバック
;Ａ体位の場合はＡを入れる
;****************************************

;--------------------------------------------------
;プレイヤー側
;--------------------------------------------------
@固有COMABLE_プレイヤー側_YYYY_XXXX(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;コマンドをキャラが実行する側の時の関数
;RETURN 0の時にコマンドが表示されなくなる

RETURNF 1

;--------------------------------------------------
;ターゲット側
;--------------------------------------------------
@固有COMABLE_ターゲット側_YYYY_XXXX(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;コマンドをキャラに実行する側の時の関数
;RETURN 0の時にコマンドが表示されなくなる

RETURNF 1

;--------------------------------------------------
;体位を封じる場合_Ｖ_プレイヤー側
;--------------------------------------------------
@固有Ｖ体位ABLE_ターゲット側_体位名_XXXX(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラが実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

;--------------------------------------------------
;体位を封じる場合_Ｖ_ターゲット側
;--------------------------------------------------
@固有Ｖ体位ABLE_ターゲット側_体位名_XXXX(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

;--------------------------------------------------
;体位を封じる場合_Ａ_プレイヤー側
;--------------------------------------------------
@固有Ａ体位ABLE_ターゲット側_体位名_XXXX(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラが実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

;--------------------------------------------------
;体位を封じる場合_Ａ_ターゲット側
;--------------------------------------------------
@固有Ａ体位ABLE_ターゲット側_体位名_XXXX(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1
