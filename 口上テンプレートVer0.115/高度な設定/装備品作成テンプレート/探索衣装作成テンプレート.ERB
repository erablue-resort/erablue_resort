
;---------------------------------------------------------------------------------------------------------
;・使用法
;「YYYY」をジョブ証の名前で一括置換する。「X」に任意の数字や文字列を入れる。
;フレーバーやアビリティの設定をして完成。
;---------------------------------------------------------------------------------------------------------
@証関数_YYYY(処理モード)
#DIMS 処理モード
LOCALS = 

SELECTCASE 処理モード
	CASE "ジョブ証種別"
		証関連文字列受け渡し = 通常ジョブ
	CASE "フレーバー"
		LOCALS += "";＊＊＊＊＊＊＊ここを編集＊＊＊＊＊＊＊
		LOCALS += "";＊＊＊＊＊＊＊ここを編集＊＊＊＊＊＊＊
		LOCALS += "";＊＊＊＊＊＊＊ここを編集＊＊＊＊＊＊＊
		詳細文文字列受け渡し変数 = %LOCALS%
ENDSELECT

@証アビ関数_YYYY_X(処理モード, キャラ番号)
#DIM キャラ番号
#DIMS 処理モード
#DIM 消費ＭＰ = ;＊＊＊＊＊＊＊ここを編集＊＊＊＊＊＊＊
LOCALS = 

SELECTCASE 処理モード
	CASE "アビ名"
		TSTR:コマンド名受渡 = ;＊＊＊＊＊＊＊ここを編集＊＊＊＊＊＊＊
	CASE "アビ説明文"
		LOCALS += "■（アビ名）<br>";＊＊＊＊＊＊＊ここを編集＊＊＊＊＊＊＊
		LOCALS += @"　消費MP：{消費ＭＰ}<br>"
		LOCALS += "　";＊＊＊＊＊＊＊ここを編集＊＊＊＊＊＊＊
		詳細文文字列受け渡し変数 = %LOCALS%
		RETURN 消費ＭＰ
	CASE "アビ効果"
		;＊＊＊＊＊＊＊ここを編集＊＊＊＊＊＊＊
ENDSELECT



;特殊効果を作りたい場合は、以下に関数を作ること

;-------------------------------------------------------
;ターン開始時に呼ばれる関数
;-------------------------------------------------------
;@固有ターン開始時処理_ジョブ証_YYYY

;-------------------------------------------------------
;ターン終了時に呼ばれる関数
;-------------------------------------------------------
;@固有ターン終了時処理_ジョブ証_YYYY

;-------------------------------------------------------
; 特定のバフ・デバフ計算時に呼ばれる関数
;-------------------------------------------------------
; @固有バフ・デバフ処理_ジョブ証_YYYY_バフ・デバフ種類(隊列番号, 特殊補正後数値)
; #DIM 隊列番号
; #DIM 特殊補正後数値

; RETURN 特殊補正後数値

;-------------------------------------------------------
;奥義処理後に呼ばれる関数
;-------------------------------------------------------
;@固有奥義後処理_ジョブ証_YYYY(全滅フラグ)
;全滅フラグ：処理前の奥義で敵が全滅してるなら1が入る
; #DIM 全滅フラグ

;-------------------------------------------------------
;戦闘開始時に呼ばれる関数
;-------------------------------------------------------
;@固有戦闘開始時処理_ジョブ証_YYYY

;-------------------------------------------------------
;通常攻撃処理後に呼ばれる関数
;-------------------------------------------------------
;@固有通常攻撃後処理_ジョブ証_YYYY(全滅フラグ, 連撃数)
;全滅フラグ：処理前の奥義で敵が全滅してるなら1が入る
; #DIM 全滅フラグ
; #DIM 連撃数


