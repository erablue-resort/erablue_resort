;--------------------------------------------------
;コマンド実行後に表示される文章
;モードを開始したり削除したりするものを記載
;--------------------------------------------------
;・使い方
;「XXXX」をキャラクター番号に置換、コマンド番号の所に任意のコマンド番号を入れ、中身に処理を記述する
;テンプレート中にある分岐は例示としておいてあるものなので、必要な場合は各自でコピペをお願いします
;--------------------------------------------------
;・わかりづらい変数
;・コマンド実行による各部位の快楽量
	;CUP:対象キャラ:快Ｃ
	;CUP:対象キャラ:快Ｖ
	;CUP:対象キャラ:快Ａ
	;CUP:対象キャラ:快Ｂ
	;CUP:対象キャラ:快Ｓ
;
;・合意済みかどうか
	;GETBIT CFLAG:1 , 0

;TCVAR:対象キャラ:弱点コマンドフラグ
	;該当コマンドが弱点かどうか
	;現在弱点コマンドをゲーム中に変更する手段は無く、パラメータ設定ファイルで指定するか、指定の無い場所は開始時にランダムで決定される
	;口上テンプレートのパラメータ設定ファイルで全部弱点指定してしまえば弱点コマンドフラグで分岐する必要がない

;コマンド初回実行
	;IF STRFIND(コマンド初回フラグ:対象キャラ:0, @"-コマンド番号-") < 0
	;コマンド番号は任意に変更のこと

;--------------------------------------------------
;汎用コマンド実行時口上関数
;--------------------------------------------------
; @KOJO_COM_XXXX_コマンド番号(対象キャラ)
; #DIM 対象キャラ
; CALL 口上変数初期化

; KSTR:(K++) = 

; CALL 口上変数表示(対象キャラ, 0)


;--------------------------------------------------
;各コマンド別テンプレート
;--------------------------------------------------

;--------------------------------------------------
;うふふコマンド
;--------------------------------------------------
;MODE_乳首吸い削除
;--------------------------------------------------
@KOJO_MODE_乳首吸いプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す

;キャラが吸う方の口上
;取得快楽：Ｂ
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_乳首吸いターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す

;キャラが吸われる方の口上
;取得快楽：Ｂ
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_キス解除
;--------------------------------------------------
@KOJO_MODE_キスプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

;キスは実行対象でほとんど違い無いので、ターゲット側と共通でいいかも
IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_キスターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

;キスは実行対象でほとんど違い無いので、プレイヤー側と共通でいいかも
IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_ディープキス解除
;--------------------------------------------------
@KOJO_MODE_ディープキスプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

;キスは実行対象でほとんど違い無いので、ターゲット側と共通でいいかも
IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_ディープキスターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

;キスは実行対象でほとんど違い無いので、プレイヤー側と共通でいいかも
IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_クリキャップ解除
;--------------------------------------------------
@KOJO_MODE_クリキャップターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;アイテム類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_オナホール解除
;--------------------------------------------------
@KOJO_MODE_オナホールターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;アイテム類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_バイブ解除
;--------------------------------------------------
@KOJO_MODE_バイブターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;アイテム類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_アナルバイブ解除
;--------------------------------------------------
@KOJO_MODE_アナルバイブターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;アイテム類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_アナルビーズ解除
;--------------------------------------------------
@KOJO_MODE_アナルビーズターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;アイテム類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	SELECTCASE CUP:対象キャラ:快Ａ
		;各部位の累積が10000で絶頂
		CASE IS > 10000
			;即絶頂
			KSTR:(K++) = 
		CASE IS > 6000
			;かなり感じている
			KSTR:(K++) = 
		CASE IS > 3000
			;人並みに感じている
			KSTR:(K++) = 
		CASEELSE
			;まだ快楽に慣れていない
			KSTR:(K++) = 
	ENDSELECT
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_ニプルキャップ解除
;--------------------------------------------------
@KOJO_MODE_ニプルキャップターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;アイテム類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_搾乳機解除
;--------------------------------------------------
@KOJO_MODE_搾乳機ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;アイテム類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_Ｖ挿入系解除
;各体位ごとにあるのでちょっと長い
;この辺ひとまとめにしてもいいかも
;--------------------------------------------------
@KOJO_MODE_正常位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_正常位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_後背位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_後背位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_寝バックプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_寝バックターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_対面座位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_対面座位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_背面座位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_背面座位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_騎乗位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_騎乗位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_側位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_側位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_屈曲位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_屈曲位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_駅弁プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_駅弁ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_背面駅弁プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_背面駅弁ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_対面立位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_対面立位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_立ちバックプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_立ちバックターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_Ａ挿入系解除
;各体位ごとにあるのでちょっと長い
;この辺ひとまとめにしてもいいかも
;--------------------------------------------------
@KOJO_MODE_Ａ正常位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_Ａ正常位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_Ａ後背位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_Ａ後背位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_Ａ寝バックプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_Ａ寝バックターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_Ａ対面座位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_Ａ対面座位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_Ａ背面座位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_Ａ背面座位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_Ａ騎乗位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_Ａ騎乗位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_Ａ側位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_Ａ側位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_Ａ屈曲位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_Ａ屈曲位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_Ａ駅弁プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_Ａ駅弁ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_Ａ背面駅弁プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_Ａ背面駅弁ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_Ａ対面立位プレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_Ａ対面立位ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_Ａ立ちバックプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_Ａ立ちバックターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_手コキ系解除
;--------------------------------------------------
@KOJO_MODE_手コキプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラが手コキする方
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_手コキターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラが手コキされる方
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_授乳手コキプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラが手コキする方
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_授乳手コキターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラが手コキされる方
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_フェラ系解除
;--------------------------------------------------

@KOJO_MODE_フェラチオプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラがフェラチオをする方
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_フェラチオターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラがフェラチオをされる方
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_パイズリフェラプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラがフェラチオをする方
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_パイズリフェラターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラがフェラチオをされる方
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_イラマチオプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラが突っ込まれる方
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_イラマチオターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラが突っ込む方
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_シックスナイン解除
;--------------------------------------------------
@KOJO_MODE_シックスナインプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

;シックスナインは実行と対象でほぼ変わりないのでターゲット側と共通でいいかも
IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_シックスナインターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
CALL 口上変数初期化

;シックスナインは実行と対象でほぼ変わりないのでプレイヤー側と共通でいいかも
IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_パイズリ解除
;--------------------------------------------------
@KOJO_MODE_パイズリプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラがパイズリする方
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_パイズリターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラがパイズリされる方
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_縦パイズリプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラがパイズリする方
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_縦パイズリターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラがパイズリされる方
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_ボテ腹ズリ解除
;--------------------------------------------------

@KOJO_MODE_ボテ腹ズリプレイヤー側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラがボテ腹の方
CALL 口上変数初期化

IF 対象キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:実行キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:実行キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(実行キャラ番号, 0)

@KOJO_MODE_ボテ腹ズリターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
;取得快楽：なし
;キャラがボテ腹で扱く方
CALL 口上変数初期化

IF 実行キャラ番号 == MASTER
	;相手があなたの時
	IF TALENT:対象キャラ番号:恋慕
		KSTR:(K++) = 
	ELSEIF TALENT:対象キャラ番号:セフレ
		KSTR:(K++) = 
	ELSE
		KSTR:(K++) = 
	ENDIF
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;触手系
;--------------------------------------------------
;MODE_触手乳首責め解除
;--------------------------------------------------
@KOJO_MODE_触手乳首責めターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_触手クリ(ペニス)責め解除
;--------------------------------------------------
@KOJO_MODE_触手クリ責めターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

@KOJO_MODE_触手ペニス責めターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_触手口辱解除
;--------------------------------------------------
@KOJO_MODE_触手口辱ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_触手挿入解除
;--------------------------------------------------
@KOJO_MODE_触手挿入ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_触手アナル挿入解除
;--------------------------------------------------
@KOJO_MODE_触手アナル挿入ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_触手緊縛解除
;--------------------------------------------------
@KOJO_MODE_触手緊縛ターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)


;--------------------------------------------------
;自慰系
;--------------------------------------------------
;MODE_クリオナ解除
;--------------------------------------------------
@KOJO_MODE_クリオナターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_セルフ手コキ解除
;--------------------------------------------------
@KOJO_MODE_セルフ手コキターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_チクニー解除
;--------------------------------------------------
@KOJO_MODE_チクニーターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_セルフ乳首吸い解除
;--------------------------------------------------
@KOJO_MODE_セルフ乳首吸いターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_膣オナ解除
;--------------------------------------------------
@KOJO_MODE_膣オナターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)

;--------------------------------------------------
;MODE_アナニー解除
;--------------------------------------------------
@KOJO_MODE_アナニーターゲット側解除_XXXX(実行キャラ番号, 対象キャラ番号)
#DIM 対象キャラ
#DIM 実行キャラ番号
#DIM 対象キャラ番号
;モード解除は相手がPLAYERやTARGETとは限らないので別で渡す
CALL 口上変数初期化

;触手類は基本的にプレイヤー側は使わない（＝常にあなた）なので場合分けしない

IF TALENT:対象キャラ番号:恋慕
	KSTR:(K++) = 
ELSEIF TALENT:対象キャラ番号:セフレ
	KSTR:(K++) = 
ELSE
	KSTR:(K++) = 
ENDIF

CALL 口上変数表示(対象キャラ番号, 0)
