@COM_ORDER
#DIM 実行値
#DIM ＋表示フラグ

実行値 = 0
＋表示フラグ = 0

;好感度
IF CFLAG:好感度レベル
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=CFLAG:好感度レベル / 5
	PRINT 好感度
	PRINTV 'LV,CFLAG:好感度レベル,'(,CFLAG:好感度レベル / 5,')
	＋表示フラグ = 1
ENDIF
;恋慕
IF CFLAG:恋慕レベル
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=CFLAG:恋慕レベル / 3
	PRINT 恋慕
	PRINTV 'LV,CFLAG:恋慕レベル,'(,CFLAG:恋慕レベル / 3,')
	＋表示フラグ = 1
ENDIF
;屈服
IF CFLAG:屈服レベル
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=CFLAG:屈服レベル / 3
	PRINT 屈服
	PRINTV 'LV,CFLAG:屈服レベル,'(,CFLAG:屈服レベル / 3,')
	＋表示フラグ = 1
ENDIF
;快楽堕ち
IF CFLAG:快楽堕ちレベル
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=CFLAG:快楽堕ちレベル / 3
	PRINT 快楽堕ち
	PRINTV 'LV,CFLAG:快楽堕ちレベル,'(,CFLAG:快楽堕ちレベル / 3,')
	＋表示フラグ = 1
ENDIF
;酩酊
IF BASE:酩酊
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=BASE:酩酊 / 100
	PRINT 酩酊
	PRINTV '(,BASE:酩酊 / 100,')
	＋表示フラグ = 1
ENDIF
;ABL:マゾ性感
IF ABL:マゾ性感
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=ABL:マゾ性感 * 2
	PRINT マゾ性感
	PRINTV 'LV,ABL:マゾ性感,'(,ABL:マゾ性感 * 2,')
	＋表示フラグ = 1
ENDIF
;レズ調教
IF HETEROSEX(TARGET,PLAYER) == 1
	;ABL:同性愛
	IF ABL:同性愛
		SIF ＋表示フラグ
			PRINT  + 
		実行値 +=ABL:同性愛 * 3
		PRINT 同性愛
		PRINTV 'LV,ABL:同性愛,'(,ABL:同性愛 * 3,')
		＋表示フラグ = 1
	ENDIF
	;TALENT:両刀
	IF TALENT:性別嗜好 == -1
		SIF ＋表示フラグ
			PRINT  + 
		実行値 +=10
		PRINT 両刀
		PRINTV '(,10,')
		＋表示フラグ = 1
	ENDIF
	;好奇心
	IF TALENT:性的興味 > 0
		SIF ＋表示フラグ
			PRINT  + 
		実行値 +=7
		PRINT 好奇心
		PRINTV '(,7,')
		＋表示フラグ = 1
	ENDIF
	;保守的
	IF TALENT:性的興味 < 0
		PRINT  - 
		実行値 -= 13
		PRINT 保守的
		PRINTV '(,13,')
		＋表示フラグ = 1
	ENDIF
;ゲイ調教
ELSEIF HETEROSEX(TARGET,PLAYER) == 2
	;ABL:同性愛
	IF ABL:同性愛
		SIF ＋表示フラグ
			PRINT  + 
		実行値 +=ABL:同性愛 * 3
		PRINT 同性愛
		PRINTV 'LV,ABL:同性愛,'(,ABL:同性愛 * 3,')
		＋表示フラグ = 1
	ENDIF
	;TALENT:両刀
	IF TALENT:性別嗜好 == -1
		SIF ＋表示フラグ
			PRINT  + 
		実行値 +=10
		PRINT 両刀
		PRINTV '(,10,')
		＋表示フラグ = 1
	ENDIF
	;好奇心
	IF TALENT:性的興味 > 0
		SIF ＋表示フラグ
			PRINT  + 
		実行値 +=7
		PRINT 好奇心
		PRINTV '(,7,')
		＋表示フラグ = 1
	ENDIF
	;保守的
	IF TALENT:性的興味 < 0
		PRINT  - 
		実行値 -= 13
		PRINT 保守的
		PRINTV '(,13,')
		＋表示フラグ = 1
	ENDIF
ELSE
	;好奇心
	IF TALENT:性的興味 > 0
		SIF ＋表示フラグ
			PRINT  + 
		実行値 +=5
		PRINT 好奇心
		PRINTV '(,5,')
		＋表示フラグ = 1
	ENDIF
	;保守的
	IF TALENT:性的興味 < 0
		PRINT  - 
		実行値 -= 10
		PRINT 保守的
		PRINTV '(,10,')
		＋表示フラグ = 1
	ENDIF
ENDIF

;刻印チェック
IF MARK:苦痛刻印
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=MARK:苦痛刻印 * 5
	PRINT 苦痛刻印
	PRINTV 'LV,MARK:苦痛刻印,'(,MARK:苦痛刻印 * 5,')
	＋表示フラグ = 1
ENDIF

IF TALENT:プライド > 0
	T = 4
ELSEIF TALENT:プライド < 0
	T = 1
ELSE
	T = 2
ENDIF

;屈服刻印および反発刻印
IF MARK:屈服刻印
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=MARK:屈服刻印 * 3 * T
	PRINT 屈服刻印
	PRINTV 'LV,MARK:屈服刻印,'(,MARK:屈服刻印 * 3 * T,')
	＋表示フラグ = 1
ENDIF
IF MARK:反発刻印
	PRINT  - 
	実行値 -= MARK:反発刻印 * 2 * T
	PRINT 反発刻印
	PRINTV 'LV,MARK:反発刻印,'(,MARK:反発刻印 * 2 * T,')
	＋表示フラグ = 1
ENDIF



;服従
IF TALENT:服従
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=10
	PRINT 服従
	PRINTV '(,10,')
	＋表示フラグ = 1
ENDIF
;隷属
IF TALENT:服従 > 1
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=20
	PRINT 隷属
	PRINTV '(,20,')
	＋表示フラグ = 1
ENDIF
;反抗的（服従、隷属で無効）
IF TALENT:態度 > 0 && !TALENT:服従
	PRINT  - 
	実行値 -= 5
	PRINT 反抗的
	PRINTV '(,5,')
	＋表示フラグ = 1
ENDIF
;気丈
IF TALENT:度胸 > 0
	PRINT  - 
	実行値 -= 5
	PRINT 気丈
	PRINTV '(,5,')
	＋表示フラグ = 1
ENDIF
;素直
IF TALENT:態度 < 0
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=5
	PRINT 素直
	PRINTV '(,5,')
	＋表示フラグ = 1
ENDIF
;プライド高い
IF TALENT:プライド > 0
	PRINT  - 
	実行値 -= 15
	PRINT プライド高い
	PRINTV '(,15,')
	＋表示フラグ = 1
ENDIF
;プライド低い
IF TALENT:プライド < 0
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=5
	PRINT プライド低い
	PRINTV '(,5,')
	＋表示フラグ = 1
ENDIF
;弱味
IF TALENT:弱味
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=12
	PRINT 弱味
	PRINTV '(,12,')
	＋表示フラグ = 1
ENDIF
;即落ち
IF TALENT:即落ち
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=10
	PRINT 即落ち
	PRINTV '(,10,')
	＋表示フラグ = 1
ENDIF
;妄信
IF TALENT:妄信
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=8
	PRINT 妄信
	PRINTV '(,8,')
	＋表示フラグ = 1
ENDIF


;調教者の能力、素質
;謎の魅力
IF TALENT:PLAYER:謎の魅力
	SIF ＋表示フラグ
		PRINT  + 
	実行値 +=8
	PRINT 調教者の謎の魅力
	PRINTV '(,8,')
	＋表示フラグ = 1
ENDIF

A = 実行値
S = ＋表示フラグ
RETURN 実行値, ＋表示フラグ


