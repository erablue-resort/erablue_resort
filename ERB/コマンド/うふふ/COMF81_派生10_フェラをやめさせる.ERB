
@COMNAME_S81_10
#FUNCTIONS
TSTR:コマンド名受渡 = フェラをやめさせる

SIF MODE_確認_完全一致("パイズリフェラ", TARGET, PLAYER) > -1
	TSTR:コマンド名受渡 = パイズリフェラをやめさせる

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_S_81_10
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")
同時モード許可 = 1
助手モード許可 = 1

@COM_TOOLTIP_S81_10
LOCALS = <br>■フェラをやめさせる<br>
LOCALS += "フェラチオモードを終了するコマンド。<br>パイズリフェラモード時、[パイズリフェラをやめさせる]に変化。"
LOCALS += "<br>COMタイプ：うふふ<br>取得ソース：なし"
LOCALS += "<br>固有の取得経験：なし<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM_S81_10
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

;-------------------------------------------------
;モードの設定
;-------------------------------------------------
CALL MODE_DELETE_プレイヤー側("フェラチオ", TARGET)
CALL MODE_DELETE_プレイヤー側("パイズリフェラ", TARGET)

RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE_S81_10
;フェラチオをやめる実行判定
SIF !TFLAG:ゲームフェイズ管理
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE_S("81_10")
	RETURN RESULT
;フェラチオ系モードじゃない時はアウト
SIF MODE_確認_完全一致("フェラチオ系", TARGET, PLAYER) < 0
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM_S81_10
PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%を止め、口から陰茎を引き抜いた


