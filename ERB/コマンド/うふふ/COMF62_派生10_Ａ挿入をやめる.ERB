
@COMNAME_S62_10
#FUNCTIONS
TSTR:コマンド名受渡 = Ａ挿入をやめる

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_S_62_10
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("Ａ系")
同時モード許可 = 1
助手モード許可 = 1

@COM_TOOLTIP_S62_10
LOCALS = <br>■Ａ挿入をやめる<br>
LOCALS += "Ａ挿入系モードを終了するコマンド。"
LOCALS += "<br>COMタイプ：Ａ系<br>取得ソース：快Ａ・接触"
LOCALS += "<br>固有の取得経験：Ａ経験(TARGET)・Ａ性交経験(TARGET)<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM_S62_10
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、気力
DOWNBASE:体力 += 20
DOWNBASE:気力 += 50

CALL SOURCE_CALC_快Ａ(TARGET, PLAYER, 200)
CALL SOURCE_CALC_接触(TARGET, PLAYER, 100)
CALL SOURCE_CALC_快Ｃ(PLAYER, TARGET, 200)

EXP:愛情経験 += TALENT:恋慕 * 2
;-------------------------------------------------
;モードの設定
;-------------------------------------------------
CALL MODE_DELETE("Ａ挿入系")
RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE_S62_10
;Ａ挿入をやめる実行判定
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE_S("62_10")
	RETURN RESULT
;Ａ挿入系モードじゃない時はアウト
SIF MODE_確認("Ａ挿入系", TARGET) == 0
	RETURN 0
;同時挿入なら同時モードでないとダメ
SIF MODE_確認("Ａ同時挿入", TARGET) && COMMODE() != 2
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM_S62_10
PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%の肛門からゆっくりと陰茎を引き抜いた


