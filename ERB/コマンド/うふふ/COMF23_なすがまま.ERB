

@COMNAME23
#FUNCTIONS
TSTR:コマンド名受渡 = なすがまま

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_23
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")

@COM_TOOLTIP_23
LOCALS = <br>■なすがまま<br>
LOCALS += "行動せず、相手のしたいようにさせるコマンド。<br>５コマンドの間、オートコマンドを連続発動させる。"
LOCALS += "<br>COMタイプ：うふふ<br>取得ソース：なし"
LOCALS += "<br>固有の取得経験：なし<br>"
TSTR:ツールチップ受渡 = %LOCALS%


;なすがまま
@COM23
TFLAG:オートコマンド連続発動 = 5

TIME += 10
RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE23
;なすがまま実行判定
SIF !TFLAG:ゲームフェイズ管理
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(23)
	RETURN RESULT
;指示モードだと発動しない
SIF FLAG:モード管理 == 1
	RETURN 0
;オートコマンド禁止だと発動しない
SIF GETBIT(FLAG:オートコマンド切り替えオプション, 1) && GETBIT(FLAG:オートコマンド切り替えオプション, 3)
	RETURN 0
RETURN 1


;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM23
PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%のなすがままだ…

