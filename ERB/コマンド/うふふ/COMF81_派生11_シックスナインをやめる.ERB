
; @COMNAME_S81_11
; #FUNCTIONS
; TSTR:コマンド名受渡 = シックスナインをやめる

; ;===================================================
; ;コマンドタイプ
; ;===================================================
; @COMTYPE_S_81_11
; #FUNCTION
; TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")
; 同時モード許可 = 1
; 

; @COM_TOOLTIP_S81_11
; LOCALS = <br>■シックスナインをやめる<br>
; LOCALS += "シックスナインモードを終了するコマンド。"
; LOCALS += "<br>COMタイプ：うふふ<br>取得ソース：なし"
; LOCALS += "<br>固有の取得経験：なし<br>"
; TSTR:ツールチップ受渡 = %LOCALS%

; @COM_S81_11
; ;-------------------------------------------------
; ;ソース追加処理
; ;-------------------------------------------------

; ;-------------------------------------------------
; ;モードの設定
; ;-------------------------------------------------
; CALL MODE_DELETE_プレイヤー側("シックスナイン", TARGET)
; CALL MODE_DELETE_プレイヤー側("シックスナイン", PLAYER)

; RETURN 1

; ;-------------------------------------------------
; ;実行判定
; ;-------------------------------------------------
; @COM_ABLE_S81_11
; ;シックスナインをやめる実行判定
; SIF !TFLAG:ゲームフェイズ管理
; 	RETURN 0
; ;一括管理
; SIF GLOBAL_COMABLE_S("81_11")
; 	RETURN RESULT
; ;シックスナインモードじゃない時はアウト
; SIF MODE_確認_完全一致("シックスナイン", TARGET, PLAYER) < 0 && MODE_確認_完全一致("シックスナイン", PLAYER, TARGET) < 0
; 	RETURN 0
; RETURN 1

; ;-------------------------------------------------
; ;実行時メッセージ
; ;-------------------------------------------------
; @MESSAGE_COM_S81_11
; PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%を止め、口から陰茎を引き抜いて離れた…


