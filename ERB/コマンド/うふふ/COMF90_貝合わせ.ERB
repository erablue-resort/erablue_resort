;===================================================
;貝合わせ
;===================================================
@COMNAME90
#FUNCTIONS
TSTR:コマンド名受渡 = 貝合わせ

;許可時の特別文章はここに記入
IF TFLAG:オートコマンドフラグ >= 4000
	;反転時、つまりあなたがタチ
	許可取り文章 = %CALLNAME:TARGET%は%CALLNAME:PLAYER%に[貝合わせ]して欲しいようだ…
ELSE
	許可取り文章 = %CALLNAME:PLAYER%は%CALLNAME:TARGET%と[貝合わせ]したいようだ…
ENDIF


;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_90
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")
弱点コマンドカテゴリ = C_女_同性
オートコマンド許可 = 3
反転オート許可 = 3


;===================================================
;ツールチップ文章
;===================================================
@COM_TOOLTIP_90
TSTR:ツールチップ受渡 '= "<br>■貝合わせ<br>"
TSTR:ツールチップ受渡 += "互いの陰唇を擦りつけ快楽を与えあうコマンド。<br>貝合わせモードとなり、モードを変更するか性行為を終了するまで持続的に特定のソースが発生する。"
TSTR:ツールチップ受渡 += "<br>COMタイプ：うふふ<br>取得ソース：快Ｃ・露出・接触・不潔"
TSTR:ツールチップ受渡 += "<br>固有の取得経験：なし<br>"


;===================================================
;コマンド実行処理
;===================================================
@COM90
JUMP 疑似性交COM("貝合わせ", 400, 400)


;===================================================
;実行許可判定
;===================================================
@CAN_COM90(ARG)
JUMP 疑似性交CAN_COM(ARG, "貝合わせ", 19)


;===================================================
;実行可能判定
;===================================================
@COM_ABLE90
;貝合わせ実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(90)
	RETURN RESULT

;PLAYER側の判定
SIF !疑似性交COM_ABLE_女性キャラ(PLAYER)
	RETURN 0
;TARGET側の判定
SIF !疑似性交COM_ABLE_女性キャラ(TARGET)
	RETURN 0
;潤滑が高くないとダメ(両者の合計)
SIF PALAM:PLAYER:潤滑 + PALAM:TARGET:潤滑 < 2000
	RETURN 0

;お風呂場プレイ中はプレイマットが無いとダメ
IF BATHROOM(CFLAG:PLAYER:現在マップ種別, CFLAG:PLAYER:現在位置)
	SIF ITEM:プレイマット == 0 && NOITEM == 0
		RETURN 0
ENDIF
RETURN 1


;===================================================
;実行時メッセージ
;===================================================
@MESSAGE_COM90
PRINTFORM %CALLNAME:PLAYER%は仰向けに寝転んだ%CALLNAME:TARGET%の
SIF PALAM:PLAYER:潤滑 >= PALAMLV:4 || PALAM:TARGET:潤滑 >= PALAMLV:4
	PRINT ぐしょぐしょに濡れた
PRINTL 陰唇に自らの陰唇を口づけるように重ね合わせると、腰を揺すって擦りあげ始めた…
