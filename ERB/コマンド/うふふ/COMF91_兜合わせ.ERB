;===================================================
;兜合わせ
;===================================================
@COMNAME91
#FUNCTIONS
TSTR:コマンド名受渡 = 兜合わせ

;許可時の特別文章はここに記入
IF TFLAG:オートコマンドフラグ >= 4000
	;反転時、つまりあなたがタチ
	許可取り文章 = %CALLNAME:TARGET%は%CALLNAME:PLAYER%に[兜合わせ]して欲しいようだ…
ELSE
	許可取り文章 = %CALLNAME:PLAYER%は%CALLNAME:TARGET%と[兜合わせ]したいようだ…
ENDIF


;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_91
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")
弱点コマンドカテゴリ = C_ちんこ_同性
オートコマンド許可 = 3
反転オート許可 = 3


;===================================================
;ツールチップ文章
;===================================================
@COM_TOOLTIP_91
TSTR:ツールチップ受渡 '= "<br>■兜合わせ<br>"
TSTR:ツールチップ受渡 += "互いのペニスの先端を擦りつけ快楽を与えあうコマンド。<br>兜合わせモードとなり、モードを変更するか性行為を終了するまで持続的に特定のソースが発生する。"
TSTR:ツールチップ受渡 += "<br>COMタイプ：うふふ<br>取得ソース：快Ｃ・露出・接触・不潔"
TSTR:ツールチップ受渡 += "<br>固有の取得経験：なし<br>"


;===================================================
;コマンド実行処理
;===================================================
@COM91
JUMP 疑似性交COM("兜合わせ", 200, 200)


;===================================================
;実行許可判定
;===================================================
@CAN_COM91(ARG)
JUMP 疑似性交CAN_COM(ARG, "兜合わせ", 16)


;===================================================
;実行可能判定
;===================================================
@COM_ABLE91
;兜合わせ実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(91)
	RETURN RESULT

;PLAYER側の判定
SIF !疑似性交COM_ABLE_男性キャラ(PLAYER)
	RETURN 0
;ある程度勃起してないとダメ(PLAYER)
SIF BASE:PLAYER:勃起 < 1000
	RETURN 0
;TARGET側の判定
SIF !疑似性交COM_ABLE_男性キャラ(TARGET)
	RETURN 0
;ある程度勃起してないとダメ(TARGET)
SIF BASE:TARGET:勃起 < 1000
	RETURN 0
RETURN 1


;===================================================
;実行時メッセージ
;===================================================
@MESSAGE_COM91
PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%と向かい合い、ペニスの鈴口と鈴口を口づけるように重ね合わせると、腰を揺すって擦りあげ始めた…
