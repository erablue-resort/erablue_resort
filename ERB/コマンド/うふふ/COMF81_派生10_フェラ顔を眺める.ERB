
@COMNAME_S81_10
#FUNCTIONS
TSTR:コマンド名受渡 = フェラ顔を眺める

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_S_81_10
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")
オートコマンド許可 = 1
連続オート許可 = 1

@COM_TOOLTIP_S81_10
LOCALS = <br>■フェラ顔を眺める<br>
LOCALS += "フェラチオをされている時、相手の顔を眺めるコマンド。"
LOCALS += "<br>コマンド「何もしない」互換。"
LOCALS += "<br>COMタイプ：うふふor被挿入中<br>取得ソース：なし"
LOCALS += "<br>固有の取得経験：なし<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM_S81_10
CALL COM21
RETURN RESULT

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE_S81_10
;実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE_S("81_10")
	RETURN RESULT
;フェラチオモードじゃない時はアウト
SIF !MODE_存在判定_完全一致("フェラチオ", TARGET, PLAYER)
	RETURN 0

RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM_S81_10
SELECTCASE RAND:3
	CASE 0
		PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%がペニスを咥える姿を堪能している……
	CASE 0
		PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%がペニスを咥える姿を楽しんでいる……
	CASE 0
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%のペニスを丹念にしゃぶっている……
ENDSELECT


