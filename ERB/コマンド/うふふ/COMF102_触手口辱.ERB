;-------------------------------------------------
;触手口辱
;触手系コマンド、レベル1
;-------------------------------------------------
@COMNAME102
#FUNCTIONS
TSTR:コマンド名受渡 = 触手口辱
IF MODE_確認_完全一致("触手口辱", PLAYER, TARGET) > -1
	TSTR:コマンド名受渡 = 触手口辱解除
ENDIF
;複数モード_ダミーをリセット
複数モード_ダミー = 0

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_102
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("触手系")
;特殊な条件が必要なコマンドなので、弱点コマンドのランダム対象からは外す
;弱点コマンドカテゴリ = B
同時モード許可 = 1


@COM_TOOLTIP_102
LOCALS = <br>■触手口辱<br>
LOCALS += "キャラの口に触手を入れ、口腔内を嬲るコマンド。"
LOCALS += "<br>COMタイプ：触手系<br>取得ソース：快Ｓ・接触"
LOCALS += "<br>固有の取得経験：触手操作経験(あなた)<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM102
#DIM 快楽基礎値

IF MODE_確認_完全一致("触手口辱", PLAYER, TARGET) > -1
	CALL MODE_DELETE_ターゲット側("触手口辱", TARGET)
ELSE
	CALL MODE_CHANGE("触手口辱", PLAYER, TARGET)
ENDIF
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------



RCVAR:触手コマンドフラグ = 1

快楽基礎値 = あなた特殊能力:触手変化術 * 3
CALL SOURCE_CALC_快Ｓ(TARGET, PLAYER, 快楽基礎値 * 40)
CALL SOURCE_CALC_接触(TARGET, PLAYER, 快楽基礎値 * 20)
;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;同性愛　レズなら戻り値1ゲイなら戻り値2
IF HETEROSEX(TARGET,PLAYER)
	EXP:同性愛経験 += 1
	EXP:PLAYER:同性愛経験 += 1
ENDIF

EXP:PLAYER:触手操作経験 ++
RETURN 1
;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE102
SIF !TFLAG:ゲームフェイズ管理
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(102)
	RETURN RESULT
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM102

IF MODE_確認_完全一致("触手口辱", PLAYER, TARGET) > -1
	;メッセージ表示タイミングはモード付加後なのでモードがあれば＝吸い付いたタイミング
	PRINTFORM %CALLNAME:PLAYER%は自らの触手を%CALLNAME:TARGET%の口に潜り込ませ、腔内や舌を蹂躙した
ELSE
	;メッセージ表示タイミングはモード付加後なのでモードが無いなら＝解除したタイミング
	PRINTFORM %CALLNAME:PLAYER%は触手を%CALLNAME:TARGET%の口から離した…
ENDIF

