
@COMNAME_S82_10
#FUNCTIONS
TSTR:コマンド名受渡 = パイズリをやめさせる

;複数モード_ダミーをリセット
複数モード_ダミー = 0

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_S_82_10
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")
同時モード許可 = 1
助手モード許可 = 1

@COM_TOOLTIP_S82_10
LOCALS = <br>■パイズリをやめさせる<br>
LOCALS += "パイズリモードを終了するコマンド。"
LOCALS += "<br>COMタイプ：うふふ<br>取得ソース：なし"
LOCALS += "<br>固有の取得経験：なし<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM_S82_10
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

;-------------------------------------------------
;モードの設定
;-------------------------------------------------
CALL MODE_DELETE("パイズリ")


;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE_S82_10
;パイズリをやめる実行判定
SIF !TFLAG:ゲームフェイズ管理
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE_S("82_10")
	RETURN RESULT
;パイズリモードじゃない時はアウト
SIF MODE_確認("パイズリ", TARGET) == 0
	RETURN 0
;助手も同じ体位モードでいるなら同時モードじゃないとダメ
SIF MODE_確認("パイズリ", 助手は誰だ()) && COMMODE() != 2
	RETURN 0
;助手が同じ体位モードじゃないなら同時モードではダメ
SIF !MODE_確認("パイズリ", 助手は誰だ()) && COMMODE() == 2
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM_S82_10
PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%を止め、乳間から抜け出した


