;-------------------------------------------------
;クリキャップ
;道具使用コマンド、レベル2
;-------------------------------------------------
@COMNAME42
#FUNCTIONS
TSTR:コマンド名受渡 = クリキャップ

;SIF TEQUIP:11
SIF MODE_確認_ターゲット側("クリキャップ", TARGET) > -1
	TSTR:コマンド名受渡 = クリキャップ解除

;弱点コマンド欄に専用の文字列を表示したい場合、これを設定すること
弱点コマンド欄表示名 = クリキャップ

;複数モード_ダミーをリセット
複数モード_ダミー = 0

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_42
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("道具系")
;弱点コマンドカテゴリ = C
SIF MODE_確認_ターゲット側("クリキャップ", TARGET) > -1
	弱点コマンドカテゴリ = 

同時モード許可 = 1
助手モード許可 = 1


@COM_TOOLTIP_42
LOCALS = <br>■クリキャップ<br>
LOCALS += "クリトリスに自動で振動する性具を取りつけ、刺激するコマンド。<br>クリキャップ装備状態となり、外すか性行為を終了するまで持続的に快楽が発生する。"
LOCALS += "<br>COMタイプ：道具系<br>取得ソース：快Ｃ・露出・接触"
LOCALS += "<br>固有の取得経験：道具使用経験(あなた)<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM42
;着脱の処理
IF MODE_確認_ターゲット側("クリキャップ", TARGET) > -1
	CALL MODE_DELETE_ターゲット側("クリキャップ", TARGET)
ELSE
	CALL MODE_CHANGE("クリキャップ", PLAYER, TARGET)
ENDIF
;TEQUIP:クリキャップ = !TEQUIP:クリキャップ
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

CALL SOURCE_CALC_快Ｃ(TARGET, PLAYER,200)
CALL SOURCE_CALC_露出(TARGET, PLAYER, 20)
CALL SOURCE_CALC_接触(TARGET, PLAYER, 10)
;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;同性愛　レズなら戻り値1ゲイなら戻り値2
IF HETEROSEX(TARGET,PLAYER)
	EXP:同性愛経験 += 1
	EXP:PLAYER:同性愛経験 += 1
ENDIF

EXP:PLAYER:道具使用経験 ++

RETURN 1

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

CALL SOURCE_CALC_快Ｃ(TARGET, -1,100)
CALL SOURCE_CALC_露出(TARGET, PLAYER, 10)
;-------------------------------------------------
;経験の処理
;-------------------------------------------------

RETURN 1
;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE42
;電動クリキャップ実行判定
SIF !TFLAG:ゲームフェイズ管理
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(42)
	RETURN RESULT
;解除はいつでも可能
SIF MODE_確認_ターゲット側("クリキャップ", TARGET) > -1
	RETURN 1
;クリがない場合駄目
SIF TALENT:TARGET:Ｃ感度 == -2
	RETURN 0
;オトコやふたなりはダメ
SIF TALENT:性別 & 2
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:2 == 0 && NOITEM == 0
	RETURN 0

;下半身着衣
;前鎧、めくれない服、タイツ、下着があると無理
SIF TEQUIP:下腹部前鎧あり
	RETURN 0
SIF TEQUIP:下半身服あり && (!TEQUIP:スカートめくり可 && !TEQUIP:ずらし可)
	RETURN 0
SIF TEQUIP:下タイツあり && !TEQUIP:下タイツ破れ
	RETURN 0
SIF TEQUIP:下半身下着あり
	RETURN 0

;シャワー中はダメ
SIF TEQUIP:32
	RETURN 0
;部位使ってると駄目
SIF TEQUIP:TARGET:クリトリス
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM42
;解除時
IF MODE_確認_ターゲット側("クリキャップ", TARGET) < 0
	PRINTFORML %CALLNAME:TARGET%から電動クリキャップを外した
ELSE
	PRINTFORML %CALLNAME:TARGET%に電動クリキャップを取り付けた
ENDIF



