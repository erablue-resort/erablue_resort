
@COMNAME_S80_10
#FUNCTIONS
TSTR:コマンド名受渡 = 手コキをやめさせる

SIF MODE_確認("授乳手コキ", TARGET)
	TSTR:コマンド名受渡 = 授乳手コキをやめさせる

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_S_80_10
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")
助手モード許可 = 1

@COM_TOOLTIP_S80_10
LOCALS = <br>■手コキをやめさせる<br>
LOCALS += "手コキモードを終了するコマンド。<br>授乳手コキモード時、[授乳手コキをやめさせる]に変化。"
LOCALS += "<br>COMタイプ：うふふ<br>取得ソース：なし"
LOCALS += "<br>固有の取得経験：なし<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM_S80_10
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

;-------------------------------------------------
;モードの設定
;-------------------------------------------------
CALL MODE_DELETE("手コキ")
CALL MODE_DELETE("授乳手コキ")

RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE_S80_10
;手コキをやめる実行判定
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE_S("80_10")
	RETURN RESULT
;手コキ系モードじゃない時はアウト
SIF MODE_確認("手コキ系", TARGET) == 0
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM_S80_10
PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%を止め、陰茎から手を離させた


