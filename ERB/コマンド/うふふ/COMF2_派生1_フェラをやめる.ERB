;-------------------------------------------------
;フェラをやめる
;愛撫系コマンド、レベル1
;-------------------------------------------------
@COMNAME_S2_1
#FUNCTIONS
TSTR:コマンド名受渡 = フェラをやめる

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_S_2_1
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")

@COM_TOOLTIP_S2_1
LOCALS = <br>■フェラをやめる<br>
LOCALS += "逆フェラモードをやめるコマンド。"
LOCALS += "<br>COMタイプ：うふふ<br>取得ソース：なし"
LOCALS += "<br>固有の取得経験：なし<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM_S2_1
;-------------------------------------------------
;モードの設定
;-------------------------------------------------
CALL MODE_DELETE("逆フェラ")

RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE_S2_1
;フェラする実行判定
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE_S("2_1")
	RETURN RESULT
;逆フェラモードじゃない時はアウト
SIF MODE_確認("逆フェラ", TARGET) == 0
	RETURN 0
;助手も同じ体位モードでいるなら同時モードじゃないとダメ
SIF MODE_確認("逆フェラ", 助手は誰だ()) && COMMODE() != 2
	RETURN 0
;助手が同じ体位モードじゃないなら同時モードではダメ
SIF !MODE_確認("逆フェラ", 助手は誰だ()) && COMMODE() == 2
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------

@MESSAGE_COM_S2_1
PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%のペニスから唇を離した…

