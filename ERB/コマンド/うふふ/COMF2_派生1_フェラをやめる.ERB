; ;-------------------------------------------------
; ;フェラをやめる
; ;愛撫系コマンド、レベル1
; ;-------------------------------------------------
; @COMNAME_S2_1
; #FUNCTIONS
; TSTR:コマンド名受渡 = フェラをやめる

; ;===================================================
; ;コマンドタイプ
; ;===================================================
; @COMTYPE_S_2_1
; #FUNCTION
; TFLAG:コマンドタイプ受渡 = COMTYPE("うふふ")

; @COM_TOOLTIP_S2_1
; LOCALS = <br>■フェラをやめる<br>
; LOCALS += @"%CALLNAME:PLAYER%が行うフェラをやめるコマンド。"
; LOCALS += "<br>COMタイプ：うふふ<br>取得ソース：なし"
; LOCALS += "<br>固有の取得経験：なし<br>"
; TSTR:ツールチップ受渡 = %LOCALS%

; @COM_S2_1
; ;-------------------------------------------------
; ;モードの設定
; ;-------------------------------------------------
; CALL MODE_DELETE_プレイヤー側("フェラチオ系", PLAYER)

; RETURN 1

; ;-------------------------------------------------
; ;実行判定
; ;-------------------------------------------------
; @COM_ABLE_S2_1
; ;フェラする実行判定
; SIF !TFLAG:ゲームフェイズ管理
; 	RETURN 0
; ;一括管理
; SIF GLOBAL_COMABLE_S("2_1")
; 	RETURN RESULT
; ;逆フェラモードじゃない時はアウト
; SIF MODE_確認_プレイヤー側("フェラチオ", PLAYER) < 0
; 	RETURN 0
; RETURN 1

; ;-------------------------------------------------
; ;実行時メッセージ
; ;-------------------------------------------------

; @MESSAGE_COM_S2_1
; PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%のペニスから唇を離した…

