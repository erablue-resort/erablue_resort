;固有コマンド枠
;-------------------------------------------------
;コマンド名
;-------------------------------------------------
@COMNAME370
#FUNCTIONS
;キャラ個別のコマンド名を呼ぶ
TRYCALLFORMF CHARACOMNAME370_{NO:TARGET}

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_370
#FUNCTION
TRYCALLFORMF COMTYPE_370_{NO:TARGET}

@COM_TOOLTIP_370
TRYCALLFORM COM_TOOLTIP_370_{NO:TARGET}


;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM370
;個別の実行判定を呼ぶ
TRYCALLFORM CHARACOM370_{NO:TARGET}
RETURN RESULT

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE370
;実行判定
SIF !TFLAG:ゲームフェイズ管理
	RETURN 0
SIF !TARGET
	RETURN 0
;個別のABLEを呼ぶ
TRYCCALLFORM COM_ABLE370_{NO:TARGET}
	SIF RESULT == 0
		RETURN 0
CATCH
	RETURN 0
ENDCATCH
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM370
;個別のメッセージを呼ぶ
TRYCALLFORM MESSAGE_COM370_{NO:TARGET}


@MESSAGE_COM370_非口上時
;個別のメッセージを呼ぶ
TRYCALLFORM MESSAGE_COM370_{NO:TARGET}_非口上時


