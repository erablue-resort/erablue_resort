
@COMNAME401
#FUNCTIONS
TSTR:コマンド名受渡 = 宿泊エリアへ行く
SIF CFLAG:PLAYER:現在マップ種別 == 999
	TSTR:コマンド名受渡 = 部屋を出る

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_401
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("単独可能")


;-------------------------------------------------
;コレクション
;-------------------------------------------------
@COM401
CALL 宿泊エリア処理

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE401
;移動実行判定
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(401)
	RETURN RESULT
IF CFLAG:PLAYER:現在マップ種別 == 999
	;種別999の時はどこに居てもOK
ELSEIF CFLAG:MASTER:現在位置 != 20 || CFLAG:PLAYER:現在マップ種別 != 0
	;種別0かつ位置20（自室）でないならダメ
	RETURN 0
ENDIF
;うふふ時と飲み会時はダメ
SIF TFLAG:調教モード > 1
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
