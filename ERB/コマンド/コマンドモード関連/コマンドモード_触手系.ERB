
@系統リスト_触手系(モード名)
#FUNCTION
#DIMS モード名
IF GROUPMATCH(モード名, "触手乳首責め")
	モード確認結果 = 1
ENDIF

@MODE_持続快楽_触手系(キャラ番号)
#DIM キャラ番号
#DIM 主犯
#DIM 快楽基礎値

IF MODE_確認("触手系", キャラ番号)
	快楽基礎値 = あなた特殊能力:触手変化術 * 3
	TCVAR:キャラ番号:触手コマンドフラグ = 1
ELSE
	RETURN 0
ENDIF

IF MODE_確認("触手乳首責め", キャラ番号)
	主犯 = PLAYER
	CALL SOURCE_CALC_快Ｂ(キャラ番号, 主犯, 快楽基礎値 * 50)
	CALL SOURCE_CALC_接触(キャラ番号, 主犯, 快楽基礎値 * 20)
	EXP:キャラ番号:Ｂ経験 ++
	EXP:主犯:触手操作経験 ++
	TCVAR:キャラ番号:乳首開発フラグ = 3
	RETURN 1
ENDIF


@MODETYPE_触手乳首責め(ARG, ARGS)
;0=ペニス 1=クリ 2=指（手） 3=口 4=胸 5=膣 6=アナル
;触手のためプレイヤー側は一切場所を専有しない
SELECTCASE ARG
	CASE 4
		SIF ARGS == "ターゲット"
			RETURN 1
ENDSELECT
