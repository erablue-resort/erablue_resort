@MODE_持続快楽_Ａ騎乗位(実行キャラ番号, 対象キャラ番号)
#DIM 実行キャラ番号
#DIM 対象キャラ番号
CALL MODE_持続快楽_Ａ挿入系(実行キャラ番号, 対象キャラ番号, 120, 200)

@MODETYPE_Ａ騎乗位(部位番号, 処理モード, プレイヤー番号, ターゲット番号)
#DIM 部位番号
#DIM プレイヤー番号
#DIM ターゲット番号
#DIMS 処理モード

IF 処理モード == "写真用文字列_プレイヤー"
	;モード固有の「～～をする」の文章を入れ込む
	詳細文文字列受け渡し変数 = 跨った%CALLNAME:ターゲット番号%のアナルに下からペニスを突き入れる
	;下の部位専有判定と別の値を返す
	RETURN 部位状況_写真用
ELSEIF 処理モード == "写真用文字列_ターゲット"
	;モード固有の「～～をする」の文章を入れ込む
	IF 陥落チェック(ターゲット番号) == 0 && BASE:ターゲット番号:酩酊 > 300
		;未陥落の泥酔姦。ヤってる最中にも酩酊は下がり続けるので、泥酔の500からちょい下げて300までで
		;むしろ未陥落でもないと別に酔ってコトに及んだ情報残してもシコくなさそうなので未陥落のみ
		詳細文文字列受け渡し変数 = %TEXTR("酔った勢いで/酔いに浮かされて/泥酔して")%、%CALLNAME:プレイヤー番号%の上に跨って%TEXTR("ペニス/チンポ")%を
		詳細文文字列受け渡し変数 += @"%TEXTR("アナル/お尻/尻穴/ケツ穴")%%TEXTR("に咥えこみ/で受け入れ")%、%TEXTR("腰をグラインドさせる/腰を振る/腰を跳ねさせる/腰振り奉仕をする/快楽を貪る")%"
	ELSE
		詳細文文字列受け渡し変数 = %CALLNAME:プレイヤー番号%に跨り、%TEXTR("ペニス/チンポ")%を
		詳細文文字列受け渡し変数 += @"%TEXTR("アナル/お尻/尻穴/ケツ穴")%%TEXTR("に咥えこみ/で受け入れて")%%TEXTR("腰をグラインドさせる/腰を振る/腰を跳ねさせる/腰振り奉仕をする/快楽を貪る")%"
	ENDIF
	;下の部位専有判定と別の値を返す
	RETURN 部位状況_写真用
ENDIF

;0=ペニス 1=クリ 2=指（手） 3=口 4=胸 5=膣 6=アナル
;挿入で使用するのはペニスと膣で1を返す
SELECTCASE 部位番号
	CASE 部位_ペニス
		SIF 処理モード == "プレイヤー"
			RETURN 部位状況_占有
	CASE 部位_口
		;両方ハーヴィンだと口は届く
		SIF TALENT:プレイヤー番号:種族 == 4 && TALENT:ターゲット番号:種族 == 4
			RETURN 部位状況_なし
		;どっちかがハーヴィンだと口は届かない
		SIF TALENT:プレイヤー番号:種族 == 4 || TALENT:ターゲット番号:種族 == 4
			RETURN 部位状況_届かない
	CASE 部位_膣
		SIF 処理モード == "プレイヤー"
			RETURN 部位状況_届かない
	CASE 部位_アナル
		SIF 処理モード == "ターゲット"
			RETURN 部位状況_占有
		SIF 処理モード == "プレイヤー"
			RETURN 部位状況_届かない
ENDSELECT

@MESSAGE_MODE_Ａ騎乗位解除(実行キャラ番号, 対象キャラ番号)
#DIM 実行キャラ番号
#DIM 対象キャラ番号

PRINTFORML %CALLNAME:実行キャラ番号%は%CALLNAME:対象キャラ番号%の肛門からゆっくりと陰茎を引き抜いた
