;待つ（隠密状態のみ）
@COMNAME416
#FUNCTIONS
TSTR:コマンド名受渡 = 待つ

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_416
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("隠密")

@COM_TOOLTIP_416
LOCALS = <br>■待つ<br>
LOCALS += "隠れたままその場で待つコマンド。<br>１５分消費する。"
LOCALS += "<br>COMタイプ：隠密<br>取得ソース：なし"
LOCALS += "<br>固有の取得経験：なし<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM416
TIME += 15


;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE416
;実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(416)
	RETURN RESULT
;隠密時のみ
SIF !CFLAG:MASTER:隠密
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
