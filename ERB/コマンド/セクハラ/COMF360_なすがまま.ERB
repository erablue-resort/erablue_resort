

@COMNAME360
#FUNCTIONS
TSTR:コマンド名受渡 = なすがまま

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_360
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("セクハラ")

@COM_TOOLTIP_360
LOCALS = <br>■なすがまま<br>
LOCALS += "行動せず、相手のしたいようにさせるコマンド。"
LOCALS += "<br>COMタイプ：セクハラ<br>取得ソース：なし"
LOCALS += "<br>固有の取得経験：なし<br>"
TSTR:ツールチップ受渡 = %LOCALS%


;なすがまま
@COM360
TIME += 10
RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE360
;なすがまま実行判定
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(360)
	RETURN RESULT
SIF CFLAG:うふふ != 2
	RETURN 0
RETURN 1


;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM360
PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%のなすがままだ…

