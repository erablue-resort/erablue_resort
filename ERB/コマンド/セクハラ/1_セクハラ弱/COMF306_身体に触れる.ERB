
@COMNAME306
#FUNCTIONS
TSTR:コマンド名受渡 = 身体に触れる

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_306
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("セクハラ弱")
睡眠中許可 = 1

@COM_TOOLTIP_306
LOCALS = <br>■身体に触れる<br>
LOCALS += "相手の身体に優しく触れるコマンド。<br>コマンド実行時にどこに触れるかを選択する。"
LOCALS += "<br>COMタイプ：セクハラ<br>取得ソース：接触・露出・歓楽(一定条件時)・不快(一定条件時)・反感(一定条件時)など"
LOCALS += "<br>固有の取得経験：なし<br>"
TSTR:ツールチップ受渡 = %LOCALS%


@COM306
#DIM 選択先保存
#DIMS 選択肢リスト, 100
#DIM 選択肢数
#DIM ループ用
;身体に触れる

IF RFLAG:サイドボタン受渡し変数
	;サイドボタンから来た時は選択処理挟まず直接コマンド実行に飛ぶ
	選択先保存 = RFLAG:サイドボタン受渡し変数
ELSE
	;まずは選択肢のリストを作る
	CALL 触れ合い選択肢リスト(選択肢リスト)
	選択肢数 = RESULT

	DRAWLINE
	PRINTL どこに触れますか？
	DRAWLINE
	FOR ループ用, 0, 選択肢数
		CALLFORM 身体に触れる_ABLE_%選択肢リスト:ループ用%
		IF RESULT
			CALLFORMF COMNAME_S306_{RESULT}
			PRINTBUTTON @"[{ループ用}] %TSTR:コマンド名受渡%", ループ用
			PRINTL 
		ENDIF
	NEXT
	PRINTL 
	PRINTBUTTON "[999] 戻る", 999
	PRINTL 
	DRAWLINE
	BINPUT
	選択先保存 = RESULT
	SELECTCASE 選択先保存
		CASE 999
			RETURN -1
		CASE 0 TO 99
			CALLFORM 身体に触れる_ABLE_%選択肢リスト:選択先保存%
			選択先保存 = RESULT
	ENDSELECT
ENDIF

;COM実行処理
;弱点コマンド処理の都合上、CALL_COMを経由させる
RSTR:SELECTCOM_S受け渡し = 306_{選択先保存}
SELECTCOM_S = %RSTR:SELECTCOM_S受け渡し%
CALL CALL_COM(TARGET)
RETURN RESULT

;触れ合いの選択肢をソートした状態で渡すための関数
@触れ合い選択肢リスト(選択肢リスト変数)
#DIM CONST 関数接頭辞長さ = STRLENSU("身体に触れる_ABLE_")
#DIMS REF 選択肢リスト変数, 0
#DIM 選択肢数
#DIMS 選択肢名
#DIM 関数総数
#DIM ループ用

#DIMS CONST 選択先順番制御 = "背中に触れる", "耳を触る"

VARSET 選択肢リスト変数
選択肢数 = 0
FOR ループ用, 0, VARSIZE("選択先順番制御")
	SIF EXISTFUNCTION(@"身体に触れる_ABLE_%選択先順番制御:ループ用%") == 1
		選択肢リスト変数:(選択肢数++) '= 選択先順番制御:ループ用
NEXT

VARSET RESULTS
関数総数 = ENUMFUNCBEGINSWITH("身体に触れる_ABLE_")
FOR ループ用, 0, 関数総数
	選択肢名 '= SUBSTRINGU(RESULTS:ループ用, 関数接頭辞長さ, -1)
	SIF MATCH(選択先順番制御, 選択肢名)
		CONTINUE
	選択肢リスト変数:(選択肢数++) '= 選択肢名
NEXT

RETURN 選択肢数

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE306
;実行判定
SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(306)
	RETURN RESULT
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM306
CALLFORM MESSAGE_COM_S%RSTR:SELECTCOM_S受け渡し%

@MESSAGE_COM306_非口上時
CALLFORM MESSAGE_COM_S%RSTR:SELECTCOM_S受け渡し%_非口上時
