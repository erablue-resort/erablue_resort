;調教時テキスト
@TRAIN_MESSAGE
DRAWLINE
RESULT = 1
IF TFLAG:オートコマンドフラグ > 1000
	TRYCCALLFORM MESSAGE_AUTOCOM{TFLAG:オートコマンドフラグ - 1000}
		RETURN 1
	CATCH
	ENDCATCH
ELSEIF TFLAG:オートコマンドフラグ > 0
	TRYCCALLFORM MESSAGE_PRE_AUTOCOM{TFLAG:オートコマンドフラグ}
		RETURN 1
	CATCH
	ENDCATCH
ELSEIF SELECTCOM_S != ""
	TRYCCALLFORM MESSAGE_COM_S%SELECTCOM_S%
		RETURN 1
	CATCH
	ENDCATCH
ELSE
	TRYCCALLFORM MESSAGE_COM{SELECTCOM}
		RETURN 1
	CATCH
	ENDCATCH
ENDIF

RETURN 0

@TRAIN_MESSAGE_非口上時
DRAWLINE
RESULT = 1
IF TFLAG:オートコマンドフラグ > 1000
	TRYCCALLFORM MESSAGE_AUTOCOM{TFLAG:オートコマンドフラグ - 1000}_非口上時
		RETURN 1
	CATCH
	ENDCATCH
ELSEIF TFLAG:オートコマンドフラグ > 0
	TRYCCALLFORM MESSAGE_PRE_AUTOCOM{TFLAG:オートコマンドフラグ}_非口上時
		RETURN 1
	CATCH
	ENDCATCH
ELSEIF SELECTCOM_S != ""
	TRYCCALLFORM MESSAGE_COM_S%SELECTCOM_S%_非口上時
		RETURN 1
	CATCH
	ENDCATCH
ELSE
	TRYCCALLFORM MESSAGE_COM{SELECTCOM}_非口上時
		RETURN 1
	CATCH
	ENDCATCH
ENDIF

RETURN 0

;-------------------------------------------------
;だいたい移植済み
;残ってるのは紅魔館時代の未実装コマンドの残骸
;-------------------------------------------------


; ;-------------------------------------------------
; ;9,ダブルフェラ
; ;-------------------------------------------------
; @MESSAGE_SCOM9
; LOCAL = MASTER_POSE(4,1,1)
; PRINTFORM %CALLNAME:TARGET%と%CALLNAME:LOCAL%は
; ;奴隷と助手が恋慕か親愛
; SIF TALENT:恋慕 && TALENT:LOCAL:恋慕
; 	PRINTFORM うっとりした上目遣いで%CALLNAME:PLAYER%を見上げつつ、
; PRINTFORML %CALLNAME:PLAYER%のペニスに二人で舌を這わせている…
; ;-------------------------------------------------
; ;10,ダブル素股
; ;-------------------------------------------------
; @MESSAGE_SCOM10
; LOCAL = MASTER_POSE(2,1,1)
; PRINTFORML %CALLNAME:TARGET%と%CALLNAME:LOCAL%の性器でペニスを挟み、二人の間で扱きあげた…

; ;-------------------------------------------------
; ;11,ダブルパイズリ
; ;-------------------------------------------------
; @MESSAGE_SCOM11
; LOCAL = MASTER_POSE(5,1,1)
; PRINTFORM %CALLNAME:TARGET%と%CALLNAME:LOCAL%は
; ;奴隷と助手が恋慕か親愛
; SIF TALENT:恋慕 && TALENT:LOCAL:恋慕
; 	PRINTFORM うっとりした上目遣いで%CALLNAME:PLAYER%を見上げつつ、
; PRINT 互いの
; ;奴隷と助手が巨乳か爆乳
; IF TALENT:バストサイズ > 0 && TALENT:LOCAL:バストサイズ > 0
; 	PRINT 豊満な
; ;奴隷と助手が貧乳か絶壁
; ELSEIF TALENT:バストサイズ < 0 && TALENT:LOCAL:バストサイズ < 0
; 	PRINT 薄い
; ENDIF
; PRINTFORML 胸を重ね合わせて%CALLNAME:PLAYER%のペニスを扱きあげている…

; ;-------------------------------------------------
; ;12,パイズリフェラ
; ;-------------------------------------------------
; @MESSAGE_SCOM12
; PRINTFORM %CALLNAME:TARGET%は
; ;恋慕か親愛
; SIF TALENT:恋慕
; 	PRINT うっとりとした表情をしながら、
; ;貧乳
; IF TALENT:バストサイズ == -1
; 	PRINTL 薄い胸だけでなく舌も使って懸命にペニス全体を刺激していた…
; ;巨乳
; ELSEIF TALENT:バストサイズ == 1
; 	PRINTL ペニスを豊満な胸で包み込み、余った亀頭を舐めて刺激している…
; ;爆乳
; ELSEIF TALENT:バストサイズ == 2
; 	PRINTL 豊満すぎる程の胸でペニスを包み隠してしまい、時折舌を入れて、懸命にペニスを舐め上げている…
; ELSE
; 	PRINTL 胸でペニスをはさみ、余った部分を舌で、刺激している…
; ENDIF

; ;-------------------------------------------------
; ;13,交互挿入
; ;-------------------------------------------------
; @MESSAGE_SCOM13
; LOCAL = MASTER_POSE(6,1,1)

; ;現在2人以上の処女を同ターンで奪うことは出来ないため、処女関連はここ一つにまとめる
; IF TALENT:処女
; 	PRINTFORML %CALLNAME:LOCAL%の膣内に挿入したまま%CALLNAME:TARGET%を抱き寄せ、その男を知らない\@ TALENT:種族 < 0 ? 幼いワレメ # 秘裂 \@も一緒に貫いてやった…
; ;連続
; ELSEIF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
; 	PRINTFORM %CALLNAME:TARGET%と%CALLNAME:LOCAL%
; 	;正常位＋後背位
; 	IF ((TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 2) || (TEQUIP:体位 == 2 && TEQUIP:LOCAL:体位２ == 1))
; 		PRINT の体を密着させ、
; 	;正常位＋正常位
; 	ELSEIF TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 1
; 		PRINT を仰向けに並べ、
; 	ELSE
; 		PRINT の尻をこちらに向けさせ、
; 	ENDIF
; 	SIF TALENT:MASTER:2 & 2
; 		PRINT 二人の膣内の感触の違いを味わいながら
; 	PRINTL 再び挿入を繰り返した…
; ELSE
; 	IF RAND:2 == 0
; 		PRINTFORM %CALLNAME:LOCAL%の膣内に挿入したまま%CALLNAME:TARGET%を抱き寄せ、
; 		IF RAND:2 == 0
; 			PRINT その
; 			SIF PALAM:潤滑 >= PALAMLV:4
; 				PRINT 濡れそぼった
; 			PRINTFORM \@ ( TALENT:種族 < 0 && TALENT:LOCAL:種族 < 0 ) ? 幼いワレメ # 秘裂 \@も
; 		ELSE
; 			IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
; 				PRINT 重ねられた
; 			ELSE
; 				PRINT 並んだ
; 			ENDIF
; 			PRINT 二つの
; 			SIF PALAM:潤滑 >= PALAMLV:4 && PALAM:LOCAL:潤滑 >= PALAMLV:4
; 				PRINT 濡れそぼった
; 			PRINTFORM \@ ( TALENT:種族 < 0 && TALENT:LOCAL:種族 < 0 ) ? 幼いワレメ # 秘裂 \@を
; 		ENDIF
; 	ELSE
; 		IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
; 			IF RAND:2 == 0
; 				PRINTFORM %CALLNAME:TARGET%と%CALLNAME:LOCAL%を抱き合わせて、その\@ ( TALENT:種族 < 0 && TALENT:LOCAL:種族 < 0 ) ? 幼いワレメ # 秘裂 \@を
; 			ELSE
; 				PRINTFORM %CALLNAME:TARGET%の\@ TALENT:種族 < 0 ? 幼いワレメ # 秘裂 \@と%CALLNAME:LOCAL%の\@ TALENT:LOCAL:種族 < 0 ? 幼いワレメ # 秘裂 \@を密着させると、
; 			ENDIF
; 		ELSE
; 			PRINTFORM %CALLNAME:TARGET%の\@ TALENT:種族 < 0 ? 幼いワレメ # 秘裂 \@と%CALLNAME:LOCAL%の\@ TALENT:LOCAL:種族 < 0 ? 幼いワレメ # 秘裂 \@を並べ、
; 		ENDIF
; 	ENDIF
; 	PRINTL 交互に貫いた…
; ENDIF

; IF TALENT:恋慕 && TALENT:LOCAL:恋慕
; 	WAIT
; 	PRINTFORML %CALLNAME:TARGET%と%CALLNAME:LOCAL%は交互に挿入されながら、%CALLNAME:MASTER%のペニスをねだってきた…
; ELSEIF EXP:同性愛経験 >= 1000 && EXP:LOCAL:同性愛経験 >= 1000
; 	WAIT
; 	PRINTFORML %CALLNAME:TARGET%と%CALLNAME:LOCAL%は互いの舌を絡めあいながら、%CALLNAME:MASTER%のペニスを受け入れている…
; ENDIF

; ;前回の共同参加者の保存
; TFLAG:152 = LOCAL


