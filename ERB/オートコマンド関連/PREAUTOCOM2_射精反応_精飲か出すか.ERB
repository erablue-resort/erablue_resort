;------------------------------------------------------------------------------
;谷間に射精すると開いて見せてくれる
;------------------------------------------------------------------------------
@PRE_AUTOCOM2

CALL SOURCE_CALC_露出_反感無し(TARGET, PLAYER, 50)
;SIF TALENT:恋慕 || TALENT:性的興味
SIF TCVAR:精飲経験
	EXP:精飲経験 += 5

CALL MODE_DELETE("フェラ系")
CALL MODE_DELETE("シックスナイン")

@PRE_AUTOCOM_ABLE2
SIF !TARGET
	RETURN 0
;口内射精直後
SIF TFLAG:フィニッシュ場所 != 2
	RETURN 0
RETURN 1

@MESSAGE_PRE_AUTOCOM2
PRINTFORM %CALLNAME:TARGET%は口に出された精液
;IF TALENT:恋慕 || TALENT:性的興味
IF TCVAR:精飲経験
	PRINT をそのまま飲み下し
	IF ABL:技巧 > 2 || TCVAR:精飲経験 > 1
		PRINTL 、
		PRINTL 口を開いて全部飲み干した咥内を見せてきた…
	ELSE
		PRINTL た…
	ENDIF
ELSE
	IF ABL:技巧 > 2
		PRINTL を手早くティッシュに吐き出し、片付けた
	ELSE
		PRINTL に咳き込んでしまい、口から吐き出した…
	ENDIF
ENDIF


