;Ver0.041と旧バージョンとの互換のための処理
;Ver0.044にしたとき消す、互換を42に
 
;追加TALENT、追加CSTRのチェック
@VERUP_41_CHARA(ARG)
IF TALENT:ARG:性別嗜好 == -1
	TALENT:ARG:性別嗜好 = 0
	TALENT:ARG:性愛傾向 = 3
ENDIF

;その他広域変数の書き換えなど
@VERUP_41()
IF 精力追加フォロー <= 1
	FOR LOCAL, 0, CHARANUM
		CALL 精力ゲージ_初期化(LOCAL)
	NEXT
ENDIF


;対応処理用フラグを対応済状態にセット
@VERUP_41_FINISH()
精力追加フォロー = 2
