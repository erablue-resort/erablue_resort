;Ver0.055と旧バージョンとの互換のための処理
;Ver0.058にしたとき消す、互換を56に

;追加TALENT、追加CSTRのチェック
@VERUP_55_CHARA(ARG)

;恋慕と疑似恋慕共存パターンの撲滅
SIF TALENT:ARG:疑似恋慕 && TALENT:ARG:恋慕
	TALENT:ARG:恋慕 = 0
;たぶん恋慕を０にする方が正しいはず

;その他広域変数の書き換えなど
@VERUP_55()

; IF なんらかフォロー == 0
; 	;なんらかのフォロー処理
; 	;ここで"なんらかフォロー = 1"しない
; ENDIF

IF カロリーフォロー == 0
	DT_COLUMN_ADD "所持素材データベース", "摂取カロリー", 3
ENDIF

;対応処理用フラグを対応済状態にセット
@VERUP_55_FINISH()
; なんらかフォロー = 1

カロリーフォロー = 1
