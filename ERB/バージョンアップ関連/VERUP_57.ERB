;Ver0.057と旧バージョンとの互換のための処理
;Ver0.060にしたとき消す、互換を58に

;追加TALENT、追加CSTRのチェック
@VERUP_57_CHARA(ARG)
SIF 基礎BATTLE_STATE:ARG:Lv <= 0
	CALL 初期ステータス設定_テンプレート適用(タイプ数値文字列変換(基礎BATTLE_STATE:ARG:ステータスタイプ保存), ARG)

;その他広域変数の書き換えなど
@VERUP_57()

; IF なんらかフォロー == 0
; 	;なんらかのフォロー処理
; 	;ここで"なんらかフォロー = 1"しない
; ENDIF

CALL 衣装削除処理("シグナムのダンジョン衣装")
CALL 衣装削除処理("なのはのダンジョン衣装")
CALL 衣装削除処理("フェイトのダンジョン衣装")

IF プリーストフォロー == 0
	CFLAG:(GETCHARA(2871)):招待不可フラグ = 3
ENDIF

;対応処理用フラグを対応済状態にセット
@VERUP_57_FINISH()
; なんらかフォロー = 1

プリーストフォロー = 1

