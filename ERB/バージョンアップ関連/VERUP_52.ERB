;Ver0.052と旧バージョンとの互換のための処理
;Ver0.055にしたとき消す、互換を53に

;追加TALENT、追加CSTRのチェック
@VERUP_52_CHARA(ARG)

;その他広域変数の書き換えなど
@VERUP_52()
#DIM キャラ番号取得

; IF なんらかフォロー == 0
; 	;なんらかのフォロー処理
; 	;ここで"なんらかフォロー = 1"しない
; ENDIF

IF イベント完了フラグ確認("また「幽霊」が現れて")
	キャラ番号取得 = キャラ検索("[理想の聖母]ヘルツェロイデ")
	IF CFLAG:キャラ番号取得:招待不可フラグ != 0
		CFLAG:キャラ番号取得:招待不可フラグ = 1
ENDIF

IF イベント完了フラグ確認("ヤイアの後ろに「幽霊」が？")
	キャラ番号取得 = キャラ検索("[妹君の幽母]カレンティア")
	IF CFLAG:キャラ番号取得:招待不可フラグ != 0
		CFLAG:キャラ番号取得:招待不可フラグ = 1
ENDIF

;対応処理用フラグを対応済状態にセット
@VERUP_52_FINISH()
; なんらかフォロー = 1

