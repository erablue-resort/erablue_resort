;Ver0.047と旧バージョンとの互換のための処理
;Ver0.050にしたとき消す、互換を48に
 
;追加TALENT、追加CSTRのチェック
@VERUP_47_CHARA(ARG)

IF STRCOUNT(所持制服文字列, "汎用/レオタード（汎用）_") && 現在制服:ARG:0 == "レオタード（汎用）"
	現在制服:ARG:0 = レオタード
ENDIF

;その他広域変数の書き換えなど
@VERUP_47()
 
; IF なんらかフォロー == 0
; 	;なんらかのフォロー処理
; 	;ここで"なんらかフォロー = 1"しない
; ENDIF
IF STRCOUNT(所持制服文字列, "汎用/レオタード（汎用）_")
	所持制服文字列 '= REPLACE(所持制服文字列, "汎用/レオタード（汎用）_", "")
ENDIF

IF エンディングフォロー == 0
	CALL エンディングイベントデータベースセット
ENDIF


;対応処理用フラグを対応済状態にセット
@VERUP_47_FINISH()
; なんらかフォロー = 1

エンディングフォロー = 1
