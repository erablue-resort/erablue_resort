;Ver0.054と旧バージョンとの互換のための処理
;Ver0.057にしたとき消す、互換を55に

;追加TALENT、追加CSTRのチェック
@VERUP_54_CHARA(ARG)

;その他広域変数の書き換えなど
@VERUP_54()

; IF なんらかフォロー == 0
; 	;なんらかのフォロー処理
; 	;ここで"なんらかフォロー = 1"しない
; ENDIF

IF ウエディングフォロー == 0
	IF STRCOUNT(所持制服文字列, "汎用/ウェディングドレス_") > 0
		所持制服文字列 '= REPLACE(所持制服文字列, "汎用/ウェディングドレス_", "")
		CALL 制服登録処理("ウエディングドレス")
	ENDIF
ENDIF

;対応処理用フラグを対応済状態にセット
@VERUP_54_FINISH()
; なんらかフォロー = 1

ウエディングフォロー = 1
