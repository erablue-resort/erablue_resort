;Ver0.054と旧バージョンとの互換のための処理
;Ver0.057にしたとき消す、互換を55に

;追加TALENT、追加CSTRのチェック
@VERUP_54_CHARA(ARG)
;その他広域変数の書き換えなど
@VERUP_54()
#DIM 対象キャラ
; IF なんらかフォロー == 0
; 	;なんらかのフォロー処理
; 	;ここで"なんらかフォロー = 1"しない
; ENDIF

IF ウエディングフォロー == 0
	IF STRCOUNT(所持制服文字列, "汎用/ウェディングドレス_") > 0
		所持制服文字列 '= REPLACE(所持制服文字列, "汎用/ウェディングドレス_", "")
		CALL 制服登録処理("ウエディングドレス")
	ENDIF
ENDIF

IF 固有処理_25変更フォロー == 0
	対象キャラ = GETCHARA(25)
	VARSET 特定状況口上用フラグ:対象キャラ:0, 0, 1, 20
	CALL DAYEND_CHARAEVENT_25
ENDIF

IF ミルクデータベースフォロー == 0
	CALL ミルクデータベースセット
ENDIF

;対応処理用フラグを対応済状態にセット
@VERUP_54_FINISH()
; なんらかフォロー = 1

ウエディングフォロー = 1
固有処理_25変更フォロー = 1
ミルクデータベースフォロー = 1
