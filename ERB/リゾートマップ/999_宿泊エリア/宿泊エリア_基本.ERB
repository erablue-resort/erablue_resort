
;処理的には必要ないが、エラーを出さないために記述
@エリア情報登録_999(処理モード)
#DIMS 処理モード
SELECTCASE 処理モード
	CASE "エリア名"
		TSTR:場所名受渡 = 宿泊エリア
ENDSELECT


@SKIP_MOVE_999(目的地, 移動キャラ = 0)
#DIM 目的地
#DIM 移動キャラ

;スタート地点に関係なく即座に目的地へ移動可能
RETURN 目的地
