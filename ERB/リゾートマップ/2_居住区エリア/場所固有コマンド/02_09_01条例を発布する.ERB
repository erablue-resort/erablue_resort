
; ;-------------------------------------------------
; ;村役場で条例を発布する
; ;-------------------------------------------------
; @COMNAME_PLACE_380_2_9
; #FUNCTION
; TSTR:コマンド名受渡 = 条例を発布する
; RETURNF 1
; ;===================================================
; ;コマンドタイプ
; ;===================================================
; @COMTYPE_380_2_9
; #FUNCTION
; TFLAG:コマンドタイプ受渡 = COMTYPE("日常")
		
; @COM380_2_9

; LOCALS = 
; LOCALS += "<div rect='81,375,1937,2812' border='31' bcolor='#C0C0C0' padding='25,50,25'>"
; LOCALS += "<br>"
; IF 画面表示用変数 == 100
; 	LOCALS += @"<font color='#%カラーパレット_HTML("黄")%'>[100] 居住区の基本方針</font>"
; ELSE
; 	LOCALS += "<button value='100'>[100] 居住区の基本方針</button>"
; ENDIF
; LOCALS += "<br>"
; LOCALS += "<br>"
; ; LOCALS += "<button value='998'>[998] システムの説明</button>"
; ; LOCALS += "<br>"
; LOCALS += "<button value='999'>[999] 戻る</button>"
; LOCALS += "<br>"
; LOCALS += "</div>"


; LOCALS += "<div rect='2050,375,3875,2812' border='31' bcolor='#C0C0C0' padding='25,50,25'>"
; SELECTCASE 画面表示用変数
; 	CASE 0
; 		LOCALS += "リゾート全体に影響する条例の発布を行います。<br>"
; 		LOCALS += "基本的に項目ごとに複数の選択肢があり、その中から一つを選ぶ形式です。<br>"
; 		LOCALS += "また、形式の切り替えにデメリットやコストは存在しません。<br>"
; 	CASE 100
; 		LOCALS += 条例関数_居住区の基本方針()
; ENDSELECT
; LOCALS += "</div>"




; RETURN -1

; ;-------------------------------------------------
; ;実行判定
; ;-------------------------------------------------
; @COM_ABLE380_2_9
; ;条件に合う場合0（不許可）を返す
; ;どの条件にも引っかからないなら1（許可）を返す
; ;一括管理
; SIF GLOBAL_COMABLE(380)
; 	RETURN RESULT
; ;睡眠中
; SIF CFLAG:睡眠
; 	RETURN 0
; ;隠密中はダメ
; SIF CFLAG:PLAYER:隠密
; 	RETURN 0
; RETURN 1

; ;-------------------------------------------------
; ;実行時メッセージ
; ;-------------------------------------------------
; @MESSAGE_COM380_2_9
; ;ここには到達しない


; @条例関数_居住区の基本方針()
; #FUNCTIONS
; #DIM HTML文字列

; HTML文字列 = ■居住区の基本方針<br>
; HTML文字列 += "　現在の方針："
; SELECTCASE 条例発布:0:0
; 	CASE 0
; 		HTML文字列 += "自主性に任せる<br>"
; 		HTML文字列 += "　　特に何も指示せず、居住者たちの自由にさせる方針。<br>"
; 		HTML文字列 += "　　効果：なし<br>"
; 	CASE 1
; 		HTML文字列 += "映え重視の観光都市<br>"
; 		HTML文字列 += "　　<br>"
; 		HTML文字列 += "　　効果：<br>"
; 	CASE 2
; 		HTML文字列 += "経済重視の商業都市<br>"
; 		HTML文字列 += "　　<br>"
; 		HTML文字列 += "　　効果：<br>"
; 	CASE 3
; 		HTML文字列 += "環境重視の緑化都市<br>"
; 		HTML文字列 += "　　<br>"
; 		HTML文字列 += "　　効果：<br>"
; ENDSELECT

; IF 条例発布:0:0 == 0
; 	HTML文字列 += @"<font color='#%カラーパレット_HTML("黄")%'>[200] 自主性に任せる</font>"
; ELSE
; 	HTML文字列 += "<button value='200'>[200] 自主性に任せる</button>"
; ENDIF

; RETURNF HTML文字列
