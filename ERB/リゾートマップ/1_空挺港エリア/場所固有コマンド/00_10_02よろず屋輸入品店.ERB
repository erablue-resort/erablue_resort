
;-------------------------------------------------
;輸入品店
;-------------------------------------------------
@COMNAME_PLACE_381_1_10
#FUNCTION
TSTR:コマンド名受渡 = 輸入品店を見る
RETURNF 1

;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_381_1_10
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("単独可能")

@COM_TOOLTIP_381_1_10
LOCALS = <br>■輸入品店を見る<br>
LOCALS += "島の外から輸入した珍しい品を購入するコマンド。<br>毎日違う品物がラインナップされる。"
LOCALS += "<br>COMタイプ：単独可能<br>取得ソース：なし"
LOCALS += "<br>固有の取得経験：なし<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@COM381_1_10
CALL よろず屋輸入品店処理
RETURN RESULT

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE381_1_10
;条件に合う場合0（不許可）を返す
;どの条件にも引っかからないなら1（許可）を返す
;一括管理
SIF GLOBAL_COMABLE(381)
	RETURN RESULT
;隠密中はダメ
SIF CFLAG:PLAYER:隠密
	RETURN 0
RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM381_1_10



