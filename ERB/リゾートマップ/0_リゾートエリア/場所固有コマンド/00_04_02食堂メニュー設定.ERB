
; ;-------------------------------------------------
; ;食堂メニュー設定
; ;-------------------------------------------------

; @COMNAME_PLACE_381_0_4
; #FUNCTION
; TSTR:コマンド名受渡 = メニュー設定
; RETURNF 1

; ;===================================================
; ;コマンドタイプ
; ;===================================================
; @COMTYPE_381_0_4
; #FUNCTION
; TFLAG:コマンドタイプ受渡 = COMTYPE("単独可能")

; @COM_TOOLTIP_381_0_4
; LOCALS = <br>■メニュー設定<br>
; LOCALS += "所持しているメニューのうち、どれを食堂に出すか設定するコマンド。<br>"
; LOCALS += "<br>COMタイプ：単独可能<br>取得ソース：歓楽・征服(一定条件時)"
; LOCALS += "<br>固有の取得経験：なし<br>"
; TSTR:ツールチップ受渡 = %LOCALS%

; @COM381_0_4

; CALL 食堂メニュー設定処理
; RETURN RESULT

; ;-------------------------------------------------
; ;実行判定
; ;-------------------------------------------------
; @COM_ABLE381_0_4
; ;条件に合う場合0（不許可）を返す
; ;どの条件にも引っかからないなら1（許可）を返す
; ;一括管理
; SIF GLOBAL_COMABLE(381)
; 	RETURN RESULT
; ;睡眠中
; SIF CFLAG:睡眠
; 	RETURN 0
; ;隠密中はダメ
; SIF CFLAG:PLAYER:隠密
; 	RETURN 0
; RETURN 1

; ;-------------------------------------------------
; ;実行時メッセージ
; ;-------------------------------------------------
; @MESSAGE_COM381_0_4
