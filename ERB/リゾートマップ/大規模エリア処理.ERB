@エリアNEW表示判定(エリア名)
#FUNCTION
#DIMS エリア名
;存在するけど訪れたことのないエリアがある場合に１を返す
IF ダンジョンクリアフラグ_沿岸部の森 && 空艇港初来訪イベントフラグ == 0
	SIF エリア名 == "" || エリア名 == "空艇港エリア"
	RETURNF 1
ENDIF
IF MATCH(開拓エリア開発番号, 大規模番号_居住区エリア) > 0 && 居住区初来訪イベントフラグ == 0
	SIF エリア名 == "" || エリア名 == "居住区エリア"
	RETURNF 1
ENDIF
; IF MATCH(開拓エリア開発番号, 大規模番号_海エリア) > 0 && 海初来訪イベントフラグ == 0
; 	SIF エリア名 == "" || エリア名 == "海エリア"
; 	RETURNF 1
; ENDIF

RETURNF 0
