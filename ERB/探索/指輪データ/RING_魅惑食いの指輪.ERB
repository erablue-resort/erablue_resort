
@指輪個別文章表示_魅惑食いの指輪
LOCALS = 
LOCALS += @"■魅惑食いの指輪<br><br>"
LOCALS += "魅了されている相手へ大きな効果を発揮する指輪<br>"
LOCALS += "魅了のデバフを受けているキャラクターがいる時、威力が増加する。<br>"
LOCALS += "強敵「ヒトミダケ」の力が籠められた非常に珍しい指輪。<br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%

@指輪基礎効果文章表示_魅惑食いの指輪(指輪番号)
#DIM 指輪番号
#DIMS オプション効果一時保存, 20
LOCALS = 
LOCALS += "【基礎効果】<br>"
LOCALS += "最大ＨＰ+600 最大ＭＰ+30 攻撃力+100 防御力+250 回復力+200 対魅了特攻<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正")%
IF LOCALS:1 != ""
	LOCALS += "<br>【オプション効果】<br>"
	SPLIT LOCALS:1, "_", オプション効果一時保存
	FOR LOCAL, 0, 20
		IF オプション効果一時保存:LOCAL == ""
			BREAK
		ENDIF
		LOCALS += @"%オプション効果一時保存:LOCAL%"
		IF STRCOUNT(オプション効果一時保存:LOCAL, "率")
			LOCAL += 1
			IF TOINT(オプション効果一時保存:LOCAL) > 0
				LOCALS += "+"
			ENDIF
			LOCALS += @"%オプション効果一時保存:LOCAL%"
			LOCALS += "% "
		ELSE
			LOCAL += 1
			IF TOINT(オプション効果一時保存:LOCAL) > 0
				LOCALS += "+"
			ENDIF
			LOCALS += @"%オプション効果一時保存:LOCAL% "
		ENDIF
	NEXT
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪ステータス決定_魅惑食いの指輪(オプション数, 情報取得フラグ = 0)
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列
#DIMS 特殊補正文字列

基本補正文字列 = 最大ＨＰ_600_最大ＭＰ_30_攻撃力_100_防御力_250_回復力_200
特殊補正文字列 = 対魅了特攻

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	詳細文文字列受け渡し変数 += @"%特殊補正文字列%"
	RETURN 0
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv1のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(3)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "魅惑食いの指輪", "基礎ステータス補正", 基本補正文字列, "特殊ステータス補正", 特殊補正文字列, "オプションステータス補正", オプション補正文字列)


@固有バフ・デバフ処理_指輪_魅惑食いの指輪_与ダメ増加(隊列番号, ダメージ値)
#DIM 隊列番号
#DIM ダメージ値

;対象が魅了状態なら
IF デバフ存在チェック("魅了", 戦闘行動対象) > -1
	;ダメージ２倍
	RETURN ダメージ値 * 2
ENDIF

;上に引っかからなかったらダメージをそのまま返す
RETURN ダメージ値



