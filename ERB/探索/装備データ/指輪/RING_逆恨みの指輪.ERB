
@指輪個別文章表示_逆恨みの指輪
LOCALS = 
LOCALS += @"■逆恨みの指輪<br><br>"
LOCALS += "逆恨みを抱いて死んだアンデッドが残した指輪。<br>"
LOCALS += "攻撃力に特化してる代わりに、防御面が減少する効果を持つ。<br>"
LOCALS += "ハイリスク・ハイリターンな運用が難しい一品。<br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 20000

@指輪基礎効果文章表示_逆恨みの指輪(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
#DIMS オプション効果一時保存, 20
LOCALS = 
LOCALS += "【基礎効果】<br>"
LOCALS += "最大ＨＰ-200 最大ＭＰ+100 攻撃力+400 防御力-100 行動速度+200<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【オプション効果】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

;ランダム抽選では出現しない（入手法：ダンジョン「深海の古戦場跡」でボス「ゲラシーン」を撃破）
;@指輪出現率_逆恨みの指輪(入手タイプ, 装備ランク)

@指輪ステータス決定_逆恨みの指輪(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 最大ＨＰ_-200_最大ＭＰ_100_攻撃力_400_防御力_-100_行動速度_200

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 2
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv2のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(2)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "逆恨みの指輪", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)





