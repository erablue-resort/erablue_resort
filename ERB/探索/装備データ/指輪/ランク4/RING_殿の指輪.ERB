
@指輪個別文章表示_殿の指輪
LOCALS = 
LOCALS += @"■殿の指輪<br><br>"
LOCALS += "さる軍の最後尾で備えを欠かさなかった人物が特注したという指輪。<br>"
LOCALS += "隊列の最後尾にいる人物にのみ、この指輪は力を与える。<br>"
LOCALS += "その性能は防御と支援を重視しており、不測の事態にも動じない。<br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 60000

@指輪基礎効果文章表示_殿の指輪(指輪番号 = 0, 効果番号 = -1)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【基礎効果】<br>"
LOCALS += "最大ＨＰ+600 最大ＭＰ+600 防御力+300 回復力+600 命中率+15% ※条件あり<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【オプション効果】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_殿の指輪(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 4
	RETURN 0
SELECTCASE 入手タイプ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_ＳＲ
ENDSELECT

@指輪基礎効果適用判定_殿の指輪(対象キャラ)
#DIM 対象キャラ
SIF BATTLE_STATE:3:キャラ登録番号 == 対象キャラ
	RETURN 1
RETURN 0

@指輪ステータス決定_殿の指輪(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 最大ＨＰ_600_最大ＭＰ_600_防御力_300_回復力_600_命中率_15

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 3
ENDIF

;オプション数に応じてオプション効果をランダムで付加

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(3)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "殿の指輪", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)
