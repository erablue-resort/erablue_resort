
@指輪個別文章表示_知足者富の指輪
LOCALS = 
LOCALS += @"■知足者富の指輪<br><br>"
LOCALS += "程々の胸こそ真に豊かであると考える老子の魔力が込められた指輪。<br>"
LOCALS += "彼の認める体型の持ち主にのみ、この指輪は力を貸す。<br>"
LOCALS += "その悟りの力があれば、敵の動きを読むことも可能かもしれない。<br>"
LOCALS += "<br>"

詳細文文字列受け渡し変数 = %LOCALS%
基礎販売額 = 35000

@指輪基礎効果文章表示_知足者富の指輪(指輪番号, 効果番号)
#DIM 効果番号
#DIM 指輪番号
LOCALS = 
LOCALS += "【基礎効果】<br>"
LOCALS += "最大ＨＰ+150 攻撃力+200 防御力+100 回復力+200<br>"
LOCALS += "行動速度+50 命中率+10% ※条件あり<br>"

LOCALS:1 = %DT_CELL_GETS("所持指輪データベース", 指輪番号, "オプションステータス補正", 1)%
IF LOCALS:1 != ""
	LOCALS += @"<br>【オプション効果】<br>%オプション効果表示文字列作成(LOCALS:1)%"
ENDIF

詳細文文字列受け渡し変数 = %LOCALS%

@指輪出現率_知足者富の指輪(入手タイプ, 装備ランク)
#DIMS 入手タイプ
#DIM 装備ランク

SIF 装備ランク < 2
	RETURN 0
SELECTCASE 入手タイプ
	CASE "古ぼけた装備"
		RETURN 装備レアリティ_ＳＲ
ENDSELECT

@指輪基礎効果適用判定_知足者富の指輪(対象キャラ)
#DIM 対象キャラ
;男性はダメ
SIF TALENT:対象キャラ:性別 == 2
	RETURN 0
;Bが存在しないとダメ
SIF TALENT:対象キャラ:Ｂ感度 == -2
	RETURN 0
RETURN (TALENT:対象キャラ:バストサイズ == 0 || TALENT:対象キャラ:バストサイズ == 1)

@指輪ステータス決定_知足者富の指輪(オプション数, 情報取得フラグ = 0, 効果指定 = -1)
#DIM 効果指定
#DIM オプション数
#DIM 情報取得フラグ
#DIMS 基本補正文字列

基本補正文字列 = 最大ＨＰ_150_攻撃力_200_防御力_100_回復力_200_行動速度_50_命中率_10

IF 情報取得フラグ
	詳細文文字列受け渡し変数 = %基本補正文字列%
	;オプション固定用にオプションランクを返す
	RETURN 2
ENDIF

;オプション数に応じてオプション効果をランダムで付加
;基礎指輪なのでLv2のオプションしか付かない
;例外作りたいときは例外の関数作ってもろて

オプション補正文字列 = 
IF オプション数
	FOR LOCAL, 0, オプション数
		CALL オプション効果決定(2)
	NEXT
ENDIF

LOCAL = DT_ROW_ADD("所持指輪データベース", "指輪名", "知足者富の指輪", "基礎ステータス補正", 基本補正文字列, "オプションステータス補正", オプション補正文字列)
