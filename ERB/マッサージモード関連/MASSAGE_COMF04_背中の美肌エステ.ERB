;-------------------------------------------------
;背中の美肌エステ
;-------------------------------------------------
@MASSAGE_COMNAME4
#FUNCTIONS
TSTR:コマンド名受渡 = 背中の美肌エステ


;===================================================
;コマンドタイプ
;===================================================
@MASSAGE_COMTYPE_4
#FUNCTION
TFLAG:コマンドタイプ受渡 = MASSAGE_COMTYPE("通常マッサージ")

@MASSAGE_COM_TOOLTIP_4
LOCALS = <br>■背中の美肌エステ<br>
LOCALS += "特性のアロマオイルで背中の肌ケアをする。保湿、美肌効果がある。"
LOCALS += "背中を出しがちなエルーンのお客様に特に効果が高い。"
LOCALS += "<br>COMタイプ：マッサージ<br>取得ソース：快Ｓ・接触・歓楽"
LOCALS += "<br>固有の取得経験：なし<br>"
TSTR:ツールチップ受渡 = %LOCALS%

@MASSAGE_COM4
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

;基盤ができたあとで理性を削る処理を入れる

;歓楽強め。リラックス効果も強め。
;エルーンに対して効果が高い
IF TALENT:TARGET:種族 == 2
	CALL SOURCE_CALC_快Ｓ(TARGET, PLAYER, 150)
	CALL SOURCE_CALC_接触(TARGET, PLAYER, 100)
	CALL SOURCE_CALC_歓楽(TARGET, PLAYER, 300)
ELSE
;エルーン以外だとちょっと露出が増える。愛撫が100だから……とりあえず80くらい？
	CALL SOURCE_CALC_快Ｓ(TARGET, PLAYER, 100)
	CALL SOURCE_CALC_接触(TARGET, PLAYER, 100)
	CALL SOURCE_CALC_歓楽(TARGET, PLAYER, 200)
	CALL SOURCE_CALC_露出(TARGET, PLAYER, 80)
ENDIF

;-------------------------------------------------
;経験の処理
;-------------------------------------------------

RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------

@MASSAGE_COM_ABLE4
SIF SAVESTR:ゲームフェイズ管理 != "マッサージモード"
	RETURN 0

;このへんは現状よくわかんないのでとりあえず書かないでおく



RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MASSAGE_MESSAGE_COM4
PRINTFORML %CALLNAME:PLAYER%はエッセンシャルオイルを%CALLNAME:TARGET%の背に垂らし、マッサージしながら丁寧に塗りひろげた。

