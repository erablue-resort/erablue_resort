;-------------------------------------------------
;お腹の痩身オイルスパ
;-------------------------------------------------
@MASSAGE_COMNAME5
#FUNCTIONS
TSTR:コマンド名受渡 = お腹の痩身オイルスパ
SIF TALENT:TARGET:性別 == 1
	TSTR:コマンド名受渡 = 腹直筋オイルエステ

;===================================================
;コマンドタイプ
;===================================================
@MASSAGE_COMTYPE_5
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("通常マッサージ")

@MASSAGE_COM_TOOLTIP_5
IF TALENT:TARGET:性別 == 1
	LOCALS = <br>■腹直筋オイルエステ<br>
	LOCALS += "男性のお腹周りをオイルマッサージする。"
	LOCALS += "<br>COMタイプ：通常マッサージ<br>取得ソース：快Ｓ・接触・歓楽"
	LOCALS += "<br>固有の取得経験：なし<br>"
	TSTR:ツールチップ受渡 = %LOCALS%
ELSE
	LOCALS = <br>■お腹の痩身オイルスパ<br>
	LOCALS += "腹筋のあたりをオイルマッサージする。保湿、美肌効果がある。"
	LOCALS += "施術後はお腹周りがスッキリし、くびれもはっきりして痩せて見えると評判。"
	LOCALS += "<br>COMタイプ：通常マッサージ<br>取得ソース：快Ｓ・接触・歓楽"
	LOCALS += "<br>固有の取得経験：なし<br>"
	TSTR:ツールチップ受渡 = %LOCALS%
ENDIF
@MASSAGE_COM5
;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------

;基盤ができたあとで理性を削る処理を入れる

;男女ともに数値は同じ
CALL SOURCE_CALC_快Ｓ(TARGET, PLAYER, 100)
CALL SOURCE_CALC_接触(TARGET, PLAYER, 100)
CALL SOURCE_CALC_歓楽(TARGET, PLAYER, 200)
;露出に関しては……わかんない。基本衣装がへそ出しかどうかで分けるべきかもだけど、とりあえず面倒だから一律
CALL SOURCE_CALC_露出(TARGET, PLAYER, 50)


;-------------------------------------------------
;経験の処理
;-------------------------------------------------

RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------

@MASSAGE_COM_ABLE5
SIF SAVESTR:ゲームフェイズ管理 != "マッサージモード"
	RETURN 0

;このへんは現状よくわかんないのでとりあえず書かないでおく



RETURN 1

;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MASSAGE_MESSAGE_COM5
PRINTFORML %CALLNAME:PLAYER%はエッセンシャルオイルを%CALLNAME:TARGET%のお腹に垂らし、手のひらで揉みこむように丁寧にマッサージした。

