
@CALLPRINT_用語解説(指定用語)
#DIMS 指定用語

IF OPTION変数:ツールチップ切り替え
	PRINTFORM %指定用語%
	RETURN 0
ENDIF

TSTR:ツールチップ受渡 = 
TRYCALLFORM 用語解説文章_%指定用語%
IF TSTR:ツールチップ受渡 != ""
	HTML_PRINT @"<font color='#%カラーパレット_HTML("用語解説")%'><nonbutton title='<br>%TSTR:ツールチップ受渡%'>%指定用語%</nonbutton></font>", 1
ELSE
	PRINTFORM %指定用語%
ENDIF


@HTML_用語解説(指定用語)
#FUNCTIONS
#DIMS 指定用語

IF OPTION変数:ツールチップ切り替え
	RETURNF 指定用語
ENDIF

TSTR:ツールチップ受渡 = 
TRYCALLFORM 用語解説文章_%指定用語%
IF TSTR:ツールチップ受渡 != ""
	RETURNF @"<font color='#%カラーパレット_HTML("用語解説")%'><nonbutton title='<br>%TSTR:ツールチップ受渡%'>%指定用語%</nonbutton></font>"
ELSE
	RETURNF 指定用語
ENDIF


