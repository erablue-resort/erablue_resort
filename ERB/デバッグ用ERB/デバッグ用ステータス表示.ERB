
@OPTION_DEBUG
#DIM ループ用
#DIMS 名前格納


FOR ループ用,0,CHARANUM
	SIF CFLAG:ループ用:滞在期間 < 1
		CONTINUE
	PRINTFORM %NAME:ループ用%
	PRINTFORML 　現在位置は？　{CFLAG:ループ用:現在マップ種別}:{CFLAG:ループ用:現在位置}
	PRINTFORML 性欲：　{BASE:ループ用:性欲}
NEXT

; #;FOR LOCAL, 0, 4
; #;	BATTLE_STATE:LOCAL:0 = LOCAL + 1
; #;NEXT
; #;CALL DUNGEON_MAIN_LOOP("沿岸部の森")

; CALL 汎用指輪取得処理("栄冠の指輪・重")
; CALL 汎用指輪取得処理("栄冠の指輪・力")
; CALL 汎用指輪取得処理("栄冠の指輪・速")


WAIT

