@変身データベースセット
#DIM キャラ番号

CALL RELEASE変身データベース
CALL CREATE変身データベース
FOR キャラ番号, 0, CHARANUM
	CALL INSERT変身データベース(キャラ番号)
NEXT


@変身データベースリセット
CALL 変身解除実行処理ALL
CALL 変身データベースセット


@変身解除実行処理ALL
#DIM 行番号

FOR 行番号, 0, DT_ROW_LENGTH("変身データベース")
	CALL 変身解除実行処理(DT_CELL_GET("変身データベース", 行番号, "キャラ番号"))
NEXT


@変身セット削除(キャラ番号)
#DIM キャラ番号

CALL REMOVE変身データベース(キャラ番号)


@変身セット更新(キャラ番号)
#DIM キャラ番号

CALL 変身解除実行処理(キャラ番号)
CALL UPDATE変身データベース(キャラ番号)


@変身セット登録(キャラ番号)
#DIM キャラ番号

CALL INSERT変身データベース(キャラ番号)


;キャラ番号から変身データベースのIDの逆引き
@FIND_変身ID(キャラ番号)
#FUNCTION
#DIM キャラ番号
#DIM TMP_COUNT
#DIM TMP_SELECT, 1

TMP_COUNT = DT_SELECT("変身データベース", @"キャラ番号 = {キャラ番号}", , TMP_SELECT)
SIF TMP_COUNT == 1
	RETURNF TMP_SELECT:0
RETURNF 0


;各カラムのGET系
@GET_変身_キャラ番号(ID)
#FUNCTION
#DIM ID
RETURNF DT_CELL_GET("変身データベース", ID, "キャラ番号", 1)

@GET_変身_元NAME(ID)
#FUNCTIONS
#DIM ID
RETURNF DT_CELL_GETS("変身データベース", ID, "元NAME", 1)

@GET_変身_元CALLNAME(ID)
#FUNCTIONS
#DIM ID
RETURNF DT_CELL_GETS("変身データベース", ID, "元CALLNAME", 1)

@GET_変身_変身NAME(ID)
#FUNCTIONS
#DIM ID
RETURNF DT_CELL_GETS("変身データベース", ID, "変身NAME", 1)

@GET_変身_変身CALLNAME(ID)
#FUNCTIONS
#DIM ID
RETURNF DT_CELL_GETS("変身データベース", ID, "変身CALLNAME", 1)

@GET_変身_変身タイミング文字列(ID)
#FUNCTIONS
#DIM ID
RETURNF DT_CELL_GETS("変身データベース", ID, "変身タイミング文字列", 1)

@GET_変身_解除タイミング文字列(ID)
#FUNCTIONS
#DIM ID
RETURNF DT_CELL_GETS("変身データベース", ID, "解除タイミング文字列", 1)

@GET_変身_変身コマンド名(ID)
#FUNCTIONS
#DIM ID
RETURNF DT_CELL_GETS("変身データベース", ID, "変身コマンド名", 1)

@GET_変身_変身解除コマンド名(ID)
#FUNCTIONS
#DIM ID
RETURNF DT_CELL_GETS("変身データベース", ID, "変身解除コマンド名", 1)

@GET_変身_変身期間(ID)
#FUNCTION
#DIM ID
RETURNF DT_CELL_GET("変身データベース", ID, "変身期間", 1)

@GET_変身_変身残りターン数(ID)
#FUNCTION
#DIM ID
RETURNF DT_CELL_GET("変身データベース", ID, "変身残りターン数", 1)

@GET_変身_変身コスト(ID)
#FUNCTION
#DIM ID
RETURNF DT_CELL_GET("変身データベース", ID, "変身コスト", 1)


;各カラムのSET系
@SET_変身_キャラ番号(ID, VAL)
#DIM ID
#DIM VAL
RETURN DT_CELL_SET("変身データベース", ID, "キャラ番号", VAL, 1)

@SET_変身_元NAME(ID, VAL)
#DIM ID
#DIMS VAL
RETURN DT_CELL_SET("変身データベース", ID, "元NAME", VAL, 1)

@SET_変身_元CALLNAME(ID, VAL)
#DIM ID
#DIMS VAL
RETURN DT_CELL_SET("変身データベース", ID, "元CALLNAME", VAL, 1)

@SET_変身_変身NAME(ID, VAL)
#DIM ID
#DIMS VAL
RETURN DT_CELL_SET("変身データベース", ID, "変身NAME", VAL, 1)

@SET_変身_変身CALLNAME(ID, VAL)
#DIM ID
#DIMS VAL
RETURN DT_CELL_SET("変身データベース", ID, "変身CALLNAME", VAL, 1)

@SET_変身_変身タイミング文字列(ID, VAL)
#DIM ID
#DIMS VAL
RETURN DT_CELL_SET("変身データベース", ID, "変身タイミング文字列", VAL, 1)

@SET_変身_解除タイミング文字列(ID, VAL)
#DIM ID
#DIMS VAL
RETURN DT_CELL_SET("変身データベース", ID, "解除タイミング文字列", VAL, 1)

@SET_変身_変身コマンド名(ID, VAL)
#DIM ID
#DIMS VAL
RETURN DT_CELL_SET("変身データベース", ID, "変身コマンド名", VAL, 1)

@SET_変身_変身解除コマンド名(ID, VAL)
#DIM ID
#DIMS VAL
RETURN DT_CELL_SET("変身データベース", ID, "変身解除コマンド名", VAL, 1)

@SET_変身_変身期間(ID, VAL)
#DIM ID
#DIM VAL
RETURN DT_CELL_SET("変身データベース", ID, "変身期間", VAL, 1)

@SET_変身_変身残りターン数(ID, VAL)
#DIM ID
#DIM VAL
RETURN DT_CELL_SET("変身データベース", ID, "変身残りターン数", VAL, 1)

@SET_変身_変身コスト(ID, VAL)
#DIM ID
#DIM VAL
RETURN DT_CELL_SET("変身データベース", ID, "変身コスト", VAL, 1)
