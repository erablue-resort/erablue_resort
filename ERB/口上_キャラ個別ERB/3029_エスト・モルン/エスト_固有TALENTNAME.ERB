;****************************************
;キャラが所持する素質の表示名を自由に変更する関数
;****************************************
;・使い方
;「XXXX」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;****************************************

;--------------------------------------------------
;通常素質名
;--------------------------------------------------
@GET_固有TALENTNAME_3029(素質番号, 素質値, ツールチップフラグ, 対象キャラ)
#FUNCTION
#DIM 素質番号
#DIM 素質値
#DIM ツールチップフラグ
#DIM 対象キャラ

SELECTCASE 素質番号
	CASE 254
		;自由奔放
		IF 素質値 >= 2
			IF ツールチップフラグ
				TSTR:コマンド名受渡 '= "<br>■夢の主<br>傍若無人互換。<br>人の常識では測れない価値観を持つ存在。<br>許可が必要なオートコマンドや、プレイヤーが禁止したオートコマンドを許可無しに行う。<br> "
			ELSE
				TSTR:コマンド名受渡 '= "夢の主"
			ENDIF
		ENDIF
ENDSELECT

