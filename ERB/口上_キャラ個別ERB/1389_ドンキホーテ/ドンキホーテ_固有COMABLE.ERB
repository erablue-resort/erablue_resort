;****************************************
;COMABLEにキャラ固有の処理を入れるための関数
;****************************************
;・使い方
;「376」をキャラクター番号に置換し、「YYYY」をCOM番号に置換。それぞれの関数に処理を入れる
;なお、「コマンドを使用禁止する」ための関数のため、条件を無視してOKにする目的では使用出来ない
;****************************************

;--------------------------------------------------
;プレイヤー側
;--------------------------------------------------
@固有COMABLE_プレイヤー側_1_1389(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;コマンドをキャラが実行する側の時の関数
;RETURN 0の時にコマンドが表示されなくなる

RETURNF 1

;--------------------------------------------------
;ターゲット側
;--------------------------------------------------
@固有COMABLE_ターゲット側_85_1389(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;足コキ
;ロシナンテを履いてるので足系コマンド使用不可

RETURNF 0

@固有COMABLE_ターゲット側_MASSAGE_1_1389(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;足つぼマッサージ
;ロシナンテを履いてるので足系コマンド使用不可

RETURNF 0

