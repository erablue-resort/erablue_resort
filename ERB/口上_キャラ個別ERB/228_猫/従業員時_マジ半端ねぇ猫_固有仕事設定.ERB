;****************************************
;商業区に特殊な店舗を立てるための関数
;****************************************
;・使い方
;「XXXX」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;****************************************


;--------------------------------------------------
;業務設定
;--------------------------------------------------
@固有仕事設定_228_0(対象キャラ, 処理モード)
#FUNCTION
#DIM 対象キャラ
#DIMS 処理モード

SELECTCASE 処理モード
	CASE "仕事名"
		;業務の名前
		TSTR:コマンド名受渡 = マスコット
	CASE "仕事解説"
		;業務の名前
		TSTR:ツールチップ受渡 = 
		TSTR:ツールチップ受渡 += "リゾートの従業員ならぬ従業猫として施設内を歩き回り、観光客に癒しを振りまく。<br>子供にちょっと荒く撫でられても引っ掻いたりしないのがマスコットとしてのプライド。" 
	CASE "条件判定"
		;業務を実行出来るようになる条件
		;特に条件無しならそのままRETURN 1
		RETURNF 1
ENDSELECT

@固有仕事効果_一日終了時_228_0(対象キャラ)
#DIM 対象キャラ
#DIM おやつ代売上

;レポートに表示する文字列、49の部分は変えないこと
業務別報告文字列:49 += @"%CALLNAME:対象キャラ, 18, LEFT%は今日もリゾートのマスコットとして観光客に愛でられている……<br>"

;以下に効果処理を入れる
;効果文章などもここで表示すること

;例示として一定量のルピを手に入れる場合
おやつ代売上 = (RAND:100 + 100)
MONEY += おやつ代売上
業務別報告文字列:49 += @"　%CALLNAME:対象キャラ, 18, LEFT%に餌付けできる専用おやつは、%NUM_FORMAT(おやつ代売上), 6%ルピの利益をもたらした。<br>"

;複数業務を作る時は以上2つの関数をコピペし、末尾の0を他の数字に変えること


