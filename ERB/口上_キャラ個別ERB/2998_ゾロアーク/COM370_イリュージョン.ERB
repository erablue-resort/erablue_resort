
;----------------------------------------------------
;キャラ固有コマンド処理
;----------------------------------------------------
;コマンド名
;----------------------------------------------------
@CHARACOMNAME370_2998
#FUNCTION
LOCAL = GETCHARA(2998)
IF CSTR:LOCAL:スタイル管理文字列 != ""
	TSTR:コマンド名受渡 = みやぶる
ELSEIF CSTR:LOCAL:スタイル管理文字列 == ""
	TSTR:コマンド名受渡 = 化けてもらう
ENDIF
;===================================================
;コマンドタイプ
;===================================================
@COMTYPE_370_2998
#FUNCTION
TFLAG:コマンドタイプ受渡 = COMTYPE("日常")

@COM_TOOLTIP_370_2998
LOCALS = <br>■イリュージョン<br>
LOCALS += "他人に姿を変えるわざ。いたずらに最適！"
LOCALS += "<br>COMタイプ：日常<br>取得ソース：なし"
LOCALS += "<br>固有の取得経験：なし<br>"
TSTR:ツールチップ受渡 = %LOCALS%

;-------------------------------------------------
;実行処理
;-------------------------------------------------
@CHARACOM370_2998
LOCAL = GETCHARA(2998)
IF CSTR:LOCAL:スタイル管理文字列 != ""
	CSTR:LOCAL:スタイル管理文字列 = 
	CALL スタイル変更_2998(LOCAL, "変更後処理")
ELSEIF CSTR:LOCAL:スタイル管理文字列 == ""
;	SELECTCASE RAND:3
;		CASE 0
;			CSTR:LOCAL:スタイル管理文字列 = [チャンピオン]シロナ
;		CASE 1
;			CSTR:LOCAL:スタイル管理文字列 = [蒼天の守護騎士]カタリナ
;		CASE 2
			CSTR:LOCAL:スタイル管理文字列 = [碧く麗しき十天の輝星]ジータ
;	ENDSELECT
	CALL スタイル変更_2998(LOCAL, "変更後処理")
ENDIF

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE370_2998
;一括管理
SIF GLOBAL_COMABLE(370)
	RETURN RESULT
LOCAL = GETCHARA(2998)
SIF TARGET != LOCAL
	RETURN 0
RETURN 1


;-------------------------------------------------
;実行時メッセージ
;-------------------------------------------------
@MESSAGE_COM370_2998
LOCAL = GETCHARA(2998)
IF CSTR:LOCAL:スタイル管理文字列 != ""
	PRINTFORML ゾロアークはとくせい「イリュージョン」で姿を変えた！
	PRINTFORML 

ELSEIF CSTR:LOCAL:スタイル管理文字列 == ""
	PRINTFORML ゾロアークは元の姿に戻った。
	PRINTFORML 

ENDIF
