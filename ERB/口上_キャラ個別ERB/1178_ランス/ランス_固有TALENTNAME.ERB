;****************************************
;能力表示画面で、経験表示を切り替えるための関数
;****************************************
;・使い方
;「XXXX」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;****************************************

;--------------------------------------------------
;コマンド実行時処理
;--------------------------------------------------
@GET_固有TALENTNAME_1178(素質番号, 素質値, ツールチップフラグ, 対象キャラ)
#FUNCTION
#DIM 素質番号
#DIM 素質値
#DIM ツールチップフラグ
#DIM 対象キャラ

SELECTCASE 素質番号
	CASE 162
		;男性器サイズ
		SELECTCASE 素質値
			CASE -2
				IF ツールチップフラグ
					TSTR:コマンド名受渡 = こどもちんこ互換
				ELSE
					TSTR:コマンド名受渡 = はいぱーへいき
				ENDIF
			CASE -1
				IF ツールチップフラグ
					TSTR:コマンド名受渡 = 短小互換
				ELSE
					TSTR:コマンド名受渡 = ハイパー兵器（自称）
				ENDIF
			CASE 0
				IF ツールチップフラグ
					TSTR:コマンド名受渡 = 平凡互換
				ELSE
					TSTR:コマンド名受渡 = ハイパー兵器？
				ENDIF
			CASE 1
				IF ツールチップフラグ
					TSTR:コマンド名受渡 = 巨根互換
				ELSE
					TSTR:コマンド名受渡 = ハイパー兵器
				ENDIF
			CASE 2
				IF ツールチップフラグ
					TSTR:コマンド名受渡 = 規格外互換
				ELSE
					TSTR:コマンド名受渡 = 超・ハイパー兵器
				ENDIF
		ENDSELECT
ENDSELECT
