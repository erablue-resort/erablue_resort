;****************************************
;縁結び系の処理に使う特殊関数
;****************************************
;・使い方
;「XXXX」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;****************************************

;--------------------------------------------------
;右キャラ時選択条件
;--------------------------------------------------
@縁結び相手条件_セフレ_1178(実行キャラ, 対象キャラ)
#DIM 実行キャラ
#DIM 対象キャラ

;相手に選ぶ時、及び選ばれた時に進展するかどうかで使用

; RETURN 0 で通常の条件に戻す
; RETURN -1 で問答無用で選択出来なくする
; RETURN 1 で問答無用で選択出来るようにする

;ランスくんは相手がセフレ持ちじゃなくてもセフレ結び出来る（相手が女のときのみ）
IF TALENT:対象キャラ:性別 == 1
	;意外と保守的なのでふたなりに補正は掛けない
	RETURN 1
ENDIF

RETURN 0

;--------------------------------------------------
;縁結び進行時処理
;--------------------------------------------------
@縁結び進行固有補正_セフレ_実行側_1178(実行キャラ, 対象キャラ, 進行度)
#DIM 実行キャラ
#DIM 対象キャラ
#DIM REF 進行度
;縁結び進行時に、進行値に処理を行う関数
;主に進行度に補正を掛ける目的で使用

;ランスくんは自分からセフレを作る時に爆速で進展する（相手が女のときのみ）
;なんと５倍速
IF TALENT:対象キャラ:性別 == 1
	;意外と保守的なのでふたなりに補正は掛けない
	進行度 *= 5
ENDIF
