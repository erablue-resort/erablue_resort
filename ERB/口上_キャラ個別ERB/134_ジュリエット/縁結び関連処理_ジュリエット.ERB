﻿;****************************************
;縁結び系の処理に使う特殊関数
;****************************************
;・使い方
;「134」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;****************************************

;--------------------------------------------------
;左キャラ時選択条件
;--------------------------------------------------
@縁結び特殊条件_左キャラ時_134(対象キャラ)
#DIM 対象キャラ
;通常の条件とは別の条件を設定するためのもの

; RETURN 0 で通常の条件に戻す
; RETURN -1 で問答無用で選択出来なくする
; RETURN 1 で選択出来るようにする

;基本的に縁結び相手条件_134と組み合わせて使うこと
;例）アリーザは陥落不可だがRETURN 1で選択可能にし、相手条件でスタンのみ許可する、など

RETURN 0

;--------------------------------------------------
;右キャラ時選択条件
;--------------------------------------------------
@縁結び相手条件_恋慕_134(実行キャラ, 対象キャラ)
#DIM 実行キャラ
#DIM 対象キャラ

;相手に選ぶ時、及び選ばれた時に進展するかどうかで使用

; RETURN 0 で通常の条件に戻す
; RETURN -1 で問答無用で選択出来なくする
; RETURN 1 で問答無用で選択出来るようにする
;「特定の相手だけ恋慕可能で他のキャラは封印」

; IF NAME:対象キャラ == "任意のキャラのNAME"
; 	RETURN 1
; ELSE
; 	RETURN -1
; ENDIF
;「自由に縁結びできるシステムで相手を固定するのは。恋慕不可素質との併用が望ましい」

@縁結び相手条件_セフレ_134(実行キャラ, 対象キャラ)
#DIM 実行キャラ
#DIM 対象キャラ

;相手に選ぶ時、及び選ばれた時に進展するかどうかで使用

; RETURN 0 で通常の条件に戻す
; RETURN -1 で問答無用で選択出来なくする
; RETURN 1 で問答無用で選択出来るようにする

; IF NAME:対象キャラ == "任意のキャラのNAME"
; 	RETURN 1
; ELSE
; 	RETURN -1
; ENDIF


;--------------------------------------------------
;縁結び進行時処理
;--------------------------------------------------
@縁結び進行固有補正_恋慕_実行側_134(実行キャラ, 対象キャラ, 進行度)
#DIM 実行キャラ
#DIM 対象キャラ
#DIM REF 進行度
;縁結び進行時に、進行値に処理を行う関数
;主に進行度に補正を掛ける目的で使用
;キャラが縁結びの左側になってるときに使う関数
;例えば「特定の相手に縁結びをしているときに進行度が3倍される」場合は以下のようになる
IF NAME:対象キャラ == "[想国の神王]ロミオ"
	進行度 *= 5
ENDIF

@縁結び進行固有補正_恋慕_対象側_134(対象キャラ, 実行キャラ, 進行度)
#DIM 実行キャラ
#DIM 対象キャラ
#DIM REF 進行度
;縁結び進行時に、進行値に処理を行う関数
;主に進行度に補正を掛ける目的で使用
IF NAME:実行キャラ == "[想国の神王]ロミオ"
	進行度 += 5
ENDIF


@縁結び進行固有補正_セフレ_実行側_134(実行キャラ, 対象キャラ, 進行度)
#DIM 実行キャラ
#DIM 対象キャラ
#DIM REF 進行度
;縁結び進行時に、進行値に処理を行う関数
;主に進行度に補正を掛ける目的で使用
IF NAME:対象キャラ == "[想国の神王]ロミオ"
	進行度 *= 5
ENDIF

@縁結び進行固有補正_セフレ_対象側_134(対象キャラ, 実行キャラ, 進行度)
#DIM 実行キャラ
#DIM 対象キャラ
#DIM REF 進行度
;縁結び進行時に、進行値に処理を行う関数
;主に進行度に補正を掛ける目的で使用
IF NAME:実行キャラ == "[想国の神王]ロミオ"
	進行度 += 5
ENDIF
