;;--------------------------------------------------
;;●パチュリー・ノーレッジ口上＠erablue_resort用
;;　――固有コマンド：相談する
;;--------------------------------------------------
;
;;--------------------------------------------------
;;●ライセンス/更新履歴/概要/使用フラグ
;;--------------------------------------------------
;;　いずれもパチュリー口上_readme.txt記載に従う
;;--------------------------------------------------
;
;;--------------------------------------------------
;;〇固有コマンド：相談する
;;--------------------------------------------------
;;パチュリーと初対面の際に自動発動
;;その後、日常で常時開放
;;口上カラーの切り替えなどを実行する
;;--------------------------------------------------
;@CHARACOMNAME370_1147
;#FUNCTIONS
;	TSTR:コマンド名受渡 = 相談する
;
;
;;===================================================
;;コマンドタイプ
;;===================================================
;@COMTYPE_370_1147
;#FUNCTION
;	TFLAG:コマンドタイプ受渡 = COMTYPE("日常")
;
;@COM_TOOLTIP_370_1147
;	LOCALS = <br>■相談する<br>
;	LOCALS += "パチュリーと相談するコマンド。"
;	LOCALS += "<br>口上設定を変更することができる。"
;	LOCALS += "<br>COMタイプ：日常"
;	LOCALS += "<br>取得ソース：なし"
;	LOCALS += "<br>固有の取得経験：なし<br>"
;	TSTR:ツールチップ受渡 = %LOCALS%
;
;;-------------------------------------------------
;;実行処理
;;-------------------------------------------------
;;初対面時と流用するため、相談部分本体は別にする
;;-------------------------------------------------
;@CHARACOM370_1147
;#DIM 対象キャラ
;
;	対象キャラ = キャラ検索("[動かない大図書館]パチュリー")
;	CALL CHARACOM370_1147_相談部分(対象キャラ)
;
;	TIME += 15
;
;	RETURN 1
;
;;-------------------------------------------------
;;実行判定
;;-------------------------------------------------
;;「会話」と同じにする
;;-------------------------------------------------
;@COM_ABLE370_1147
;	;会話実行判定
;	SIF SAVESTR:ゲームフェイズ管理 != "通常モード"
;		RETURN 0
;	;一括管理
;	SIF GLOBAL_COMABLE(370)
;		RETURN RESULT
;	;睡眠中
;	SIF CFLAG:睡眠
;		RETURN 0
;	;口を何かに使ってるなら無理
;	SIF TEQUIP:PLAYER:口
;		RETURN 0
;	SIF TEQUIP:TARGET:口
;		RETURN 0
;	;ここまできたらOK
;	RETURN 1
;
;;-------------------------------------------------
;;実行時メッセージ
;;-------------------------------------------------
;;内容は完全に「相談本体」のほうへ。
;;-------------------------------------------------
;@MESSAGE_COM370_1147
;#DIM 対象キャラ
;
;	対象キャラ = キャラ検索("[動かない大図書館]パチュリー")
;	PRINTFORML %CALLNAME:対象キャラ%と相談した……
;
;
;;-------------------------------------------------
;;相談本体
;;-------------------------------------------------
;;特殊状況フラグ
;;　=="初対面"　だと「初対面時」のやつ。必ず「この口上について」が出る
;;-------------------------------------------------
;@CHARACOM370_1147_相談部分(対象キャラ, 特殊状況フラグ = "")
;#DIM 対象キャラ
;#DIMS 特殊状況フラグ
;
;;	;おまじない
;;	;口上カラー設定が未設定であれば「初対面」としておく
;;	IF 特定状況口上用フラグ:対象キャラ:0 == 0
;;		特殊状況フラグ '= "初対面"
;;	;逆に、口上カラーが設定ずみなのに「初対面」で入ってきてたら空欄に戻しておく
;;	;（２週目とかの対策のおまじないだよ）
;;	ELSEIF 特定状況口上用フラグ:対象キャラ:0 != 0 && 特殊状況フラグ == "初対面"
;;		特殊状況フラグ = ""
;;	ENDIF
;
;	;口上カラーフラグの確認
;	;１～３の範囲になければ１にしておく
;	;（カラーフラグのデフォルト値は１（システム用カラー））
;	SIF !INRANGE(特定状況口上用フラグ:対象キャラ:0, 1, 3)
;		特定状況口上用フラグ:対象キャラ:0 = 1
;
;
;	;設定スタート
;	$KOJO_EVENT_初対面_1147_設定トップ
;
;	CALL 口上変数初期化_1147
;
;	KSTR:(K++) = _DRAWLINE_
;	
;	KSTR:(K++) = 口上設定を行います。
;	KSTR:(K++) = 設定項目を選んでください。
;	KSTR:(K++) =  
;	KSTR:(K++) = _BUTTON_[初めに：当口上について]_
;	KSTR:(K++) = _BUTTON_[口上色について]_
;;	KSTR:(K++) = _BUTTON_[陥没乳首について！]_
;
;	KSTR:(K++) = _BUTTON_[HEIZENモードについて]_
;	KSTR:(K++) = _BUTTON_[デカパイ感謝モードについて]_
;	KSTR:(K++) = _BUTTON_[フレーバー素質の成長について]_
;
;
;
;	KSTR:(K++) = _BUTTON_[設定終了]_
;	KSTR:(K++) = 　
;	KSTR:(K++) = INPUTS
;	CALL 口上変数表示(対象キャラ, 0)
;	CALL 口上変数初期化_1147
;	IF 0
;	ELSEIF RESULTS == "[初めに：当口上について]"
;
;		KSTR:(K++) = 当口上はいくつかの設定があります。
;		KSTR:(K++) = 
;		KSTR:(K++) = 
;		KSTR:(K++) = 
;		KSTR:(K++) = 
;		KSTR:(K++) = 
;		KSTR:(K++) = ・口上色の変更
;		KSTR:(K++) = 　　口上表示色を変更することができます。
;		KSTR:(K++) = 
;		KSTR:(K++) = 
;		KSTR:(K++) = 
;		KSTR:(K++) = これらの設定は、独自コマンド『相談する』を選ぶことで、いつでも変更可能です。
;		KSTR:(K++) = 
;
;	ELSEIF RESULTS == "[口上色について]"
;
;	;当初は口上独自で陥没乳首にする予定だったが、CSV側で対応したためコメントアウト
;;	ELSEIF RESULTS == "[陥没乳首について！]"
;	ELSEIF RESULTS == "[HEIZENモードについて]"
;	ELSEIF RESULTS == "[フレーバー素質の成長について]"
;
;
;	ELSEIF RESULTS == "[設定終了]"
;		;下に飛ぶよ！
;		GOTO KOJO_EVENT_初対面_1147_設定終了
;	ENDIF
;
;	;選択後、設定トップに戻るよ
;	GOTO KOJO_EVENT_初対面_1147_設定トップ
;
;	CALL 口上変数表示(対象キャラ, 0)
;
;	;設定終了を選ぶとここに飛んでくる
;	$KOJO_EVENT_初対面_1147_設定終了
;
;
;
;
;=============================================================================================================================================================================================
;
;	IF 0
;	ELSEIF RESULTS == "[初めに：当口上について]"
;			KSTR:(K++) = 
;			KSTR:(K++) = 
;			KSTR:(K++) = 
;			KSTR:(K++) = 
;パチュリーは[陥没乳首]で爆乳前提
;オーナーチンポはデカい前提
;
;いやこの辺どろうらいん直後に表示でよくない？
;★クリトリスサイズ変更ONOFF
;★この辺の処理、「相談する」でも共通させたい
;
;
;		ELSEIF RESULTS == "[口上色について]"
;			$KOJO_EVENT_初対面_1147_設定_色
;			KSTR:(K++) = 口上色を設定します。
;			KSTR:(K++) = 下記選択肢から色を選んでください。
;			KSTR:(K++) = （当口上は、基本的に下記選択色で表示されます）
;			KSTR:(K++) =  
;			CALL 口上色変更_1147_カラーデフォルト
;			KSTR:(K++) = _BUTTON_[デフォルト（システム設定色）]_
;			CALL 口上色変更_1147_カラー薄紫
;			KSTR:(K++) = _BUTTON_[薄紫色]_
;			CALL 口上色変更_1147_カラー紫
;			KSTR:(K++) = _BUTTON_[紫色]_
;			;カラーを元に戻す
;			CALL 口上色変更_1147_カラーデフォルト
;			KSTR:(K++) = 　
;			KSTR:(K++) = INPUTS
;			CALL 口上変数表示(対象キャラ, 0)
;			CALL 口上変数初期化_1147
;			IF 0
;			ELSEIF RESULTS == "[デフォルト（システム設定色）]"
;				CALL 口上色変更_1147_カラーデフォルト
;				KSTR:(K++) = この色でよろしいですか？
;				特定状況口上用フラグ:対象キャラ:0 = 1
;				CALL 口上変数表示(対象キャラ, 0)
;			ELSEIF RESULTS == "[薄紫色]"
;				CALL 口上色変更_1147_カラー薄紫
;				KSTR:(K++) = この色でよろしいですか？
;				特定状況口上用フラグ:対象キャラ:0 = 2
;				CALL 口上変数表示(対象キャラ, 0)
;			ELSEIF RESULTS == "[紫色]"
;				CALL 口上色変更_1147_カラー紫
;				KSTR:(K++) = この色でよろしいですか？
;				特定状況口上用フラグ:対象キャラ:0 = 3
;				CALL 口上変数表示(対象キャラ, 0)
;			ENDIF
;			KSTR:(K++) =  
;			KSTR:(K++) = _BUTTON_[はい]_
;			KSTR:(K++) = _BUTTON_[いいえ]_
;			KSTR:(K++) = 　
;			KSTR:(K++) = INPUTS
;			CALL 口上変数表示(対象キャラ, 0)
;			CALL 口上変数初期化_1147
;			IF 0
;			ELSEIF RESULTS == "[はい]"
;				GOTO KOJO_EVENT_初対面_1147_設定トップ
;			ELSEIF RESULTS == "[いいえ]"
;				GOTO KOJO_EVENT_初対面_1147_設定_色
;			ENDIF
;		ELSEIF RESULTS == "[陥没乳首について！]"
;
;			CALL 口上変数初期化_1147
;
;			;３行開けます
;			KSTR:(K++) =  
;			KSTR:(K++) =  
;			KSTR:(K++) =  
;			KSTR:(K++) = ……ところで。
;			KSTR:(K++) = どうしてだろう。
;			KSTR:(K++) = 彼女の非常に豊かな乳房の先端は、陥没している気がする……
;			KSTR:(K++) = ※口上推奨は[陥没乳首]です※
;			KSTR:(K++) =  
;;			KSTR:(K++) = _BUTTON_[している（確信）。]_
;			KSTR:(K++) = _BUTTON_[している。間違いなかった。]_
;			KSTR:(K++) = _BUTTON_[いや気のせいだろ……]_
;			KSTR:(K++) = 　
;			KSTR:(K++) = INPUTS
;			CALL 口上変数表示(対象キャラ, 0)
;			CALL 口上変数初期化_1147
;			IF RESULTS == "[している。間違いなかった。]"
;				KSTR:(K++) = 「…………なに？」
;				KSTR:(K++) = 胸のあたりを凝視していることに気付いたのか、_呼び名_は首をひねった。
;				KSTR:(K++) = 自分の乳房が魅力的であることに気づいていないのだろうか……？
;				KSTR:(K++) = ともあれ、実際に陥没しているかは……仲良くなって、確かめることにしよう。
;			ELSEIF RESULTS == "[いや気のせいだろ……]"
;				KSTR:(K++) = 思い直した瞬間、このリゾートの星晶獣であるeraが、『そういうことになった』、と呟いた気配がした……
;				;えぇ～？　陥没乳首にしないのぉ～？
;				TALENT:対象キャラ:陥没乳首 = 0
;			ENDIF
;		;設定終了
;		ELSEIF RESULTS == "[設定終了]"
;		ENDIF
;
;		;選択後、設定トップに戻るよ
;		GOTO KOJO_EVENT_初対面_1147_設定トップ
;	ENDIF
;
;	;設定終了を選ぶとここに飛んでくる
;	$KOJO_EVENT_初対面_1147_設定終了
;
;
;
;