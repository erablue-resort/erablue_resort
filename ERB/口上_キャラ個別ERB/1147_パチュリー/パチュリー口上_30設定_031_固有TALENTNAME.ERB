;--------------------------------------------------
;●パチュリー・ノーレッジ口上＠erablue_resort用
;　――キャラが所持する素質の表示名を自由に変更する関数
;--------------------------------------------------

;--------------------------------------------------
;●ライセンス/更新履歴/概要/使用フラグ
;--------------------------------------------------
;　いずれもパチュリー口上_readme.txt記載に従う
;--------------------------------------------------

;--------------------------------------------------
;TALENT名・チップ説明処理
;--------------------------------------------------
@GET_固有TALENTNAME_1147(素質番号, 素質値, ツールチップフラグ, 対象キャラ)
#FUNCTION
#DIM 素質番号
#DIM 素質値
#DIM ツールチップフラグ
#DIM 対象キャラ

	SELECTCASE 素質番号
		;回復速度＝-1：回復遅い
		CASE 130
			;マイナス１でなければ変更しない
			IF 素質値 != -1
				;なにもしないわよ
			;名称
			ELSEIF !ツールチップフラグ
				;紫もやし……は、ちょっと迷うね。
;				TSTR:コマンド名受渡 = 紫もやし
				TSTR:コマンド名受渡 = もやし
			;ツールチップでの説明
			;なお、「%CALLNAME:MASTER%」などを使う場合、下記のように、「""」の前に半角＠を付けること。
			;＞TSTR:コマンド名受渡 = @"実は%CALLNAME:MASTER%のことが好き"
			ELSE
				TSTR:コマンド名受渡 '= "<br>■もやし"
				TSTR:コマンド名受渡 += "<br>運動苦手、運動不足の引きこもり。体力の回復する速度が遅い。"
				TSTR:コマンド名受渡 += "<br>ＨＰの回復量にマイナス補正が掛かる。"
				TSTR:コマンド名受渡 += "<br>（素質『回復遅い』の互換）"
			ENDIF
	ENDSELECT


