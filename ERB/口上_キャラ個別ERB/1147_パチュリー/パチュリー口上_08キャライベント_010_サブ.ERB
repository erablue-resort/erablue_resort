;--------------------------------------------------
;●パチュリー・ノーレッジ口上＠erablue_resort用
;　――サブイベントリスト
;--------------------------------------------------

;--------------------------------------------------
;●ライセンス/更新履歴/概要/使用フラグ
;--------------------------------------------------
;　いずれもパチュリー口上_readme.txt記載に従う
;--------------------------------------------------

;--------------------------------------------------
;〇サブイベントについて
;--------------------------------------------------
;サブイベントの類例は「温泉を作るために」「異世界の衣服？」など。
;なにかの要素開放またはその前段階/条件確認がほとんど。
;なので、口上固有イベントでやることは、あるかなあ？
;衣装の開放や、何らかの要素で居住人数の増加、とか……？
;あるいは、東方系キャラで「ダンジョン：幻想郷」を共有し、開放できるようにするとか？
;--------------------------------------------------

;なんかあると事故が発生するかもなので、この辺全部コメントアウトしておくわねッ。

;;--------------------------------------------------
;;〇パッチェさんサブイベ_YYYY（※「パッチェさんサブイベ_YYYY」を任意のイベント名に変えて使うこと、他との重複不可）
;;--------------------------------------------------
;;本体搭載のテンプレからもうちょっと進んで色々メモしておくわね
;;--------------------------------------------------
;@イベント発生条件_サブイベント_パッチェさんサブイベ_YYYY
;#DIM 対象キャラ
;
;	;引数で持ってきてないのでここで検索な
;	対象キャラ = キャラ検索("[動かない大図書館]パチュリー・ノーレッジ")
;
;	;開放条件式
;	;開放条件を満たしたら1を返すこと
;
;	;一回完了している場合は二度と出てこない場合
;	SIF イベント完了フラグ確認("パッチェさんサブイベ_YYYY")
;		RETURN 0
;
;	;恋慕でなければ不可
;	SIF !パッチェさん_陥落段階(対象キャラ, "恋慕")
;		RETURN 0
;	;パッチェさんが島にいない場合発生しない
;	SIF CFLAG:対象キャラ:滞在期間 < 1
;		RETURN 0
;
;	;調査結果を載せる言い訳みたいなものなので、ゼロを返して発生させない（ほんとはこのタイミングで１を返すよ）
;	RETURN 0
;
;@イベント消滅条件_サブイベント_パッチェさんサブイベ_YYYY
;#DIM 対象キャラ
;
;	;引数で持ってきてないのでここで検索な
;	対象キャラ = キャラ検索("[動かない大図書館]パチュリー・ノーレッジ")
;
;	;この条件を満たした時、イベントは発生してない扱いとなる
;	;発生条件＞消滅条件で見るので、発生と消滅を両方満たしている時は……
;	;＞「発生した！」というメッセージが出るが、発生していない
;	;……扱いになる
;	;そのため条件には注意すること
;
;	;消滅条件
;	;消したい場合はここで１を返す
;	;例えば、「発生条件を満たしている時のみ出現させたい」のなら以下のようになる
;	CALL イベント発生条件_サブイベント_パッチェさんサブイベ_YYYY
;	;発生条件OKの場合
;	IF RESULT
;		;0を返すことで、消滅させない
;		RETURN 0
;	;発生しない状況の場合
;	ELSE
;		;1を返し、消滅させる
;		RETURN 1
;	ENDIF
;
;	;ここは基本ゼロを返すほうがいいと思うが、ひとまず仮置きなので１を返しておく
;	RETURN 1
;
;
;
;@イベント関数_サブイベント_パッチェさんサブイベ_YYYY(ARG)
;#DIM 対象キャラ
;
;	;引数で持ってきてないのでここで検索な
;	対象キャラ = キャラ検索("[動かない大図書館]パチュリー・ノーレッジ")
;
;	DRAWLINE
;	PRINTL ［パッチェさんサブイベ_YYYY］
;	DRAWLINE
;	PRINTW
;	PRINTFORMW イベント文章
;	PRINTFORMW イベント文章
;	PRINTFORMW イベント文章
;
;	CALL イベント完了フラグ汎用処理("パッチェさんサブイベ_YYYY")
;
;
;





