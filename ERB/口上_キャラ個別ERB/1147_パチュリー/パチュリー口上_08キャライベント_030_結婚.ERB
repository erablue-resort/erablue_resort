;--------------------------------------------------
;●パチュリー・ノーレッジ口上＠erablue_resort用
;　――結婚イベント
;--------------------------------------------------

;--------------------------------------------------
;●ライセンス/更新履歴/概要/使用フラグ
;--------------------------------------------------
;　いずれもパチュリー口上_readme.txt記載に従う
;--------------------------------------------------

;--------------------------------------------------
;〇結婚イベントについて
;--------------------------------------------------
;特定のキャラをメインにするイベントカテゴリ
;汎用結婚イベントを発生させず、代わりにこのイベントを発生させる時に使用する
;「1147」を任意のキャラのNOに、「本とどっちが好きかしら」を任意のイベント名に変えて使うこと、他との重複不可
;--------------------------------------------------
;
;
;@イベントカテゴリ登録_キャライベント_本とどっちが好きかしら
;	RESULTS = 結婚イベント1147_
;
;
;@イベント発生条件_キャライベント_本とどっちが好きかしら
;#DIM 対象キャラ
;
;	;任意のキャラNO（ここの場合は対象のキャラのNO）を入れる
;	対象キャラ = キャラ検索("[動かない大図書館]パチュリー・ノーレッジ")
;
;	;結婚周りのフラグチェック
;	;理由がないなら残しておくことを推奨
;	SIF CFLAG:対象キャラ:結婚イベント解放 < 1
;		RETURN 0
;	SIF CFLAG:対象キャラ:結婚イベント禁止 > 0
;		RETURN 0
;
;	;その他の開放条件
;	;「これこれこうならRETURN 0（発生させない）」って書くといいかな？
;;	;例えば「処女ならダメ」
;;	IF TALENT:対象キャラ:処女 == 1
;;		RETURN 0
;;	;「定住してないとダメ」など
;;	ELSEIF TALENT:対象キャラ:定住者 == 0
;;		RETURN 0
;;	ENDIF
;
;
;	;開放条件を満たしたら（あるいは「開放しない条件」を全部回避したなら）1を返すこと
;	RETURN 1
;
;
;@イベント消滅条件_キャライベント_本とどっちが好きかしら
;#DIM 対象キャラ
;
;	;任意のキャラNO（ここの場合は対象のキャラのNO）を入れる
;	対象キャラ = キャラ検索("[動かない大図書館]パチュリー・ノーレッジ")
;
;	;この条件を満たした時、イベントは発生してない扱いとなる
;	;発生条件＞消滅条件で見るので、発生と消滅を両方満たしている時は「発生した！」というメッセージが出るが発生していない扱いになる
;	;そのため条件には注意すること
;
;
;	;消滅条件を満たしたら1を返す
;	;/
;	;結婚周りのフラグチェック
;	;理由がないなら残しておくことを推奨
;	SIF CFLAG:対象キャラ:結婚イベント解放 < 1
;		RETURN 1
;	SIF CFLAG:対象キャラ:結婚イベント禁止 > 0
;		RETURN 1
;	;結婚相手は滞在していることが必須
;	;なので滞在期間が１未満（リゾートにいない）なら、（一時的に）消滅する
;	SIF CFLAG:対象キャラ:滞在期間 < 1
;		RETURN 1
;
;	;その他条件
;;	;例えば「妊娠中はドレス着れないからダメ」とする場合など。
;;	IF TALENT:対象キャラ:妊娠
;;		RETURN 1
;;	ENDIF
;
;	;ここまで来たら消滅させない
;	RETURN 0
;
;	;その他、例えば、「発生条件を満たしている時のみ出現させたい」のなら以下のようになる
;	; CALL イベント発生条件_キャライベント_本とどっちが好きかしら
;	; IF RESULT
;	; 	;発生条件OKなので0を返す
;	; 	RETURN 0
;	; ELSE
;	; 	;発生条件アウトなので1を返す
;	; 	RETURN 1
;	; ENDIF
;
;@イベント関数_キャライベント_本とどっちが好きかしら(ARG)
;#DIM 対象キャラ
;
;	;任意のキャラNO（ここの場合は対象のキャラのNO）を入れる
;	対象キャラ = キャラ検索("[動かない大図書館]パチュリー・ノーレッジ")
;
;	DRAWLINE
;	PRINTFORML ［本とどっちが好きかしら］
;	DRAWLINE
;	PRINTFORMW 　
;	;-----------イベント本文 ｺｺｶﾗ-----------
;★未完成じゃぞ
;	PRINTFORMW 
;	PRINTFORMW 
;	PRINTFORMW イベント文章な
;	PRINTFORMW イベント文章な
;	PRINTFORMW イベント文章な
;	PRINTFORMW イベント文章な
;
;
;
;PRINTFORMW 　さて、%CSTR:対象キャラ:二人称%は我になんと呼んで欲しいかの？」
;PRINTL
;PRINTBUTTON "[0] 「あなた」と呼んでもらう", 0
;PRINTL
;PRINTBUTTON "[1] 「旦那様」と呼んでもらう", 1
;PRINTL
;PRINTBUTTON "[2] 任意入力", 2
;PRINTL
;DRAWLINE
;PRINTBUTTON @"[999] %CSTR:対象キャラ:二人称%のままがいい", 999
;PRINTL
;PRINTL
;BINPUT
;
;IF RESULT == 999
;	PRINTL
;ELSE
;	SELECTCASE RESULT
;		CASE 0
;			PRINTFORML パチュリー：
;			PRINTFORML 「『あなた』って呼んでほしいって？　調子に乗らないで。
;			PRINTFORML 　ただ結婚しただけなんだから呼び名を変える必要はないでしょう」
;			PRINTFORML 　
;			PRINTFORML ……そ、そうだろうか……？
;			PRINTFORML 　
;			PRINTFORML パチュリー：
;			PRINTFORML 「そもそも、『あなた』って呼ぶの、普通に使う二人称と混じるでしょう。
;			PRINTFORML 　こうやって直接呼ぶならともかく……非効率的。
;			PRINTFORML 　あなた、いくら私があなたを結婚してもいいくらいに思っているからって、あんまりそういうことを言うと愛想尽かすわよ。
;			PRINTFORML 　……あなた_H_」
;			PRINTFORML 　
;			PRINTFORML ……あれ、これは、呼んでもらっているのだろうか……？
;
;
;		CASE 1
;			CSTR:対象キャラ:二人称 = 旦那様
;		CASE 2
;			DRAWLINE
;			PRINTL 呼んでもらう二人称を入力してください。
;			DRAWLINE
;
;			INPUTS
;			CSTR:対象キャラ:二人称 '= RESULTS
;	ENDSELECT
;	PRINTL
;	PRINTL アニラ：
;	PRINTFORMW 「了解じゃ。では、これからは「%CSTR:対象キャラ:二人称%」と呼ぼうかの。
;	PRINTFORMW 　くふふ。%CSTR:対象キャラ:二人称%、%CSTR:対象キャラ:二人称%……なんだか気恥ずかしいが、嬉しくも感じるのじゃ」
;ENDIF
;
;
;
;
;
;
;
;
;
;
;
;	;-----------イベント本文 ｺｺﾏﾃﾞ-----------
;
;
;
;	;以下は汎用結婚イベで表示される、素質変化の文章（ここではいったん処理は行わない）
;	;必要ないなら消しても良い
;	;-----------素質変化テキスト ｺｺｶﾗ-----------
;	PRINTL 
;	SETCOLOR 0xFFFF00
;	PRINTFORMW %CALLNAME:対象キャラ%は［伴侶］を得ました。
;	RESETCOLOR
;	IF TALENT:対象キャラ:浮気 > 0
;		PRINTFORM %CALLNAME:対象キャラ%は
;		SETCOLOR カラーパレット("えっちな色")
;		PRINT [浮気]
;		RESETCOLOR
;		PRINT を失った……
;	ENDIF
;	IF TALENT:対象キャラ:恋人持ち != 2
;		SIF TALENT:対象キャラ:浮気 > 0
;			WAIT
;		PRINTFORM %CALLNAME:対象キャラ%は
;		SETCOLOR カラーパレット("えっちな色")
;		IF TALENT:対象キャラ:性別 == 1 || TALENT:対象キャラ:性別 == 3
;			PRINT [人妻]
;		ELSE
;			PRINT [既婚者]
;		ENDIF
;		RESETCOLOR
;		PRINT を得た！
;	ENDIF
;	PRINTL
;	PRINTL
;	FORCEWAIT
;	;-----------素質変化テキスト ｺｺﾏﾃﾞ-----------
;
;
;	;イベント中にアイテムやステータスの変動が起こる場合、以下の分岐の中に記述すること
;	;/
;	;イベント再視聴で無限に稼ぎが出来たりしないようにする措置
;	IF !FLAG:クリア済み視聴フラグ
;		;結婚イベントなので、伴侶素質を付与して履歴に登録する
;		;何かの理由があるなら消しても良いが、残すことを推奨
;		;-----------素質変化処理 ｺｺｶﾗ-----------
;		TALENT:対象キャラ:恋慕 = 2
;		初体験日:対象キャラ:伴侶 = DAY
;		CALL 履歴データベース登録(CFLAG:MASTER:人物番号, @"<img src='えっちハート'><font color='#%カラーパレット_HTML("赤ピンク")%'>%CALLNAME:対象キャラ%を[伴侶]陥落させた</font><img src='えっちハート'>","うふふ実績")
;		CALL 履歴データベース登録(CFLAG:対象キャラ:人物番号, @"<img src='えっちハート'><font color='#%カラーパレット_HTML("赤ピンク")%'>陥落素質[伴侶]を得た</font><img src='えっちハート'>","うふふ実績")
;		IF TALENT:対象キャラ:浮気 > 0
;			TALENT:対象キャラ:浮気 = 0
;		ENDIF
; ;		IF TALENT:対象キャラ:恋人持ち != 2
; ;			TALENT:対象キャラ:恋人持ち = 2
; ;		ENDIF
;		;-----------素質変化処理 ｺｺﾏﾃﾞ-----------
;	ENDIF
;
;	CALL イベント完了フラグ汎用処理("本とどっちが好きかしら")
;
;
;



