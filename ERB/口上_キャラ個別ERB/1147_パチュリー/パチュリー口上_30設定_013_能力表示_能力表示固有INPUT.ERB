;--------------------------------------------------
;●パチュリー・ノーレッジ口上＠erablue_resort用
;　――能力表示画面で任意のINPUTを機能させるための関数
;--------------------------------------------------

;--------------------------------------------------
;●ライセンス/更新履歴/概要/使用フラグ
;--------------------------------------------------
;　いずれもパチュリー口上_readme.txt記載に従う
;--------------------------------------------------

;--------------------------------------------------
;・使い方
;--------------------------------------------------
;「XXXX」(1147に置換しちゃったけど）をキャラクター番号に置換し、それぞれの関数に処理を入れる
;能力表示画面のキャラ固有領域にて事前に対応するボタン表示を用意すること！！
;/
;これらの処理を行った後、能力表示画面はRESTARTする
;変数などを操作して表示を制御する機能を想定
;（ワガママ言って用意してもらった、大感謝）
;--------------------------------------------------

;--------------------------------------------------
;通常能力画面
;--------------------------------------------------
@能力表示固有INPUT_通常能力_1147(対象キャラ, INPUT数値)
#DIM 対象キャラ
#DIM INPUT数値
;通常能力画面のボタン動作を入れる関数

RETURN 0


;;--------------------------------------------------
;;性的能力画面
;;--------------------------------------------------
;;これについては、大きくかかわる「パチュリー口上_能力表示_性的能力文章表現.ERB」に移行。
;;そっち参照な。
;;--------------------------------------------------
;@能力表示固有INPUT_性的能力_1147(対象キャラ, INPUT数値)
;#DIM 対象キャラ
;#DIM INPUT数値
;;性的能力画面のボタン動作を入れる関数
;
;RETURN 0

;--------------------------------------------------
;戦闘能力画面
;--------------------------------------------------
@能力表示固有INPUT_戦闘能力_1147(対象キャラ, INPUT数値)
#DIM 対象キャラ
#DIM INPUT数値
;戦闘能力画面のボタン動作を入れる関数

RETURN 0

;--------------------------------------------------
;キャラ解説画面
;--------------------------------------------------
@能力表示固有INPUT_キャラ解説_1147(対象キャラ, INPUT数値)
#DIM 対象キャラ
#DIM INPUT数値
;キャラ解説画面のボタン動作を入れる関数

RETURN 0

;--------------------------------------------------
;血縁関係画面
;--------------------------------------------------
@能力表示固有INPUT_血縁関係_1147(対象キャラ, INPUT数値)
#DIM 対象キャラ
#DIM INPUT数値
;血縁関係画面のボタン動作を入れる関数

RETURN 0

;--------------------------------------------------
;キャラ履歴画面
;--------------------------------------------------
@能力表示固有INPUT_キャラ履歴_1147(対象キャラ, INPUT数値)
#DIM 対象キャラ
#DIM INPUT数値
;キャラ履歴画面のボタン動作を入れる関数

RETURN 0
