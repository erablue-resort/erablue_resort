;****************************************
;COMABLEにキャラ固有の処理を入れるための関数
;****************************************
;・使い方
;「XXXX」をキャラクター番号に置換し、「YYYY」をCOM番号に置換。それぞれの関数に処理を入れる
;通常のCOMはYYYYをCOM番号、派生COMもYYYYに「10_1」などをいれる
;性処理の場合はYYYYは「手コキ処理」などが入る
;マッサージコマンドは「MASSAGE_1」のように記述
;むりやりうふふコマンドは「RAPE_1」のように記述
;
;なお、「コマンドを使用禁止する」ための関数のため、条件を無視してOKにする目的では使用出来ない
;
;特定の体位を封じる場合は、下にある体位を封じる場合の関数を使用する
;「425」をキャラクター番号に置換し、「体位名」を体位名称に置換。それぞれの関数に処理を入れる
;体位名は正常位、後背位、寝バック、対面座位、背面座位、騎乗位、側位、屈曲位、駅弁、背面駅弁、対面立位、立ちバック
;Ａ体位の場合はＡを入れる
;****************************************

;--------------------------------------------------
;プレイヤー側
;--------------------------------------------------
;@固有COMABLE_プレイヤー側_YYYY_425(対象キャラ)
;#FUNCTION
;#DIM 対象キャラ

;コマンドをキャラが実行する側の時の関数
;RETURN 0の時にコマンドが表示されなくなる

;RETURNF 1

;--------------------------------------------------
;ターゲット側
;--------------------------------------------------
@固有COMABLE_ターゲット側_4_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;アナル舐め
;多分アナルがないので使用不可

RETURNF 0

@固有COMABLE_ターゲット側_5_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;アナル愛撫
;多分アナルがないので使用不可

RETURNF 0

@固有COMABLE_ターゲット側_45_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;アナルバイブ挿入
;多分アナルがないので使用不可

RETURNF 0

@固有COMABLE_ターゲット側_46_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;アナルビーズ
;多分アナルがないので使用不可

RETURNF 0

@固有COMABLE_ターゲット側_62_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;Ａ挿入する
;多分アナルがないので使用不可

RETURNF 0

@固有COMABLE_ターゲット側_S9_4_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;アナニー
;多分アナルがないので使用不可

RETURNF 0

@固有COMABLE_ターゲット側_S9_8_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;アナルバイブオナニー
;多分アナルがないので使用不可

RETURNF 0

@固有COMABLE_ターゲット側_アナルセックス処理_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;アナルセックス処理
;多分アナルがないので使用不可

RETURNF 0

@固有COMABLE_ターゲット側_RAPE_3_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;アナルを舐める
;多分アナルがないので使用不可

@固有COMABLE_ターゲット側_RAPE_4_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;アナルを舐める
;多分アナルがないので使用不可

@固有COMABLE_ターゲット側_RAPE_22_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;アナルをほじる
;多分アナルがないので使用不可

@固有COMABLE_ターゲット側_RAPE_23_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;アナルでちんぽを犯す
;多分アナルがないので使用不可

;--------------------------------------------------
;体位を封じる場合_Ｖ_プレイヤー側
;--------------------------------------------------
@固有Ｖ体位ABLE_ターゲット側_体位名_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラが実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

;--------------------------------------------------
;体位を封じる場合_Ｖ_ターゲット側
;--------------------------------------------------
@固有Ｖ体位ABLE_ターゲット側_寝バック_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

@固有Ｖ体位ABLE_ターゲット側_背面座位_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

@固有Ｖ体位ABLE_ターゲット側_背面駅弁_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

@固有Ｖ体位ABLE_ターゲット側_立ちバック_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

@固有Ｖ体位ABLE_ターゲット側_側位_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

@固有Ｖ体位ABLE_ターゲット側_同時挿入_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

;--------------------------------------------------
;体位を封じる場合_Ａ_プレイヤー側
;--------------------------------------------------
;@固有Ａ体位ABLE_ターゲット側_体位名_425(対象キャラ)
;#FUNCTION
;#DIM 対象キャラ

;体位をキャラが実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

;RETURNF 1

;--------------------------------------------------
;体位を封じる場合_Ａ_ターゲット側
;--------------------------------------------------
@固有Ａ体位ABLE_ターゲット側_Ａ寝バック_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

@固有Ａ体位ABLE_ターゲット側_Ａ背面座位_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

@固有Ａ体位ABLE_ターゲット側_Ａ背面駅弁_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

@固有Ａ体位ABLE_ターゲット側_Ａ立ちバック_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

@固有Ａ体位ABLE_ターゲット側_Ａ側位_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1

@固有Ａ体位ABLE_ターゲット側_Ａ同時挿入_425(対象キャラ)
#FUNCTION
#DIM 対象キャラ

;体位をキャラに実行する側の時の関数
;RETURN 0の時に体位が表示されなくなる

RETURNF 1
