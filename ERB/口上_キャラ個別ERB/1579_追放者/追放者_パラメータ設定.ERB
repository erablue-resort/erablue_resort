;****************************************
;キャラの初期パラメータを口上側で制御するためのファイル
;****************************************
;・使い方
;「XXXX」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;****************************************

;--------------------------------------------------
;固有ぱんつ設定
;--------------------------------------------------
@固有ぱんつ設定_1579(対象キャラ)
#FUNCTIONS
#DIM 対象キャラ

;マナ+5
IF GETBIT(陥落チェック(対象キャラ), 0)
	RESULTS = ☆宵闇を纏いし追放者のパンティー
ELSE
	RESULTS = 宵晒の追放者のパンティー
ENDIF
