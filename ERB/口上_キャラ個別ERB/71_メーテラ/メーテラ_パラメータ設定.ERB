;****************************************
;キャラの初期パラメータを口上側で制御するためのファイル
;****************************************
;・使い方
;「71」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;****************************************

;--------------------------------------------------
;初期パラメータ変更
;--------------------------------------------------
@口上側初期パラメータ変更_71
;NEWGAME時に呼ばれる関数、ロード時には呼ばれないことに注意
;TALENTをONOFFしたり、EXPの取得や初期ABLの変更などをここに記述すること
LOCAL = GETCHARA(71)

;キャラクターが自分を指す際の呼び方
CSTR:LOCAL:一人称 = アタシ

;キャラクターがあなたを指す際の呼び方
CSTR:LOCAL:二人称 = アンタ

;キャラクターが固有に持つ独自の好感度要素の名前
;通常上昇しないので、設定した場合はキャラ固有処理_71.ERBなどで上昇処理も作ること
CSTR:LOCAL:キャラ固有好感度要素 = 

;プレゼントの好み
;基本的にはCSVで設定すべきだが、口上特有の設定をする場合はここに記述
;CSTR:LOCAL:プレゼント好み = 

;身長やバストの数値を指定する・変更する場合
BASE:LOCAL:身長 = 1690
; BASE:LOCAL:バスト = 

;素質,Ａ感度を排除。　アナルが弱いのは女騎士の特権なので（
;代わりにＢ感度を生やす
;-1=鈍感 1=敏感 -2=存在しない
TALENT:LOCAL:Ｂ感度 = 1
TALENT:LOCAL:Ａ感度 = 0

;70,快感応答
	; -1=快感の否定,;コマンドを実行しにくい。PALAM:欲情が上がりにくい
	; 1=快感に素直,;コマンドを実行しやすい。ABL:欲望と奉仕精神が上げやすい。PALAM:欲情が上がりやすい
TALENT:LOCAL:快感応答 = 1

;80,倒錯的,
	;SM系調教の効果が高い。マゾ系、中毒系のABLが上がりやすい
TALENT:LOCAL:倒錯的 = 1

;110,濡れやすさ,
	;-1=濡れにくい 1=濡れやすい
TALENT:LOCAL:濡れやすさ = 1

;ギャルと言うより……平成のマイルドヤンキー（
;230,ギャル
TALENT:LOCAL:ギャル = 1

;各々の解釈次第で現行のセフレがいるとノイズになるのでオミット
;なお、「現在」セフレがいないだけで、会話中に過去の彼氏、セフレ匂わせは出てくるものとする（
;244,セフレあり
;	;1:セフレあり
;TALENT:LOCAL:セフレあり = 1

;メーテラ姉様はTPOに合わせて態度を変えれるので（クリスマスフェイトエピソード参照）、反抗的、プライドが高い＝常に高慢なタイプではないと言う自己解釈
;要するに破廉恥姉様は公式も認める尻軽ビッチ枠なのに（偏見）、era的には攻略難易度の高い高難易度キャラになってしまうので弱体化（
;応答：生意気は公式でも常に生意気な態度なので残す（

;11,態度,
	;-1=素直,
	;1=反抗的,
TALENT:LOCAL:態度 = 0
;13,プライド,
	;-1=プライド低い,
	;1=プライド高い,
TALENT:LOCAL:プライド = 0

ABL:LOCAL:Ｃ感覚 = 1
ABL:LOCAL:Ｂ感覚 = 1
ABL:LOCAL:Ｖ感覚 = 1
ABL:LOCAL:Ｓ感覚 = 1
ABL:LOCAL:欲望 = 2
ABL:LOCAL:露出癖 = 1
ABL:LOCAL:サド性感 = 1

;天才なのでＡ以外は全部上げる（
ABL:LOCAL:指テク = 1
ABL:LOCAL:舌技 = 1
ABL:LOCAL:胸 = 1
ABL:LOCAL:腰使い = 1
ABL:LOCAL:膣 = 1
ABL:LOCAL:道具扱い = 1
ABL:LOCAL:技巧 = 1

ABL:LOCAL:話術技能 = 1

;初期値
;経験詐称
EXP:LOCAL:Ｃ経験 = 30
EXP:LOCAL:Ｖ経験 = 20
EXP:LOCAL:Ｂ経験 = 30
EXP:LOCAL:Ａ経験 = 10
EXP:LOCAL:Ｓ経験 = 30

EXP:LOCAL:Ｃ絶頂経験 = 5
EXP:LOCAL:Ｖ絶頂経験 = 3
EXP:LOCAL:Ｂ絶頂経験 = 5
EXP:LOCAL:Ｓ絶頂経験 = 5

EXP:LOCAL:絶頂経験 = 15
EXP:LOCAL:精液経験 = 20

;経験数は自己申告制なので、手淫、口淫、パイズリは申告されていない。eraでよくあるやつ
EXP:LOCAL:Ｖ性交経験 = 15
EXP:LOCAL:自慰経験 = 15
EXP:LOCAL:キス経験 = 20

;--------------------------------------------------
;ロード時パラメータ変更
;--------------------------------------------------
@口上側LOADENDパラメータ変更_71
;ロード時に呼ばれる変数。NEWGAME時に呼ばれないことに注意
;初期パラメータ関数だけだと後から追加した時にバグるため追加
LOCAL = GETCHARA(71)

;キャラクターが自分を指す際の呼び方
IF CSTR:LOCAL:一人称 == ""
	CSTR:LOCAL:一人称 = アタシ
ENDIF
;キャラクターがあなたを指す際の呼び方
IF CSTR:LOCAL:二人称 == ""
	CSTR:LOCAL:二人称 = アンタ
ENDIF

IF BASE:LOCAL:身長 > 1690 || BASE:LOCAL:身長 < 1690
	BASE:LOCAL:身長 = 1690
ENDIF

;キャラクターが固有に持つ独自の好感度要素の名前
;通常上昇しないので、設定した場合はキャラ固有処理_71.ERBなどで上昇処理も作ること
IF CSTR:LOCAL:キャラ固有好感度要素 == ""
	CSTR:LOCAL:キャラ固有好感度要素 = 
ENDIF

IF TALENT:LOCAL:態度 == 1
	TALENT:LOCAL:態度 = 0
ENDIF

IF TALENT:LOCAL:プライド == 1
	TALENT:LOCAL:プライド = 0
ENDIF

IF TALENT:LOCAL:快感応答 == 0
	TALENT:LOCAL:快感応答 = 1
ENDIF

IF TALENT:LOCAL:倒錯的 == 0
	TALENT:LOCAL:倒錯的 = 1
ENDIF

IF TALENT:LOCAL:濡れやすさ == 0
	TALENT:LOCAL:濡れやすさ = 1
ENDIF

IF TALENT:LOCAL:ギャル == 0
	TALENT:LOCAL:ギャル = 1
ENDIF

IF ABL:LOCAL:Ｃ感覚 == 0
	ABL:LOCAL:Ｃ感覚 = 1
ENDIF
IF ABL:LOCAL:Ｂ感覚 == 0
	ABL:LOCAL:Ｂ感覚 = 1
ENDIF
IF ABL:LOCAL:Ｖ感覚 == 0
	ABL:LOCAL:Ｖ感覚 = 1
ENDIF
IF ABL:LOCAL:Ｓ感覚 == 0
	ABL:LOCAL:Ｓ感覚 = 1
ENDIF
IF ABL:LOCAL:欲望 < 2
	ABL:LOCAL:欲望 = 2
ENDIF
IF ABL:LOCAL:露出癖 == 0
	ABL:LOCAL:露出癖 = 1
ENDIF
IF ABL:LOCAL:サド性感 == 0
	ABL:LOCAL:サド性感 = 1
ENDIF

;天才なのでＡ以外は全部上げる（
IF ABL:LOCAL:指テク == 0
	ABL:LOCAL:指テク = 1
ENDIF

IF ABL:LOCAL:舌技 == 0
	ABL:LOCAL:舌技 = 1
ENDIF
IF ABL:LOCAL:胸 == 0
	ABL:LOCAL:胸 = 1
ENDIF
IF ABL:LOCAL:腰使い == 0
	ABL:LOCAL:腰使い = 1
ENDIF
IF ABL:LOCAL:膣 == 0
	ABL:LOCAL:膣 = 1
ENDIF
;「アタシね～思うんだけどさぁ。能力：道具扱いって、肉〇器経験Lv1みたいなついてたら、あの子マゾ奴隷経験あるんだ～みたいな感じ。あると思わない？（ないです」
IF ABL:LOCAL:道具扱い == 0
	ABL:LOCAL:道具扱い = 1
ENDIF
IF ABL:LOCAL:技巧 == 0
	ABL:LOCAL:技巧 = 1
ENDIF

IF ABL:LOCAL:話術技能 == 0
	ABL:LOCAL:話術技能 = 1
ENDIF

;初期値
;経験詐称
IF EXP:LOCAL:Ｃ経験 < 31
	EXP:LOCAL:Ｃ経験 += 30
ENDIF
IF EXP:LOCAL:Ｖ経験 < 21
	EXP:LOCAL:Ｖ経験 += 20
ENDIF
IF EXP:LOCAL:Ｂ経験 < 31
	EXP:LOCAL:Ｂ経験 += 30
ENDIF
IF EXP:LOCAL:Ａ経験 < 11
	EXP:LOCAL:Ａ経験 += 10
ENDIF
IF EXP:LOCAL:Ｓ経験 < 31
	EXP:LOCAL:Ｓ経験 += 30
ENDIF

IF EXP:LOCAL:絶頂経験 < 16
	EXP:LOCAL:絶頂経験 += 15
ENDIF
IF EXP:LOCAL:精液経験 < 21
	EXP:LOCAL:精液経験 += 20
ENDIF


;経験数は自己申告制なので、手淫、口淫、パイズリは申告されていない。eraでよくあるやつ
IF EXP:LOCAL:Ｖ性交経験 < 16
	EXP:LOCAL:Ｖ性交経験 += 15
ENDIF
IF EXP:LOCAL:自慰経験 < 16
	EXP:LOCAL:自慰経験 += 15
ENDIF
IF EXP:LOCAL:キス経験 < 21
	EXP:LOCAL:キス経験 += 20
ENDIF


;--------------------------------------------------
;弱点コマンド制御
;--------------------------------------------------
@弱点コマンド変更_71
;各変数にコマンド番号を入れること
;派生コマンドや専用コマンドは「370_1」のように記述すること

;ここで指定しなかった枠はランダムで埋められる
;ランダムで埋める場合、枠０・１はセクハラコマンド、２・３はうふふコマンド、４は完全ランダムに選択される
LOCAL = GETCHARA(71)

弱点コマンド枠:LOCAL:0 = 320
;尻を揉む　身体に触れる～からの分岐では無効
弱点コマンド枠:LOCAL:1 = 321
;胸を揉む　身体に触れる～からの分岐では無効
弱点コマンド枠:LOCAL:2 = 60
;Ｖ挿入する
弱点コマンド枠:LOCAL:3 = 60_1
;Ｇスポ責め
;
; 弱点コマンド枠:LOCAL:4 = 

;--------------------------------------------------
;フレーバー素質設定
;--------------------------------------------------
@フレーバー素質初期設定変更_71
LOCAL = GETCHARA(71)

フレーバー素質:LOCAL:素質表示設定 = 1

;以下の要素は0の時、表示されない
フレーバー素質:LOCAL:陰毛濃さ = 0
;1:パイパン 2:薄め 3:普通 4:濃いめ 5:剛毛
フレーバー素質:LOCAL:乳首大きさ = 0
;1:豆粒大 2:小さめ 3:平均的 4:大きめ 5:巨乳首
フレーバー素質:LOCAL:乳輪大きさ = 0
;1:極小 2:小さめ 3:平均的 4:大きめ 5:巨乳輪
フレーバー素質:LOCAL:性器種類 = 0
;1:ミミズ千匹 2:数の子天井 3:タコツボ 4:巾着 5:俵締め
フレーバー素質:LOCAL:クリ大きさ = 0
;1:小さめ 2:平均的 3:大きめ
;（4にクリチンポが設定されているが、後天的に変化するパターンを想定しているので初期値にはしないこと）
フレーバー素質:LOCAL:ほくろ位置 = 0
;ビット管理
;ビット0：乳首付近　1：性器付近　2：アナル付近　3：陰茎（ふたなり時のみ）


;--------------------------------------------------
;固有好感度設定
;--------------------------------------------------
@キャラ固有好感度関係名_71(ARG)
;好感度要素の恋心、信頼、友情、性愛以外の要素の段階名を設定する
;初期パラメータ変更の方で固有好感度を付けておかないと発動しないので注意

SELECTCASE ARG
	CASE IS == 関係閾値:0
		RESULTS = 初対面
	CASE IS <= 関係閾値:1
		RESULTS = 
	CASE IS <= 関係閾値:2
		RESULTS = 
	CASE IS <= 関係閾値:3
		RESULTS = 
	CASE IS <= 関係閾値:4
		RESULTS = 
	CASE IS <= 関係閾値:5
		RESULTS = 
ENDSELECT
RETURN

;--------------------------------------------------
;固有ぱんつ設定
;--------------------------------------------------
@固有ぱんつ設定_71(対象キャラ)
#FUNCTIONS
#DIM 対象キャラ

;条件を分岐させ、パンツの名前をRESULTSにいれる
;RESULTS = 白の清楚な下着
;など
;現状、「スカートたくしあげ」のオートコマンドのみで使用

;--------------------------------------------------
;固有子供命名設定
;--------------------------------------------------
@固有子供命名設定_71(対象キャラ, 子供性別, 子供順番, 案出し回数)
#DIM 対象キャラ
#DIM 子供性別
#DIM 子供順番
#DIM 案出し回数
VARSET RESULTS

;子供性別は性別TALENTと共通、1:女性　2:男性　3:ふたなり
;子供順番は「今回生んだ子で」何番目の子供か（つまり双子以上の時に使用）
;案出し回数はこの関数が呼ばれた回数（ランダムでなく順番に名前候補を提案させたい場合に使用）

;RESULTS = 子供の名前

;--------------------------------------------------
;固有体力補正
;--------------------------------------------------
@固有体力補正_71(対象キャラ, 体力最大値)
#DIM 対象キャラ
#DIM 体力最大値

;体力の計算を一通り終えた後に掛ける補正
;ただし最大値9999は変わらない

RETURN 体力最大値

;--------------------------------------------------
;固有性欲補正
;--------------------------------------------------
@固有性欲補正_71(対象キャラ, 性欲最大値)
#DIM 対象キャラ
#DIM 性欲最大値

;性欲の計算を一通り終えた後に掛ける補正
;ただし最大値9999は変わらない

RETURN 性欲最大値

