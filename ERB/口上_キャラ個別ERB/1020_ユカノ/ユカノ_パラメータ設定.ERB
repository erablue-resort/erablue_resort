;--------------------------------------------------
;キャラの初期パラメータを口上側で制御するためのファイル
;--------------------------------------------------
;・使い方
;「1020」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;--------------------------------------------------
;ver240818
;--------------------------------------------------
;変更全般
;--------------------------------------------------
;--------------------------------------------------
;キャラの初期パラメータを口上側で制御するためのファイル
;--------------------------------------------------
;・使い方
;「1020」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;--------------------------------------------------
;ver240818
;--------------------------------------------------
;変更全般
;--------------------------------------------------
@口上側初期パラメータ変更_1020
;NEWGAME時に呼ばれる関数、ロード時には呼ばれないことに注意
;TALENTをONOFFしたり、EXPの取得や初期ABLの変更などをここに記述すること
LOCAL = FINDCHARA(NO, 1020)

;キャラクターが自分を指す際の呼び方
CSTR:LOCAL:一人称 = 私

;キャラクターがあなたを指す際の呼び方
CSTR:LOCAL:二人称 = NAME:TARGET＝サン

;キャラクターが固有に持つ独自の好感度要素の名前
;通常上昇しないので、設定した場合はキャラ固有処理_1020.ERBなどで上昇処理も作ること
CSTR:LOCAL:キャラ固有好感度要素 = 

;身長やバストの数値を指定する・変更する場合
; BASE:LOCAL:身長 = 
; BASE:LOCAL:バスト = 

@口上側LOADENDパラメータ変更_1020
;ロード時に呼ばれる変数。NEWGAME時に呼ばれないことに注意
;上の関数だけだと後から追加した時にバグるため追加
LOCAL = FINDCHARA(NO, 1020)

;キャラクターが自分を指す際の呼び方
IF CSTR:LOCAL:一人称 == "私"
	CSTR:LOCAL:一人称 = 私
ENDIF
;キャラクターがあなたを指す際の呼び方
IF CSTR:LOCAL:二人称 == "NAME:TARGET＝サン"
	CSTR:LOCAL:二人称 = NAME:TARGET＝サン
ENDIF

;キャラクターが固有に持つ独自の好感度要素の名前

IF CSTR:LOCAL:キャラ固有好感度要素 == ""
	CSTR:LOCAL:キャラ固有好感度要素 = 
ENDIF


@キャラ固有好感度関係名_1020(ARG)
SELECTCASE ARG
	CASE IS == 関係閾値:0
		RESULTS = 初対面
	CASE IS <= 関係閾値:1
		RESULTS = 
	CASE IS <= 関係閾値:2
		RESULTS = 
	CASE IS <= 関係閾値:3
		RESULTS = 
	CASE IS <= 関係閾値:4
		RESULTS = 
	CASE IS <= 関係閾値:5
		RESULTS = 
ENDSELECT
RETURN

; @固有ぱんつ設定_1020(対象キャラ)
; #FUNCTIONS
; #DIM 対象キャラ

; ;条件を分岐させ、パンツの名前をRESULTSにいれる
;RESULTS = 
; ;など


;--------------------------------------------------
;キャラについての解説
;--------------------------------------------------
@口上_ステータス画面_キャラ解説_1020(対象キャラ)
#DIM 対象キャラ
CALL 口上変数初期化

KSTR:(K++) = ◆忍◆
KSTR:(K++) = _B_ニンジャ名鑑#1020 【ドラゴン・ユカノ】_
KSTR:(K++) = 長い黒髪に勝気な美貌、赤いニンジャ装束が特徴。バストとヒップは豊満だ。
KSTR:(K++) = 　
KSTR:(K++) = その正体は悠久を生きるリアルニンジャであり、古のニンジャ大戦を戦ったニンジャ六騎士「ドラゴン・ニンジャ」その人。
KSTR:(K++) = ドラゴン・ニンジャは自分自身がニューロンに仕込んだ「自衛システム」によって度々記憶のリセットが為され、これにより彼女は数多くの異なる人生を送ってきた。
KSTR:(K++) = 「ドラゴン・ユカノ」としての生もその一つではあるが、度重なる偶然の結果により、現在の人格は「ユカノ」として安定した状態にある。
KSTR:(K++) = 記憶と共にかつての力の多くは失われているものの、それでもなおドラゴン・ドージョーの始祖としてのカラテは決して侮れるものではない。
KSTR:(K++) = ◆殺◆
CALL キャラ解説表示用(対象キャラ)
RETURN RESULT
