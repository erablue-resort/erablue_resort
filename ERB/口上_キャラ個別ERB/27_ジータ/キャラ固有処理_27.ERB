@PALAMUP_CHARAEVENT_27(PALAM種類変数, 変化量)
#DIM キャラ番号
#DIM PALAM種類変数
#DIM 変化量
;PALAMが変化した直後に飛んでくる処理
;より正確に言えばSOURCE_CALC_系関数から飛んでくるので、直接PALAMをいじっても飛んで来ない

キャラ番号 = FINDCHARA(NO, 28)
IF CFLAG:キャラ番号:滞在期間 < 0
	RETURN 0
ENDIF
SELECTCASE PALAM種類変数
	CASE 0
		CUP:キャラ番号:0 += 変化量
	CASE 1
		CUP:キャラ番号:1 += 変化量
	CASE 2
		CUP:キャラ番号:2 += 変化量
	CASE 3
		CUP:キャラ番号:3 += 変化量
	CASE 4
		CUP:キャラ番号:4 += 変化量
	CASEELSE
		RETURN 0
ENDSELECT
IF !CFLAG:キャラ番号:同室
	CALL ORGASM_ADD(キャラ番号)
ENDIF
