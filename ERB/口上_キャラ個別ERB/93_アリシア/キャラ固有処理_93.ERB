@TURNEND_CHARAEVENT_93
;コマンド実行ごとに呼ばれる処理

LOCAL = GETCHARA(93)
IF 同行候補キャラ確率:LOCAL:0 == 100 && TALENT:LOCAL:セフレ
	;若いツバメのセフレが出来た途端、娘の同行確率を下げるふしだらな母
	同行候補キャラ確率:LOCAL:0 = 10
	PRINTW アリシアは[セフレ]を得たことで、アリーザの同行確率が減少した。
	CALL 履歴データベース登録(CFLAG:LOCAL:人物番号, @"%CALLNAME:MASTER%という若いツバメに夢中になっている","日常")
ENDIF
