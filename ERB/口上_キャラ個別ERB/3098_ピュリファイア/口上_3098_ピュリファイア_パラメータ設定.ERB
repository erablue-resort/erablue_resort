;--------------------------------------------------
;●ライセンス/更新履歴/概要/使用フラグ
;--------------------------------------------------
;　いずれもピュリファイア口上_readme.txt記載に従う
;--------------------------------------------------
;****************************************
;キャラの初期パラメータを口上側で制御するためのファイル
;****************************************
;・使い方
;「3098」をキャラクター番号に置換し、それぞれの関数に処理を入れる
;****************************************

;--------------------------------------------------
;初期パラメータ変更
;--------------------------------------------------
@口上側初期パラメータ変更_3098
;NEWGAME時に呼ばれる関数、ロード時には呼ばれないことに注意
;TALENTをONOFFしたり、EXPの取得や初期ABLの変更などをここに記述すること
LOCAL = GETCHARA(3098)

;キャラクターが自分を指す際の呼び方
CSTR:LOCAL:一人称 = わたくし

;キャラクターがあなたを指す際の呼び方
IF CSTR:LOCAL:二人称 == "あなた"
	CSTR:LOCAL:二人称 = オーナーさま
ELSEIF TALENT:LOCAL:恋慕
	CSTR:LOCAL:二人称 = %CALLNAME:MASTER%さま
ELSE
	CSTR:LOCAL:二人称 = %CALLNAME:MASTER%様
ENDIF

;キャラクターが固有に持つ独自の好感度要素の名前
;通常上昇しないので、設定した場合はキャラ固有処理_3098.ERBなどで上昇処理も作ること
CSTR:LOCAL:キャラ固有好感度要素 = 

;プレゼントの好み
;基本的にはCSVで設定すべきだが、口上特有の設定をする場合はここに記述
;CSTR:LOCAL:プレゼント好み = 

;身長やバストの数値を指定する・変更する場合
; BASE:LOCAL:身長 = 
; BASE:LOCAL:バスト = 

;--------------------------------------------------
;ロード時パラメータ変更
;--------------------------------------------------
@口上側LOADENDパラメータ変更_3098
;ロード時に呼ばれる変数。NEWGAME時に呼ばれないことに注意
;初期パラメータ関数だけだと後から追加した時にバグるため追加
LOCAL = GETCHARA(3098)

;キャラクターが自分を指す際の呼び方
CSTR:LOCAL:一人称 = わたくし

;キャラクターがあなたを指す際の呼び方
;キャラクターがあなたを指す際の呼び方
IF CSTR:LOCAL:二人称 == "あなた"
	CSTR:LOCAL:二人称 = オーナーさま
ELSEIF TALENT:LOCAL:恋慕
	CSTR:LOCAL:二人称 = %CALLNAME:MASTER%さま
ELSE
	CSTR:LOCAL:二人称 = %CALLNAME:MASTER%様
ENDIF

;キャラクターが固有に持つ独自の好感度要素の名前
;通常上昇しないので、設定した場合はキャラ固有処理_3098.ERBなどで上昇処理も作ること
IF CSTR:LOCAL:キャラ固有好感度要素 == ""
	CSTR:LOCAL:キャラ固有好感度要素 = 
ENDIF

;--------------------------------------------------
;弱点コマンド制御
;--------------------------------------------------
@弱点コマンド変更_3098
;各変数にコマンド番号を入れること
;派生コマンドや専用コマンドは「370_1」のように記述すること

;ここで指定しなかった枠はランダムで埋められる
;ランダムで埋める場合、枠０・１はセクハラコマンド、２・３はうふふコマンド、４は完全ランダムに選択される
LOCAL = GETCHARA(3098)

弱点コマンド枠:LOCAL:0 = 45_1
弱点コマンド枠:LOCAL:1 = 7
弱点コマンド枠:LOCAL:2 = 90
弱点コマンド枠:LOCAL:3 = 306_5
弱点コマンド枠:LOCAL:4 = 5


;--------------------------------------------------
;キャラについての解説
;--------------------------------------------------
;@口上_ステータス画面_キャラ解説_3098(対象キャラ)
;#DIM 対象キャラ
;EXキャラの出典を解説したり、キャラの設定・背景を解説するもの

;KSTR:(K++) = メーカネース・ナーヴィス。等比加速情報素子「マナ」を発見した結果、星間文明へと発展した一つの世界。
;KSTR:(K++) = 元魔法少女にして、今は浄化の魔女と呼ばれる。魔女としての力量は衛星級。
;KSTR:(K++) = 過去の魔災害を解決するにあたって受けた凌辱の結果、性欲の強いタチレズと化してしまった。
;KSTR:(K++) = ピュリファイアと同期。彼女のことを付け狙っている。
;KSTR:(K++) = 料理好き。特に辛いもの。
