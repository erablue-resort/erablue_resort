;==================================================================================================
@IS_ゆきずりセックス中(ARG)
#FUNCTION
;-------------------------------------------------
;ARG番のキャラがゆきずりセックス中であるかを判定する関数
;--------------------------------------------------------------------------------------------------
RETURNF TCVAR:ARG:ゆきずりセックス_行為中


;==================================================================================================
@IS_ゆきずり覗かれ中(ARG)
#FUNCTION
;-------------------------------------------------
;ARG番のキャラがゆきずり覗かれ中であるかを判定する関数
;--------------------------------------------------------------------------------------------------
RETURNF TCVAR:ARG:ゆきずりセックス_覗かれ中


;==================================================================================================
@ゆきずり覗き中？(ARG)
#FUNCTION
;-------------------------------------------------
;ゆきずりの覗き状態を判定する関数
;引数のキャラがゆきずり中かつTARGETあり、覗かれ中であれば1
;どれか一つでも満たない場合は0を返す
;--------------------------------------------------------------------------------------------------
SIF IS_ゆきずりセックス中(ARG) && IS_ゆきずり覗かれ中(ARG) && TARGET == ARG
	RETURNF 1


;==================================================================================================
@使い魔覗き対象？(ARG)
#FUNCTION
;-------------------------------------------------
;使い魔覗きされているか判定する関数
;ARGが「使い魔覗きの対象」もしくは「使い魔覗き対象の相手」であれば1を、それ以外は0を返す
;--------------------------------------------------------------------------------------------------
SIF CFLAG:MASTER:使い魔覗き状態 <= 0 || CFLAG:MASTER:使い魔覗き状態 >= CHARANUM
	RETURNF 0
SIF ARG == CFLAG:MASTER:使い魔覗き状態 || ARG == TCVAR:(CFLAG:MASTER:使い魔覗き状態):ゆきずりセックス_相手キャラID
	RETURNF 1

RETURNF 0


;==================================================================================================
@同行者がいる？
#FUNCTION
;-------------------------------------------------
;あなたに同行者がいるかを確認し、一人でもいる場合は1を、いない場合は0を返します。
;--------------------------------------------------------------------------------------------------
FOR LOCAL, 0, CHARANUM
	SIF LOCAL == MASTER
		CONTINUE
	
	IF CFLAG:LOCAL:同行 > 0 || CFLAG:LOCAL:デート == MASTER + 100
		RETURNF 1
	ENDIF
NEXT

RETURNF 0


;==================================================================================================
@ゆきずりセックス_本人が誘った？(ARG)
#FUNCTION
;-------------------------------------------------
;キャラクターがNPCを誘っている状況であれば1を返します
;--------------------------------------------------------------------------------------------------
RETURNF !GETBIT(TCVAR:ARG:ゆきずりセックス_誘い, 0)


;==================================================================================================
@ゆきずりセックス_本人が誘われた？(ARG)
#FUNCTION
;-------------------------------------------------
;キャラクターがNPCに誘われている状況であれば1を返します
;--------------------------------------------------------------------------------------------------
RETURNF GETBIT(TCVAR:ARG:ゆきずりセックス_誘い, 0)


;==================================================================================================
@ゆきずりセックス_その場？(ARG)
#FUNCTION
;-------------------------------------------------
;ゆきずり場所がその場の物陰であれば1を返します
;--------------------------------------------------------------------------------------------------
RETURNF !GETBIT(TCVAR:ARG:ゆきずりセックス_誘い, 1)


;==================================================================================================
@ゆきずりセックス_自室？(ARG)
#FUNCTION
;-------------------------------------------------
;ゆきずり場所が自室であれば1を返します
;--------------------------------------------------------------------------------------------------
RETURNF GETBIT(TCVAR:ARG:ゆきずりセックス_誘い, 1)


;==================================================================================================
@ゆきずりセックス_逆アナル発生中(ARG)
#FUNCTION
;-------------------------------------------------
;NPCが「男性」で、本人が「女性」であり、「Ａ挿入系のプレイヤー」であれば1を返す
;--------------------------------------------------------------------------------------------------
IF IS_MALE(TCVAR:ARG:ゆきずりセックス_相手キャラID)
	IF IS_FEMALE(ARG) && MODE_確認_プレイヤー側("Ａ挿入系", ARG) >= 0
		RETURNF 1
	ENDIF
ENDIF
RETURNF 0


;==================================================================================================
@ゆきずりセックス_プレイ種別取得(ARG)
#FUNCTION
;-------------------------------------------------
;体位モード名を基にプレイ種別を返す関数
;--------------------------------------------------------------------------------------------------
#DIMS 実行者モード名
#DIMS 対象者モード名
;--------------------------------------------------------------------------------------------------
IF MODE_存在判定_いずれか側("Ｖ挿入系", ARG)
	RETURNF 1
ELSEIF MODE_存在判定_いずれか側("Ａ挿入系", ARG)
	RETURNF 2
ELSE
	RETURNF 3
ENDIF


;==================================================================================================
@ゆきずりセックス_プレイ内容取得(ARG)
#FUNCTIONS
;-------------------------------------------------
;対象側で最初に見つかったモードを取得
;--------------------------------------------------------------------------------------------------
#DIM モード行
#DIMS プレイ内容
;--------------------------------------------------------------------------------------------------
プレイ内容 '= ""

;モード行 = MODE_確認_ターゲット側("ALL", ARG)
;対象側に見つからなければ、実行側を探す
;IF モード行 == -1
;    モード行 = MODE_確認_プレイヤー側("ALL", ARG)
;ENDIF
モード行 = MODE_確認_完全一致("ALL", ARG, TCVAR:ARG:ゆきずりセックス_相手キャラID)
;対象側に見つからなければ、実行側を探す
IF モード行 == -1
    モード行 = MODE_確認_完全一致("ALL", TCVAR:ARG:ゆきずりセックス_相手キャラID, ARG)
ENDIF

IF モード行 >= 0
	プレイ内容 '= DT_CELL_GETS("体位モードデータベース", モード行, "モード名")
    RETURNF プレイ内容
ENDIF


;==================================================================================================
@ゆきずりセックス_実行側？(ARG)
#FUNCTION
;-------------------------------------------------
;そのキャラクターが実行側か、対象側かを取得
;1で実行側、0で対象側
;--------------------------------------------------------------------------------------------------
#DIM モード行
#DIMS プレイ内容
;--------------------------------------------------------------------------------------------------
プレイ内容 '= ""

;モード行 = MODE_確認_ターゲット側("ALL", ARG)
;対象側に見つからなければ、実行側を探す
;IF モード行 == -1
;    モード行 = MODE_確認_プレイヤー側("ALL", ARG)
;ENDIF
モード行 = MODE_確認_完全一致("ALL", ARG, TCVAR:ARG:ゆきずりセックス_相手キャラID)
;対象側に見つからなければ、実行側を探す
IF モード行 == -1
    モード行 = MODE_確認_完全一致("ALL", TCVAR:ARG:ゆきずりセックス_相手キャラID, ARG)
ENDIF


IF モード行 >= 0
	プレイ内容 '= DT_CELL_GETS("体位モードデータベース", モード行, "モード名")
	
	IF DT_CELL_GET("体位モードデータベース", モード行, "実行キャラ") == ARG
		;ARGが、このモードの実行側
		RETURNF 1
	ELSEIF DT_CELL_GET("体位モードデータベース", モード行, "対象キャラ") == ARG
		;ARGが、このモードの対象側
		RETURNF 0
	
	ENDIF
ENDIF


