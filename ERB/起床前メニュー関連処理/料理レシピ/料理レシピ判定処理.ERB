@レシピ判定_分類_メイン(分類名)
#FUNCTION
#DIMS 分類名

RETURNF DT_CELL_GETS("所持素材データベース", 使用食材:0, "食材分類", 1) == 分類名


@レシピ判定_分類_サブ(分類名, 必要個数 = 1)
#FUNCTION
#DIMS 分類名
#DIM 必要個数
#DIM 個数
#DIM ループ用

個数 = 0
FOR ループ用, 1, 4
	SIF DT_CELL_GETS("所持素材データベース", 使用食材:ループ用, "食材分類", 1) == 分類名
		個数 ++
	SIF 個数 == 必要個数
		RETURNF 1
NEXT
RETURNF 0


@レシピ判定_分類_全て(分類名, 必要個数 = 1)
#FUNCTION
#DIMS 分類名
#DIM 必要個数
#DIM 個数
#DIM ループ用

個数 = 0
FOR ループ用, 0, 4
	SIF DT_CELL_GETS("所持素材データベース", 使用食材:ループ用, "食材分類", 1) == 分類名
		個数 ++
	SIF 個数 == 必要個数
		RETURNF 1
NEXT
RETURNF 0


@レシピ判定_名前_メイン(アイテム名)
#FUNCTION
#DIMS アイテム名

RETURNF DT_CELL_GETS("所持素材データベース", 使用食材:0, "素材アイテム名", 1) == アイテム名


@レシピ判定_名前_サブ(アイテム名, 必要個数 = 1)
#FUNCTION
#DIMS アイテム名
#DIM 必要個数
#DIM 個数
#DIM ループ用

個数 = 0
FOR ループ用, 1, 4
	SIF DT_CELL_GETS("所持素材データベース", 使用食材:ループ用, "素材アイテム名", 1) == アイテム名
		個数 ++
	SIF 個数 == 必要個数
		RETURNF 1
NEXT
RETURNF 0


@レシピ判定_名前_全て(アイテム名, 必要個数 = 1)
#FUNCTION
#DIMS アイテム名
#DIM 必要個数
#DIM 個数
#DIM ループ用

個数 = 0
FOR ループ用, 0, 4
	SIF DT_CELL_GETS("所持素材データベース", 使用食材:ループ用, "素材アイテム名", 1) == アイテム名
		個数 ++
	SIF 個数 == 必要個数
		RETURNF 1
NEXT
RETURNF 0


@レシピ判定_名前含む_メイン(アイテム名)
#FUNCTION
#DIMS アイテム名

RETURNF STRFINDU(DT_CELL_GETS("所持素材データベース", 使用食材:0, "素材アイテム名", 1), アイテム名) >= 0


@レシピ判定_名前含む_サブ(アイテム名, 必要個数 = 1)
#FUNCTION
#DIMS アイテム名
#DIM 必要個数
#DIM 個数
#DIM ループ用

個数 = 0
FOR ループ用, 1, 4
	SIF STRFINDU(DT_CELL_GETS("所持素材データベース", 使用食材:ループ用, "素材アイテム名", 1), アイテム名) >= 0
		個数 ++
	SIF 個数 == 必要個数
		RETURNF 1
NEXT
RETURNF 0


@レシピ判定_名前含む_全て(アイテム名, 必要個数 = 1)
#FUNCTION
#DIMS アイテム名
#DIM 必要個数
#DIM 個数
#DIM ループ用

個数 = 0
FOR ループ用, 0, 4
	SIF STRFINDU(DT_CELL_GETS("所持素材データベース", 使用食材:ループ用, "素材アイテム名", 1), アイテム名) >= 0
		個数 ++
	SIF 個数 == 必要個数
		RETURNF 1
NEXT
RETURNF 0


@レシピ判定_ランク_メイン(ランク値)
#FUNCTION
#DIM ランク値

RETURNF DT_CELL_GET("所持素材データベース", 使用食材:0, "素材ランク", 1) == ランク値


@レシピ判定_ランク_サブ(ランク値, 必要個数 = 1)
#FUNCTION
#DIM ランク値
#DIM 必要個数
#DIM 個数
#DIM ループ用

個数 = 0
FOR ループ用, 1, 4
	SIF DT_CELL_GET("所持素材データベース", 使用食材:ループ用, "素材ランク", 1) == ランク値
		個数 ++
	SIF 個数 == 必要個数
		RETURNF 1
NEXT
RETURNF 0


@レシピ判定_ランク_全て(ランク値, 必要個数 = 1)
#FUNCTION
#DIM ランク値
#DIM 必要個数
#DIM 個数
#DIM ループ用

個数 = 0
FOR ループ用, 0, 4
	SIF DT_CELL_GET("所持素材データベース", 使用食材:ループ用, "素材ランク", 1) == ランク値
		個数 ++
	SIF 個数 == 必要個数
		RETURNF 1
NEXT
RETURNF 0
