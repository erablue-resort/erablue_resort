;****************************************
;サイド領域の複数人一枚絵を登録するための関数
;****************************************
;・使い方
;「XXXX」を任意の文字列に書き換え、キャラの組み合わせと表示条件を入れる
;「XXXX」は既存の関数と被らないこと
;キャラの組み合わせは既存の関数と被らないこと、同じ組み合わせがないかどうか事前にチェックしてください
;処理的に、組み合わせのキャラが全員同じ場所にいないと条件は読まれません
;画像のサイズはおおよそ650*650程度が限度です
;****************************************

;--------------------------------------------------
;登録データ
;--------------------------------------------------
@複数人一枚絵_登録データ_パチュリー単独(動作モード, 指定キャラ)
#DIMS 動作モード
#DIM REF 指定キャラ

SELECTCASE 動作モード
	CASE "初期登録"
		;キャラの組み合わせを指定、5キャラまで指定可能
		指定キャラ:0 = キャラ検索("[動かない大図書館]パチュリー・ノーレッジ")
	CASE "画像指定"
		IF コマンド履歴:0 == 6
			IF CSTR:(指定キャラ:0):着せ替え服 == ""
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/パチュリー単独_デフォルト_胸愛撫
			ENDIF
		ENDIF
		IF MODE_確認_完全一致("正常位", PLAYER, 指定キャラ) >= 0
			IF CSTR:(指定キャラ:0):着せ替え服 == "全裸"
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/パチュリー単独_全裸_正常位
			ENDIF
		ENDIF
	CASEELSE
		;ここにはこないはずだよ
ENDSELECT

