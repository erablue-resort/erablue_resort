;****************************************
;サイド領域の複数人一枚絵を登録するための関数
;****************************************
;・使い方
;「XXXX」を任意の文字列に書き換え、キャラの組み合わせと表示条件を入れる
;「XXXX」は既存の関数と被らないこと
;キャラの組み合わせは既存の関数と被らないこと、同じ組み合わせがないかどうか事前にチェックしてください
;処理的に、組み合わせのキャラが全員同じ場所にいないと条件は読まれません
;画像のサイズはおおよそ650*650程度が限度です
;****************************************

;--------------------------------------------------
;登録データ
;--------------------------------------------------
@複数人一枚絵_登録データ_ホムラとヒカリ(動作モード, 指定キャラ)
#DIMS 動作モード
#DIM REF 指定キャラ

SELECTCASE 動作モード
	CASE "初期登録"
		;キャラの組み合わせを指定、5キャラまで指定可能
		指定キャラ:0 = キャラ検索("[天の聖杯]ホムラ")
		指定キャラ:1 = キャラ検索("[天の聖杯]ヒカリ")
		; 指定キャラ:2 = キャラ検索("")
		; 指定キャラ:3 = キャラ検索("")
		; 指定キャラ:4 = キャラ検索("")
	CASE "画像指定"
		;表示条件を分岐しつつ画像名を指定する
		;キャラ0とキャラ1が全裸状態で同時パイズリしている場合
		IF MODE_確認_完全一致("パイズリ系", 指定キャラ:0, PLAYER) >= 0 && MODE_確認_完全一致("パイズリ系", 指定キャラ:1, PLAYER) >= 0
			;画像の位置を指定する
			;指定キャラ:0のフォルダに指定
			RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/ホムラ_ヒカリ_ダブルパイズリ
		;キャラ0とキャラ1が全裸状態で同時フェラチオしている場合
		ELSEIF MODE_確認_完全一致("フェラチオ系", 指定キャラ:0, PLAYER) >= 0 && MODE_確認_完全一致("フェラチオ系", 指定キャラ:1, PLAYER) >= 0 && CSTR:(指定キャラ:0):着せ替え服 == "全裸" && CSTR:(指定キャラ:1):着せ替え服 == "全裸"
			;画像の位置を指定する
			;指定キャラ:0のフォルダに指定
			RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/ホムラ_ヒカリ_ダブルフェラチオ
			;画像の位置を指定する
			;指定キャラ:0のフォルダに指定
			ELSEIF コマンド履歴:0 == 208
			RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/ホムラ_ヒカリ_まとめて全裸にする
			;キャラ0とキャラ1が全裸状態で同時に胸愛撫している場合
			;画像の位置を指定する
			;指定キャラ:0のフォルダに指定
			ELSEIF コマンド履歴:0== 6 && CSTR:(指定キャラ:0):着せ替え服 == "全裸" && CSTR:(指定キャラ:1):着せ替え服 == "全裸"
			RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/ホムラ_ヒカリ_同時に胸愛撫
			;画像の位置を指定する
			;指定キャラ:0のフォルダに指定
			;キャラ0とキャラ1が全裸状態で同時に乳首責めしている場合
			ELSEIF コマンド履歴:0 == 7 && CSTR:(指定キャラ:0):着せ替え服 == "全裸" && CSTR:(指定キャラ:1):着せ替え服 == "全裸"
			RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/ホムラ_ヒカリ_同時に乳首責め
			;キャラ0とキャラ1が全裸状態で同時に抱きしめる場合
			;画像の位置を指定する
			;指定キャラ:0のフォルダに指定
			ELSEIF コマンド履歴:0== 11 && CSTR:(指定キャラ:0):着せ替え服 == "全裸" && CSTR:(指定キャラ:1):着せ替え服 == "全裸"
			RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/ホムラ_ヒカリ_まとめて抱きしめる
		ENDIF
	CASEELSE
		;ここにはこないはずだよ
ENDSELECT
