@複数人一枚絵_登録データ_アニラとクビラ(動作モード, 指定キャラ)
#DIMS 動作モード
#DIM REF 指定キャラ

SELECTCASE 動作モード
	CASE "初期登録"
		;キャラの組み合わせを指定
		指定キャラ:0 = キャラ検索("[南南西の守護神]アニラ")
		指定キャラ:1 = キャラ検索("[北北西の守護神]クビラ")
	CASE "画像指定"
		;表示条件を分岐しつつ画像名を指定
		IF MODE_確認_完全一致("パイズリ系", 指定キャラ:0, PLAYER) >= 0 && MODE_確認_完全一致("パイズリ系", 指定キャラ:1, PLAYER) >= 0
			;アニラとクビラが同時にパイズリしている時
			;今回は画像はアニラのフォルダに入れてあるのでアニラのフォルダを指定
			RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/アニラとクビラダブルパイズリ
			;画像パスの文字列はIMAGEPATHと同じ書式
		ENDIF
ENDSELECT


; @複数人一枚絵_登録データ_ユエルと武蔵ちゃん(動作モード, 指定キャラ)
; #DIMS 動作モード
; #DIM REF 指定キャラ

; SELECTCASE 動作モード
; 	CASE "初期登録"
; 		;キャラの組み合わせを指定
; 		指定キャラ:0 = キャラ検索("[千年を探す者]ユエル")
; 		指定キャラ:1 = キャラ検索("[二天一流の女剣豪]新免武蔵守藤原玄信")
; 	CASE "画像指定"
; 		;表示条件を分岐しつつ画像名を指定
; 		IF 現在仕事:(指定キャラ:0):0 == 5 && 現在仕事:(指定キャラ:1):0 == 5 && GETPLACENAME(CFLAG:MASTER:現在マップ種別, CFLAG:MASTER:現在位置) == "訓練場"
; 			;アニラとクビラが同時にパイズリしている時
; 			;今回は画像はアニラのフォルダに入れてあるのでアニラのフォルダを指定
; 			RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/ユエル武蔵バトル
; 			;画像パスの文字列はIMAGEPATHと同じ書式
; 		ENDIF
; ENDSELECT
