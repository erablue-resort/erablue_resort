;****************************************
;サイド領域の複数人一枚絵を登録するための関数
;****************************************
;・使い方
;「XXXX」を任意の文字列に書き換え、キャラの組み合わせと表示条件を入れる
;「XXXX」は既存の関数と被らないこと
;キャラの組み合わせは既存の関数と被らないこと、同じ組み合わせがないかどうか事前にチェックしてください
;処理的に、組み合わせのキャラが全員同じ場所にいないと条件は読まれません
;画像のサイズはおおよそ650*650程度が限度です
;****************************************

;--------------------------------------------------
;登録データ
;--------------------------------------------------
@複数人一枚絵_登録データ_プロメティアとクラリス(動作モード, 指定キャラ)
#DIMS 動作モード
#DIM REF 指定キャラ

SELECTCASE 動作モード
	CASE "初期登録"
		;キャラの組み合わせを指定、5キャラまで指定可能
		指定キャラ:0 = キャラ検索("[相承の錬金術師]プロメティア")
		指定キャラ:1 = キャラ検索("[未完の錬金術師]クラリス")
		; 指定キャラ:2 = キャラ検索("")
		; 指定キャラ:3 = キャラ検索("")
		; 指定キャラ:4 = キャラ検索("")
	CASE "画像指定"
		;表示条件を分岐しつつ画像名を指定する
		;全裸の場合
		IF CSTR:(指定キャラ:0):着せ替え服 == "全裸" && CSTR:(指定キャラ:1):着せ替え服 == "全裸"
			IF MODE_確認_完全一致("パイズリ系", 指定キャラ:0, PLAYER) >= 0 && MODE_確認_完全一致("パイズリ系", 指定キャラ:1, PLAYER) >= 0
				;キャラ0とキャラ1が同時パイズリしている場合
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/プロメティア_クラリス_全裸ダブルパイズリ
			ELSEIF MODE_確認_完全一致("フェラチオ系", 指定キャラ:0, PLAYER) >= 0 && MODE_確認_完全一致("フェラチオ系", 指定キャラ:1, PLAYER) >= 0
				;キャラ0とキャラ1が同時フェラチオしている場合
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/プロメティア_クラリス_全裸ダブルフェラチオ
			ELSEIF MODE_存在判定_完全一致("ボテ腹ズリ", 指定キャラ:0, PLAYER) && MODE_存在判定_完全一致("ボテ腹ズリ", 指定キャラ:1, PLAYER)
				;キャラ0とキャラ1が同時ボテ腹ズリしている場合
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/プロメティア_クラリス_全裸ダブルボテ腹ズリ
			ELSEIF コマンド履歴:0 == 11
				;抱きしめた場合
				IF PREGNANCY_SIZE(指定キャラ:0) >= 2 && PREGNANCY_SIZE(指定キャラ:1) >= 2
					;二人ともボテ状態の場合
					RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/妊娠プロメティア_妊娠クラリス_全裸まとめて抱きしめる
				ELSEIF PREGNANCY_SIZE(指定キャラ:0) >= 2 && PREGNANCY_SIZE(指定キャラ:1) < 2 
					;キャラ0のみがボテ状態の場合
					RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/妊娠プロメティア_クラリス_全裸まとめて抱きしめる
				ELSEIF PREGNANCY_SIZE(指定キャラ:0) < 2 && PREGNANCY_SIZE(指定キャラ:1) >= 2 
					;キャラ1のみがボテ状態の場合
					RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/プロメティア_妊娠クラリス_全裸まとめて抱きしめる
				ELSE
					;二人とも妊娠していない場合
					RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/プロメティア_クラリス_全裸まとめて抱きしめる
				ENDIF
			ENDIF
		ELSEIF コマンド履歴:0 == 208
			;全裸にした場合
			RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/プロメティア_クラリス_まとめて全裸にする
		;全裸ではない場合
		ELSE
			IF MODE_確認_完全一致("パイズリ系", 指定キャラ:0, PLAYER) >= 0 && MODE_確認_完全一致("パイズリ系", 指定キャラ:1, PLAYER) >= 0
				;キャラ0とキャラ1が同時パイズリしている場合
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/プロメティア_クラリス_ダブルパイズリ
			ELSEIF MODE_確認_完全一致("フェラチオ系", 指定キャラ:0, PLAYER) >= 0 && MODE_確認_完全一致("フェラチオ系", 指定キャラ:1, PLAYER) >= 0
				;キャラ0とキャラ1が同時フェラチオしている場合
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/プロメティア_クラリス_ダブルフェラチオ
			ELSEIF MODE_確認_完全一致("手コキ系", 指定キャラ:0, PLAYER) >= 0 && MODE_確認_完全一致("手コキ系", 指定キャラ:1, PLAYER) >= 0
				;キャラ0とキャラ1が同時手コキしている場合
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/プロメティア_クラリス_ダブル手コキ
			ELSEIF MODE_存在判定_完全一致("ボテ腹ズリ", 指定キャラ:0, PLAYER) && MODE_存在判定_完全一致("ボテ腹ズリ", 指定キャラ:1, PLAYER)
				;キャラ0とキャラ1が同時ボテ腹ズリしている場合
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/プロメティア_クラリス_ダブルボテ腹ズリ
			ENDIF
		ENDIF
ENDSELECT
