;****************************************
;サイド領域の複数人一枚絵を登録するための関数
;****************************************
;・使い方
;「XXXX」を任意の文字列に書き換え、キャラの組み合わせと表示条件を入れる
;「XXXX」は既存の関数と被らないこと
;キャラの組み合わせは既存の関数と被らないこと、同じ組み合わせがないかどうか事前にチェックしてください
;処理的に、組み合わせのキャラが全員同じ場所にいないと条件は読まれません
;画像のサイズはおおよそ650*650程度が限度です
;****************************************

;--------------------------------------------------
;登録データ
;--------------------------------------------------
@複数人一枚絵_登録データ_宇崎月と西住しほ(動作モード, 指定キャラ)
#DIMS 動作モード
#DIM REF 指定キャラ

SELECTCASE 動作モード
	CASE "初期登録"
		;キャラの組み合わせを指定、5キャラまで指定可能
		指定キャラ:0 = キャラ検索("[ふしだらな母]宇崎月")
		指定キャラ:1 = キャラ検索("[西住流戦車道・家元]西住しほ")
		; 指定キャラ:2 = キャラ検索("")
		; 指定キャラ:3 = キャラ検索("")
		; 指定キャラ:4 = キャラ検索("")
	CASE "画像指定"
		;表示条件を分岐しつつ画像名を指定する
		;全裸の場合
		IF CSTR:(指定キャラ:0):着せ替え服 == "全裸" && CSTR:(指定キャラ:1):着せ替え服 == "全裸"
			IF MODE_確認_完全一致("パイズリ系", 指定キャラ:0, PLAYER) >= 0 && MODE_確認_完全一致("パイズリ系", 指定キャラ:1, PLAYER) >= 0
				;キャラ0とキャラ1が同時パイズリしている場合
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/月_しほ_全裸ダブルパイズリ
			ELSEIF MODE_確認_完全一致("フェラチオ系", 指定キャラ:0, PLAYER) >= 0 && MODE_確認_完全一致("フェラチオ系", 指定キャラ:1, PLAYER) >= 0
				;キャラ0とキャラ1が同時フェラチオしている場合
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/月_しほ_全裸ダブルフェラチオ
			ELSEIF MODE_存在判定_完全一致("ボテ腹ズリ", 指定キャラ:0, PLAYER) && MODE_存在判定_完全一致("ボテ腹ズリ", 指定キャラ:1, PLAYER)
				;キャラ0とキャラ1が同時ボテ腹ズリしている場合
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/月_しほ_全裸ダブルボテ腹ズリ
			ELSEIF MODE_存在判定_完全一致("キス系", PLAYER, 指定キャラ:0) && MODE_存在判定_完全一致("キス系", PLAYER, 指定キャラ:1)
				;キャラ0とキャラ1が同時キスしている場合
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/月_しほ_全裸ダブルキス
			ELSEIF コマンド履歴:0 == 11
				;抱きしめた場合
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/月_しほ_全裸まとめて抱きしめる
			ENDIF
		ELSEIF コマンド履歴:0 == 208
			;全裸にした場合
			SELECTCASE RAND:2
			CASE 0
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/月_しほ_まとめて全裸にする
			CASE 1
				RESULTS = %CSTR:(指定キャラ:0):画像フォルダ%/月_しほ_まとめて全裸にする1
			ENDSELECT
		ENDIF
ENDSELECT
